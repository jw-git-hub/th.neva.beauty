# ToDo — th.neva.beauty

Рабочий чек-лист проекта. Отмечать по мере выполнения, коммит и пуш после каждого пункта.

- Спека: `docs/superpowers/specs/2026-07-30-th-neva-beauty-design.md`
- План сборки (этапы 0–4): `docs/superpowers/plans/2026-07-30-th-neva-beauty-build.md`

---

## Этап 0–4. Сборка сайта

Ведётся по плану сборки, задача = пункт.

- [x] 1. Каркас репозитория и перенос генератора с vn
- [x] 2. Разбор цены для AggregateOffer (тесты + починка)
- [x] 3. Перенос 204 позиций прайса + парити-тест состава
- [x] 4. Константы бизнеса `site.yml` и параметризация генератора
- [x] 5. Прайс настоящей таблицей
- [x] 6. Изображения: 24 фото под шаблоны vn
- [x] 7. Таксономия из 6 разделов и главная страница
- [x] 8. Контент: 5 услуг раздела «Волосы»
- [x] 9. Контент: 3 услуги раздела «Эпиляция»
- [x] 10. Контент: 4 услуги раздела «Аппаратная косметология»
- [x] 11. Контент: 2 услуги раздела «Косметология»
- [x] 12. Контент: 3 услуги разделов «Коррекция фигуры» и «Макияж»
- [x] 13. Проверки качества страниц `check_content.py` + защита деплоя
- [x] 14. Публикация: robots.txt, GitHub Pages, DNS, боевой домен
      Сайт живёт на `https://th.neva.beauty/`.
      DNS: в зоне `neva.beauty` у reg.ru добавлена CNAME-запись
      `th` → `jw-git-hub.github.io.` Апекс `neva.beauty` записей не имеет,
      соседний поддомен `vn` не затронут.
      GitHub Pages: источник — GitHub Actions, поэтому файл `CNAME` в артефакте
      сам домен не привязывает (это работает только для деплоя из ветки).
      Домен проставлен в настройках Pages через API, `https_enforced` включён.
      Проверено на боевом домене: сертификат Let's Encrypt `CN=th.neva.beauty`
      до 01.11.2026, все 8 ключевых адресов отдают 200, `bundle.min.css` (31 КБ)
      и три JS-бандла грузятся, `canonical` и `og:url` указывают на боевой домен,
      следов `github.io` в разметке нет, в `sitemap.xml` все 23 URL на
      `th.neva.beauty`, несуществующий адрес отдаёт честный 404.
      Редирект `http://` → `https://` отдаёт 301 на все четыре edge-ноды
      GitHub (`185.199.108–111.153`).

---

## Этап 5. Глубокий аудит каждой страницы

Проход по 23 индексируемым страницам после того, как сайт собран и опубликован.
Автопроверки уже в CI, здесь — ручной разбор.

- [x] 15. Прогнать `check_content.py` и разобрать все находки.
      Автопроверки чистые: 25 страниц, 51 тест. Ручной проход поверх них: все
      ассеты из разметки лежат на диске, `canonical` совпадает с реальным путём
      и с `og:url` на всех страницах, дублей title и description нет,
      `/privacy/` и `/404.html` под `noindex` и вне `sitemap.xml` — верно.
      Находка на этап SEO: у раздела `/volosy/` и услуги `/uhod-za-volosami/`
      почти одинаковые title («Уход за волосами на Самуи — …»), страницы будут
      конкурировать друг с другом в выдаче. Развести в задачах 23 и 28.
- [x] 16. Проверить вёрстку на мобильном, планшете и десктопе.
      Прогнаны все 25 страниц на 390, 768 и 1440 px. Найдено и исправлено два
      реальных дефекта:
      1. `/massazh/` разъезжалась на 390 px (страница 406 px при экране 390):
         слово «Профессиональный» в `h2` не переносилось и распирало контейнер.
         Лечится в `base.css`: заголовкам добавлен `overflow-wrap: break-word`,
         а на ширинах до 768 px — `hyphens: auto`, чтобы перенос шёл по слогу.
      2. Таблица целей обработки в `/privacy/` (4 колонки) не помещалась в 390 px
         и тянула горизонтальную прокрутку всей страницы. Обёрнута в
         `.legal-table` с собственной прокруткой, добавлены `thead`/`tbody`.
      После правок горизонтальной прокрутки нет ни на одной странице ни на одной
      из трёх ширин.
      Находки на потом: в прайсе встречается дефис вместо тире («Курс - 10
      процедур») — типографика, править на этапе SEO вместе с текстами; на
      мобильном фото в первом экране идёт перед заголовком и сдвигает его вниз —
      оценить в маркетинговом аудите (задача 46).
- [x] 17. Проверить, что все 204 цены отображаются и совпадают со старым сайтом.
      `check_prices.py`: 204 позиции в `prices.json` — 204 на собранных страницах.
      Сверх этого сверил `prices.json` с Tilda-выгрузкой по значениям, а не
      только по количеству (тесты фиксировали лишь число позиций на услугу):
      разобрал `.t812__pricelist-item` со всех старых страниц и сравнил пары
      «название + цена». 204 против 204, расхождение одно — в названии «Обработка
      после процедуры гель , пудра» убран лишний пробел перед запятой. Все цены
      совпадают дословно.
- [x] 18. Проверить работу меню, дровера, табов прайса и всех кнопок записи.
      Кнопки записи в порядке: на каждой из 25 страниц есть WhatsApp, Telegram
      и Instagram, все ведут на контакты из `site.yml`, посторонних внешних
      ссылок нет, у всех `target="_blank"` проставлен `rel="noopener"`.
      На страницах услуг текст сообщения предзаполнен названием услуги.
      Найдено и исправлено три дефекта:
      1. Табы прайса переключались мышью, но не были связаны с панелями:
         у панелей не было `id` и `role="tabpanel"`, у табов — `aria-controls`,
         и стрелки клавиатуры не работали. Скринридер не понимал, какой список
         цен относится к выбранному разделу. Добавлены связи по `id`, роли
         и переход по стрелкам с переносом фокуса (roving tabindex).
      2. Открытое мобильное меню не забирало фокус: Tab уходил на страницу
         под меню, Escape не возвращал фокус на кнопку. Дровер получил
         `role="dialog"`, ловушку фокуса и возврат фокуса при закрытии.
      3. Списки разделов в шапке не закрывались: открытие соседнего оставляло
         первый открытым, клик мимо и Escape их не убирали. Теперь открыт
         только один, клик вне меню и Escape закрывают.
      Заодно: имена файлов CSS и JS постоянные, поэтому после деплоя браузер
      отдавал вернувшемуся посетителю старые стили и скрипты из кэша — это
      всплыло при проверке правок. В `build.py` добавлен `asset()`, который
      подставляет к ссылке отпечаток содержимого файла (`?v=1a2b3c4d`).
- [x] 19. Проверить JSON-LD в валидаторе Google Rich Results и в Яндекс.Вебмастере.
      Сделано офлайн: сверил разметку в собранном HTML с прайсом по всем услугам —
      `lowPrice`/`highPrice` совпадают с самой дешёвой и самой дорогой
      самостоятельной позицией на всех 16 услугах с ценой, у ботулинотерапии
      ценовой разметки нет намеренно; каждая цена в каталоге офферов есть в прайсе.
      На живом домене прогнаны все 23 индексируемые страницы:
      `validator.schema.org` — 20 страниц, ноль ошибок (на последних трёх Google
      включил ограничение по запросам с этого адреса);
      Google Rich Results — главная, три раздела, страница услуги: везде «элементы
      без ошибок», распознаны «Строки навигации», «Местные компании», «Организация»;
      Яндекс.Вебмастер, валидатор микроразметки — главная, раздел и услуга
      разобраны целиком без ошибок: Organization, BeautySalon, WebSite,
      BreadcrumbList, ItemList, FAQPage, Service с каталогом из 32 офферов,
      Open Graph и Twitter.
      Замечания валидаторов и что с ними:
      1. Google помечает у «Организации» отсутствие необязательных `postalCode`
         и `streetAddress`. Так и задумано: салон работает по записи без открытого
         адреса, точку сообщают в переписке. Появится адрес в Google Business
         Profile (задача 65) — добавим и в разметку.
      2. `FAQPage` ни один валидатор Google не показывает: с 2023 года
         богатые ответы по FAQ Google рисует только сайтам госорганов и медицины.
         Разметка остаётся — её читают Яндекс и ИИ-ответы, но расширенного
         сниппета в Google от неё ждать не нужно.
      Исправлено по ходу: в `ItemList` на страницах разделов звенья были голыми
      адресами без названий (`item: "https://…/tokio-inkarami/"`). Робот видел
      список ссылок, а не список услуг, и чтобы понять состав раздела, должен был
      обойти все страницы. Теперь у звена есть `name` и `url` — как в
      `BreadcrumbList`.
      Находка на этап 8: `lastmod` в `sitemap.xml` проставляется датой сборки всем
      23 адресам сразу, даже если изменились две страницы. Поисковику это говорит,
      что обновился весь сайт. Разобрать в задаче 53.
- [x] 20. Проверить Open Graph превью для главной и одной страницы услуги.
      Проверены все 25 страниц, а не две: `og:url` совпадает с `canonical`,
      ссылки на картинку абсолютные, теги Twitter повторяют Open Graph.
      Найдено и исправлено три дефекта:
      1. Одна обложка `og-cover.jpg` стояла на всех 25 страницах. Ссылку на
         услугу пересылают в WhatsApp чаще, чем главную, и карточка показывала
         общий кадр салона вместо самой процедуры. Нарезано 23 превью
         `og-<slug>.jpg` 1200×630 из тех же исходников Tilda, что и герои
         страниц; на портретных сюжетах центр кропа поднят, иначе горизонтальная
         рамка срезает лицо. Главная, политика и 404 остались на `og-cover`.
      2. Не было `og:image:width`, `height`, `type` и `alt`. Краулер мессенджера
         читает только `<head>`: без размеров он рисует карточку вслепую и при
         первой отправке ссылки часто показывает пустую рамку.
      3. У `/privacy/` и `/404.html` был пустой `og:description` — превью этих
         ссылок в чате приходило без подписи. Описания добавлены.
      Размер превью вынесен в `site.yml`, привязка картинки к странице — в
      `og_image_path()` в `build.py`. В `check_content.py` добавлена проверка
      `check_open_graph`: файл превью существует, его настоящий размер совпадает
      с заявленным в разметке и равен 1200×630. Проверки чистые: 25 страниц.
      Проверено на живом домене (задача 21): все 25 страниц запрошены
      user-agent'ами Facebook, Telegram, WhatsApp и Twitter — карточка у всех
      четырёх одинаковая, теги на месте, `og:url` и `canonical` совпадают
      с адресом запроса, 23 превью отдаются как `image/jpeg` ровно 1200×630.
      Отладчики Facebook и Telegram требуют входа в аккаунт — за заказчиком
      (задача 70); проверять там уже нечего, вход даёт только сброс кэша
      карточки, и он нужен, лишь если ссылку переслали до правок.
- [x] 21. Свести находки аудита в отчёт и исправить.
      Отчёт: `docs/audits/2026-08-03-stage5-pages-audit.md` — чем проверяли,
      что исправлено по задачам 15–20, состояние на живом домене и куда
      переехали незакрытые находки.
      Закрыт хвост задачи 20: карточки ссылок проверены на живом домене
      краулерами мессенджеров.
      Все находки этапа исправлены по ходу задач 15–20, кроме шести, у которых
      есть свой адрес: одинаковые title (23 и 28), типографика прайса (этап 6),
      порядок фото на мобильном (46), адрес в разметке (65), `lastmod`
      в `sitemap.xml` (53), имя Instagram (64).

---

## Этап 6. SEO и GEO по промтам

Отдельная сессия на страницу. В сессии: сначала промт 1 (SEO), затем промт 2 (GEO/AEO)
для той же страницы. Промты в `Данные и промты /`, константы бренда — в `neva-samui-facts.md`.

**Главная и разделы**

- [x] 22. `/` — главная.
      Целевой запрос: «салон красоты на Самуи» / «косметология на Самуи».
      По промту 1 метатеги оказались в порядке и правки не потребовали: title 51
      символ по формуле, description 152, canonical, hreflang `ru` + `x-default`,
      Open Graph с абсолютной картинкой, `<html lang="ru">`, один `h1`,
      семантика и `sitemap`/`robots` на месте. Следов «Avocado» в текстах нет,
      осталась только ссылка на Instagram старого аккаунта — задача 64.
      Исправлено:
      1. Первый экран не был самодостаточным: «Косметология, аппаратные процедуры,
         эпиляция и уход за волосами на острове Самуи» — без бренда и без формата
         приёма. Именно верх страницы чаще всего попадает в ИИ-ответы, поэтому
         абзац переписан так, чтобы читался в отрыве от страницы: «Neva Beauty —
         Koh Samui: … на острове Самуи. Приём по записи, на русском и английском».
      2. Вопросы FAQ были голыми `<summary>`. Ни поиск, ни ИИ не считают их
         заголовками, а значит не видят на странице ответа на вопрос. Теперь
         вопрос — `h3` внутри `summary` (валидно по спецификации, вид не изменился).
         Разметка FAQ вынесена в общий партиал `partials/faq.html.j2` — раньше
         те же восемь строк лежали в трёх шаблонах.
      3. `alt` у шести карточек разделов был просто названием раздела («Волосы»).
         Теперь описывает кадр и называет салон и остров.
      4. Тексты трёх преимуществ и лид блока «Коротко о Neva Beauty» были
         написаны от «мы/здесь» — вне контекста страницы их нельзя процитировать.
         Переписаны с явным названием бренда и острова.
      5. У картинки первого экрана не было `fetchpriority="high"` (в `preload`
         он был, у самой `<img>` — нет).
      6. Заголовки футера были `h4` при `h2` выше по странице — разрыв в иерархии.
      Заперто проверкой: `check_faq` в `check_content.py` сверяет вопросы из
      `FAQPage` с заголовками на странице слово в слово — на всех 25 страницах
      совпадает. Проверено падение проверки на подменённой странице.
      Вёрстка после правок: горизонтальной прокрутки нет на 390, 768 и 1440 px.
      Находка на задачу 46: на 390 px лид первого экрана занимает 6 строк вместо
      трёх, кнопка «Записаться» уезжает ниже — вместе с уже записанным туда
      порядком «фото перед заголовком» это один разговор о первом экране.
- [x] 23. `/volosy/` — Волосы.
      Целевой запрос: зонтичный «волосы / парикмахер на Самуи».
      Закрыта каннибализация из задачи 15: раздел и услуга `/uhod-za-volosami/`
      начинались одинаково («Уход за волосами на Самуи»), поиск показывал по
      запросу одну страницу из пары и выбирал её сам. Раздел уведён на зонтичный
      запрос — title «Волосы на Самуи — парикмахер, окрашивание, восстановление»,
      description переписан с «пять услуг» вместо перечисления, которое начиналось
      теми же тремя словами, что у услуги. Title самой услуги правится в задаче 28,
      предложенная формулировка — «Стрижка и окрашивание волос на Самуи — Neva Beauty».
      Ещё правки: лид первого экрана больше не начинается с «Уход за волосами»
      (это название услуги, а не раздела) и называет формат приёма; вопрос FAQ
      о цене вёл на одну страницу из пяти — теперь на все пять; добавлен вопрос
      о материалах (Davines, LEBEL, Tokio Inkarami) — единственное место, где
      раздел отвечает на «где на Самуи работают с этими марками».
- [x] 24. `/epilyaciya/` — Эпиляция.
      Каннибализации с услугами нет, метатеги и разметка были в порядке. Правки
      по извлекаемости:
      1. Слова «шугаринг» в видимом тексте не было вообще — только в `description`,
         то есть в `<head>`. Ищут при этом чаще «шугаринг», чем «сахарную эпиляцию».
         Синоним добавлен в title, в лид и в анкор ссылки на страницу услуги.
      2. Ответ на «сколько стоит эпиляция на Самуи» был отсылкой «смотрите на
         страницах услуг» — ни одной цифры, цитировать нечего. Теперь в ответе
         реальные цены по зонам и комбинированный сет.
      3. Не было ни слова о противопоказаниях и о солнце — при том, что тема
         завязана на пляж, а у соседнего раздела такой вопрос есть. Добавлен.
      4. Появилась сравнительная таблица трёх методов — контент, которым владеет
         только страница раздела: ни одна страница услуги три метода не сравнивает.
      5. Зонной лексики (бикини, ноги, подмышки) на странице не было — добавлена в лид.
- [x] 25. `/apparatnaya-kosmetologiya/` — Аппаратная косметология.
      Аппараты Morpheus 8, Scarlet S и M22 — сильные поисковые сущности, написание
      по сайту оказалось единым. Единственный разнобой — «RF» в title при «РФ-лифтинге»
      во всех текстах; сведено к «РФ-лифтинг» (просто «РФ» в русском заголовке читается
      как «Российская Федерация»). В `description` не было самой фразы «аппаратная
      косметология» — то есть главного ключа страницы, и Google нечего было
      подсвечивать в сниппете; вдобавок описание начиналось теми же словами, что
      у страницы РФ-лифтинга. Переписано. В лид добавлены консультация,
      противопоказания и перерыв от солнца — для раздела рядом с медициной это плюс
      к E-E-A-T. Добавлен вопрос FAQ о совмещении процедур с пляжем.
      `nav_label: "Аппаратная"` оставлен намеренно: обрубок виден только в верхней
      панели от 1024 px, и там это `<button>` группы, анкор-текстом его краулер
      не читает; полное название стоит в ссылке дропдауна, дровере, хлебных крошках,
      `h1` и `ItemList`, а лишние 12 символов ломают строку панели на 1024 px.

      **Общее по трём задачам.** У всех пяти разделов `h1` был без географии
      («Волосы», «Эпиляция»), хотя приходят на них по геозапросу, а для ИИ такой
      заголовок вне контекста страницы не самодостаточен. Заведён необязательный
      ключ `h1`; проставлен всем пяти разделам сразу, включая ещё не разобранные
      «Косметологию» и «Коррекцию фигуры», — иначе сайт вышел бы наполовину
      с географией в заголовке, наполовину без.
      Иерархия заголовков на всех пяти страницах разделов шла `h1 → h3`: карточки
      услуг — `h3`, а `h2` над ними не было, первый появлялся только у FAQ. Робот
      и скринридер читают это как пропущенный раздел. Добавлен `h2` над сеткой,
      `visually-hidden` — вид страницы не меняется.
      Подпись превью разделов была «Волосы — Neva Beauty — Koh Samui», то есть
      название раздела вместо описания кадра (и три тире подряд в карточке
      мессенджера). Теперь берётся из `image_alt`, который у всех разделов уже был.
      Цены в текстах: заведён плейсхолдер `{price:слаг:Позиция}` — `build.py`
      подставляет цену из `prices.json` до сборки JSON-LD, поэтому цифры попадают
      и в текст, и в `FAQPage`, а разойтись с прайсом не могут. Дублировать цены
      в `content.yml` по-прежнему нельзя.
      Заперто четырьмя проверками в `check_content.py`: `check_heading_order`
      (уровни заголовков без пропусков), `check_title_prefixes` (одинаковое начало
      title у двух страниц), `check_placeholders` (нераскрытый `{price:...}`
      или `{whatsapp}` в тексте), `check_entity_spelling` (единое написание
      аппаратов и методик). Каждая проверена на подменённой странице — падает
      на своём дефекте. По ходу переименована позиция прайса «Нано насадка
      РФ лифтинг» → «РФ-лифтинг», единственное расхождение на сайте.
      Проверки после правок: 25 страниц, 0 проблем; 204 из 204 цен; 51 тест.
      Вёрстка: горизонтальной прокрутки нет ни на одной из трёх страниц ни на
      390, ни на 768, ни на 1440 px; таблица сравнения на 390 px прокручивается
      внутри своего блока.
      Не делалось намеренно: осмысленный `alt` карточек услуг. Шаблон собирает его
      как «Лазерная эпиляция — Эпиляция, Neva Beauty — Koh Samui» — та же болезнь,
      что чинили на главной в задаче 22. Лечится ключом у каждой услуги, а услуги
      разбираются в задачах 28–44; делать 12 из 16 сейчас значило бы оставить сайт
      в половинчатом состоянии и переделывать позже. → задачи 28–44.
      Находки на потом: у страниц нет узла `WebPage`/`CollectionPage` в графе,
      разделы не перелинкованы между собой в теле страницы (→ 50); превью раздела
      и одной из его услуг — один файл у трёх пар разделов, карточки в чате
      неразличимы; на фото SMAS-лифтинга читается марка чужого аппарата
      ULTRAFORMER, а сайт нигде не заявляет, на чём делают SMAS (→ 56 или вопрос
      заказчику); карточки услуг на разделах грузятся `lazy`, хотя первая
      на десктопе попадает в первый экран (→ 55–57); на 390 px у таблицы сравнения
      нет подсказки, что её можно листать (→ 49); в прайсе удаления тату «Тату -
      1,5x1,5 см» — дефис вместо тире и потерянный пробел перед «см» (→ 38).
- [x] 26. `/kosmetologiya/` — Косметология.
      Целевой запрос уведён на зонтичный «косметолог / косметология на Самуи».
      Каннибализация с услугой `/uhodovaya-kosmetologiya/` была та же, что у пары
      «Волосы» в задаче 23: description раздела начинался словами «Уходовая
      косметология» — дословным названием своей же услуги — и дальше перечислял
      тот же набор (чистки, пилинги, Hydra Facial, карбокситерапия), которым
      открывается description услуги. Поиск по такой паре сам выбирает, какую
      страницу показать. Переписаны и title, и description: раздел теперь про
      «косметолога на Самуи» и про две услуги, перечисление процедур осталось
      странице услуги. Заодно слова «косметолог» вообще не было ни в одном
      теге `<head>` на сайте, хотя ищут его не реже, чем «косметологию».
      Правки по извлекаемости:
      1. Лид не назывался самодостаточным: три строки перечисления без консультации,
         противопоказаний и формата приёма — при том, что в разделе есть инъекционная
         услуга, а это YMYL. Добавлены очная консультация, противопоказания,
         перерыв от активного солнца и «приём по записи, на русском и английском».
      2. Ответ на «сколько стоит косметология на Самуи» был отсылкой «смотрите
         на страницах услуг» — ни одной цифры, цитировать нечего (та же болезнь,
         что чинили у эпиляции в задаче 24). Теперь в ответе реальные цены:
         чистки 1000/2500/3500 ฿, Hydra Facial 3000 ฿, карбокситерапия 3500 ฿,
         ботулинотерапия от 100 ฿ за единицу препарата.
      3. Не было вопроса о противопоказаниях и о солнце — у соседнего раздела
         аппаратной косметологии он есть, а здесь тема острее: после чисток
         и пилингов коже нужен перерыв от пляжа. Добавлен.
      4. Ответ «чем уходовая косметология отличается от ботулинотерапии» получил
         навигацию по задаче: акне → глубокая механическая чистка, чувствительная
         кожа → атравматичная. Это то, чем владеет только страница раздела.
      Попутно закрыта тихая ошибка в подстановке `{price:слаг:Позиция}`: указатель
      цен собирался словарём по названию позиции, и одинаковые названия в двух
      секциях прайса молча затирали друг друга — на страницу ушла бы цена последней.
      У РФ-лифтинга таких названий 14 (одни и те же зоны у Morpheus 8 и Scarlet S,
      цены разные), у уходовой косметологии «Чистка спины» женская и мужская.
      Теперь неоднозначное название роняет сборку с внятным сообщением; проверено
      на всех трёх случаях — неизвестная позиция, неоднозначная, обычная.
      Проверки после правок: 25 страниц, 0 проблем; 204 из 204 цен; 51 тест.
      Вёрстка: горизонтальной прокрутки нет на 390, 768 и 1440 px.
      Не делалось намеренно: сравнительная таблица. Уходовая косметология
      и ботулинотерапия не конкурируют — они решают разные задачи, и таблица
      из двух строк дублировала бы уже существующий вопрос FAQ об их различии.
      Находка: превью раздела `og-kosmetologiya.jpg` — тот же файл, что
      `og-botulinoterapiya.jpg` (инъекция), хотя в разделе восемь уходовых позиций
      против одной инъекционной. Это тот же случай, что записан в задаче 25 (→ 56).
      Для задачи 40: title услуги оставить непохожим на раздел — «Уходовая
      косметология на Самуи — чистки и пилинги» подходит, менять не нужно.
- [x] 27. `/korrekciya-figury/` — Коррекция фигуры.
      Целевой запрос уведён на зонтичный «процедуры для тела на Самуи».
      Каннибализация была третьей подряд после задач 23 и 26 и самой прямой:
      description раздела начинался словом «Эндосфера-терапия», и ровно тем же
      словом начинается description самой услуги — то есть поиск видел две
      страницы с одинаковым началом описания и выбирал одну сам. Раздел уведён
      на «процедуры для тела», названия обеих услуг из title и description убраны
      и оставлены страницам услуг.
      Правки по извлекаемости:
      1. Лид был в три строки перечислением — без длительности, противопоказаний
         и формата приёма. Добавлены диапазон сеанса (от 30 минут до двух часов),
         консультация перед первым сеансом и «приём по записи, на русском
         и английском».
      2. Ответ на «сколько стоит коррекция фигуры на Самуи» был отсылкой
         на страницы услуг. Теперь в нём восемь реальных цен: эндосфера-терапия
         800/1000/1500 ฿ по зонам и курс 13 000 ฿, массаж 1500 ฿ и курс 15 000 ฿.
         Каждая цифра привязана к своей позиции прайса отдельным плейсхолдером —
         три вида массажа стоят одинаково, но подставляются по отдельности,
         чтобы текст не разошёлся с прайсом, если одну из цен поменяют.
      3. Не было вопроса о противопоказаниях, хотя у аппаратной методики они есть
         и на странице услуги перечислены. Добавлен.
      4. Не было ответа на туристический вопрос «успею ли пройти курс за поездку» —
         при том, что обе услуги продаются курсами из 10 процедур, а половина
         аудитории на острове проездом. Добавлен вопрос про сроки курса.
      5. Добавлена сравнительная таблица «как проходят и сколько длятся».
         Здесь она уместна, в отличие от «Косметологии» из задачи 26: аппарат
         и руки решают близкие задачи и прямо конкурируют между собой, а сводки
         длительности сеансов (30/45/60/120 минут) на сайте не было нигде.
         Заголовок таблицы намеренно не повторяет вопрос FAQ об их различии —
         первая версия дублировала его слово в слово.
      Проверки после правок: 25 страниц, 0 проблем; 204 из 204 цен; 51 тест.
      Вёрстка: горизонтальной прокрутки нет на 390, 768 и 1440 px; таблица
      на 390 px прокручивается внутри своего блока, как у «Эпиляции».
      Этап 6 по разделам закрыт: главная и все пять разделов разобраны.

**Услуги: Волосы**

- [x] 28. `/uhod-za-volosami/` — Уход за волосами.
      Целевой запрос переведён с размытого «уход за волосами» на денежный
      «стрижка / окрашивание волос на Самуи» — как и было предложено в задаче 23.
      Title «Уход за волосами на Самуи — стрижка и окрашивание» → «Стрижка
      и окрашивание волос на Самуи — Neva Beauty» (50 символов): главный ключ ушёл
      в начало, а бренд появился в конце по формуле промта. Description переписан
      с упором на женские, мужские и детские стрижки — «мужская стрижка на Самуи»
      и «детская стрижка» отдельные запросы, а в `<head>` этих слов не было.
      Заведены два необязательных ключа услуги, `h1` и `image_alt`:
      1. `h1` был «Уход за волосами» — без географии и не тем ключом, по которому
         на страницу приходят. Теперь «Стрижка, окрашивание и уход за волосами
         на Самуи»: ключ впереди, но название из хлебных крошек сохранено, чтобы
         заголовок и крошка не разъезжались. У разделов такой ключ ставился всем
         пятерым разом (задача 25), потому что правка была механическая —
         дописать остров. У услуг она не механическая: каждой странице свой ключ,
         поэтому `h1` проставляется в её собственной задаче, 28–44.
      2. `alt` главного изображения собирался шаблоном как «Уход за волосами —
         Neva Beauty — Koh Samui» — название услуги вместо описания кадра, та же
         болезнь, что чинили на главной в задаче 22 и записали на 28–44 в задаче 25.
         Теперь «Расчёски, щётка, утюжок и фен парикмахера — салон Neva Beauty
         на Самуи». Ключ подхватывают три места сразу: карточка услуги на `/volosy/`,
         карточки «Смотрите также» на четырёх соседних страницах и `og:image:alt`.
      Правки по извлекаемости:
      1. Лид первого экрана не называл ни одной цифры и не говорил о формате приёма.
         Теперь в нём женская стрижка и окрашивание корней с ценами и «приём
         по записи, на русском и английском». Для этого подстановка `{price:...}`
         распространена с ответов FAQ на лид услуги и раздела — цифры по-прежнему
         берутся только из `prices.json` и попадают заодно в `Service.description`.
      2. Ответ на «сколько стоит стрижка и окрашивание на Самуи» был отсылкой
         «смотрите в разделе Цены» — ни одной цифры, цитировать нечего (та же
         болезнь, что чинили у разделов в задачах 24, 26 и 27). Теперь в ответе
         шесть реальных цен: стрижки женская, мужская и детская, окрашивание
         корней, корней с длиной и балаяж.
      3. Добавлены три вопроса, на которые страница отвечала только прайсом:
         про мужские и детские стрижки (отдельная аудитория, в тексте её не было),
         про выход из чёрного (кислотная смывка, 15 000 / 20 000 ฿ — самая дорогая
         позиция салона, а объяснения не было) и про салонные уходы с ценами.
      4. Тексты преимуществ были написаны от «мы» («Учитываем, как солнце…») —
         вне контекста страницы такой абзац не процитировать. Переписаны с явным
         названием салона и острова.
      Попутно: у главного изображения страниц услуг не было `fetchpriority="high"`,
      хотя оно предзагружается в `<head>` — ровно та же потеря, что чинили
      на главной в задаче 22, только на всех шестнадцати страницах услуг.
      И цена в потоке текста разрывалась переносом строки — «2000» на одной
      строке, «฿» на следующей; пробелы внутри подставляемой цены сделаны
      неразрывными, это лечит и уже разобранные страницы разделов.
      Заперто двумя проверками: `check_preloaded_image` (у предзагруженной
      картинки первого экрана есть `fetchpriority="high"` — атрибут легко потерять
      при правке шаблона, а в вёрстке этого не видно) и пятью написаниями
      в `check_entity_spelling` (`Tokyo Inkarami`, `Токио Инкарами`, `Natural Tech`,
      `Lebel`, `Давинес`). Обе проверены на подменённой странице — падают на своём
      дефекте. На подстановку цен заведён `test_render_prices.py`: подстановка,
      неразрывные пробелы, неизвестная и неоднозначная позиция.
      Проверки после правок: 25 страниц, 0 проблем; 204 из 204 цен; 56 тестов.
      Вёрстка: горизонтальной прокрутки нет на 390, 768 и 1440 px.
      Находки на потом: на 390 px кнопка «Записаться» первого экрана стоит
      на 1124 px — ниже сгиба, потому что фото идёт перед заголовком, а лид
      занимает 12 строк; это тот же разговор о первом экране, что записан
      в задаче 46. На фото инструментов читается марка CROC — тот же случай,
      что с ULTRAFORMER в задаче 25 (→ 56).
- [x] 29. `/tokio-inkarami/` — Tokio Inkarami.
      Целевой запрос уведён с марки на потребность: title «Tokio Inkarami на Самуи —
      восстановление волос» → «Восстановление волос на Самуи — Tokio Inkarami».
      Марку ищут те, кто уже про неё знает, а «восстановление волос на Самуи» —
      все остальные, и этот запрос в кластере волос ничем не занят: раздел держит
      «парикмахера», задача 28 — «стрижку и окрашивание», соседи — выпрямление
      и уход Davines. Обе формулировки в title остались, поменялся порядок.
      `h1` был «Tokio Inkarami» — ни географии, ни того, что это вообще такое;
      теперь «Восстановление волос Tokio Inkarami на Самуи».
      `image_alt` вместо «Tokio Inkarami — Neva Beauty — Koh Samui» описывает кадр.
      Правки по извлекаемости:
      1. Ответ на «сколько стоит Tokio Inkarami на Самуи» был отсылкой в раздел
         «Цены» — четвёртая страница подряд с этой болезнью. Теперь три цены
         по длине волос прямо в ответе.
      2. Цена появилась в лиде и в `description`: для этого подстановка
         `{price:...}` распространена с лида ещё и на `seo_desc`, так что цифра
         в сниппете поиска не может разойтись с прайсом. Сниппет с ценой —
         заметный рычаг кликабельности, а руками её туда вписывать нельзя.
      3. Добавлен вопрос «помогает ли Tokio Inkarami волосам после моря и солнца» —
         главный местный вопрос, которого на странице не было. Ответ опирается
         на заявленную производителем защитную плёнку, без обещания результата.
      4. Добавлен вопрос о разнице с персональным уходом Davines. На странице
         Davines встречный вопрос уже есть, а обратной ссылки не было — теперь
         пара страниц отвечает симметрично.
      5. Тексты преимуществ переписаны от бренда вместо «Помогает волосам…».
      Найдено и исправлено противоречие в контенте: преимущество утверждало,
      что состав «не остаётся плёнкой на поверхности», тогда как исходная
      страница старого сайта заявляет ровно обратное — защитную плёнку от солнца
      и морской воды. Формулировка была нашей выдумкой при переносе; теперь
      преимущество говорит про кору и кутикулу, не отрицая плёнку.
      Заперто проверкой `check_doubled_words`: слово, повторённое подряд
      («от от 3000 ฿»). Ровно это вылезло в задаче 28 при подстановке цены —
      в прайсе «от 3000 ฿», а в тексте перед плейсхолдером уже стояло «от».
      Проверка смотрит внутри одного текстового узла, иначе ловит законные
      повторы подписей в меню и дровере. Проверена на подменённой странице.
      Проверки после правок: 25 страниц, 0 проблем; 204 из 204 цен; 56 тестов.
      Вёрстка: горизонтальной прокрутки нет на 390, 768 и 1440 px.
      Находка: позиции Tokio Inkarami стоят и в прайсе `/uhod-za-volosami/`
      (секция «Уходы для волос») с теми же ценами, поэтому одни и те же `Offer`
      попадают в каталоги двух услуг. Прайс повторяет старый сайт, цены сходятся,
      но при финальном аудите разметки стоит решить, оставлять ли дубль (→ 50).
- [x] 30. `/biozavivka-volos/` — Биозавивка волос.
      Целевой запрос: «биозавивка волос на Самуи». Title и так по формуле и не менялся,
      а `h1` был «Биозавивка волос» — без географии; теперь «Биозавивка волос на Самуи».
      `image_alt` не был задан вообще: сборка подставляла «Биозавивка волос —
      Neva Beauty — Koh Samui». Теперь описывает кадр — и на самой странице,
      и в карточках «Похожие услуги» на соседних страницах раздела.
      Правки по извлекаемости:
      1. Ответ на «сколько стоит биозавивка на Самуи» был отсылкой в раздел «Цены» —
         пятая страница подряд с этой болезнью. Теперь в ответе все четыре цены
         по длине волос, с оговоркой, что стрижка входит.
      2. Цена появилась в лиде и в `description`: «от 4500 ฿ вместе со стрижкой».
         Стрижка в цене — сильное отличие от соседних услуг раздела, и в сниппете
         поиска оно теперь видно без перехода на страницу.
      3. Добавлен вопрос «чем биозавивка отличается от химической завивки» — главный
         вопрос по этой услуге, которого на странице не было. Ответ опирается на
         состав из старого сайта (аминокислоты, кератин, шёлк, масла), без обещаний.
      4. Вопрос «сколько держится» дополнен климатом Самуи: форма задана самому
         волосу, поэтому во влажности держится лучше ежедневной укладки. Обещания
         срока нет — 3–6 месяцев остались словами производителя составов.
      5. Тексты преимуществ переписаны от бренда: были «Препараты на основе…»,
         «форму подбирают» — вне контекста страницы такие фразы не цитируются.
      Расширен генератор: плейсхолдер цены научился адресовать строку прайса
      с неуникальным названием — `{price:слаг:Название|Уточнение}`, где уточнение
      это секция прайса или подпись строки. У биозавивки две строки «Длинные волосы
      со стрижкой» (до и ниже поясницы) с разными ценами, и без этого назвать
      их в тексте было нельзя — только отсылкой в прайс. Разделитель `|`, а не
      двоеточие: в названиях позиций двоеточие встречается — «Диспорт (1:3)».
      Заперто тестами: подстановка по секции и по подписи, опечатка в уточнении
      роняет сборку, двоеточие внутри названия не считается разделителем.
      Проверки после правок: 25 страниц, 0 проблем; 204 из 204 цен; 60 тестов.
      Вёрстка: горизонтальной прокрутки нет на 390, 768 и 1440 px.
      Находка на задачу 50: две сквозные проверки уже почти проходят и их стоит
      включить, когда этап 6 закроет все страницы, — «в ответе на вопрос про цену
      есть цифра в ฿» (сейчас падает на 14 неотредактированных страницах и на уже
      сделанной `/apparatnaya-kosmetologiya/`) и «в `h1` есть „Самуи“» (падает
      ровно на страницах, до которых этап 6 ещё не дошёл).
- [x] 31. `/keratinovoe-vypryamlenie-volos/` — Кератиновое выпрямление и ботокс волос.
      Целевой запрос: «кератиновое выпрямление волос на Самуи», вторым — «ботокс
      волос». Обе формулировки в title остались, но сам он был 47 символов и не
      дотягивал до нижней границы, поэтому добавлен бренд: «Кератиновое выпрямление
      и ботокс волос на Самуи — Neva Beauty» (61). `h1` не был задан вообще —
      сборка подставляла название услуги без географии; теперь «Кератиновое
      выпрямление и ботокс волос на Самуи». `image_alt` тоже не был задан: вместо
      «Кератиновое выпрямление и ботокс волос — Neva Beauty — Koh Samui» описывает
      кадр — и на самой странице, и в карточках «Смотрите также» на соседних
      страницах раздела.
      Правки по извлекаемости:
      1. Ответ на «сколько стоит кератиновое выпрямление на Самуи» был отсылкой
         в раздел «Цены» — шестая страница подряд с этой болезнью. Теперь в ответе
         все шесть цен по длине волос от 20 до 70 см и доплата за густоту.
      2. Цена появилась в лиде и в `description` — «от 2000 ฿». Цифру подставляет
         сборка из прайса, поэтому сниппет поиска не может разойтись с таблицей.
      3. Добавлен вопрос «можно ли купаться в море после кератинового выпрямления» —
         главный местный вопрос по этой услуге, которого на странице не было.
         Ответ опирается на факт со страницы: морская и хлорированная вода вымывают
         состав быстрее, значит на Самуи гладкость сойдёт раньше срока.
      4. Добавлен вопрос «что выбрать: выпрямление, восстановление Tokio Inkarami
         или биозавивку». Соседние страницы уже спрашивали про отличие от кератина
         и ссылались сюда, а обратных ссылок не было — теперь тройка страниц
         раздела отвечает симметрично.
      5. Вопрос «сколько держится» получил цифру: 4–6 месяцев со старого сайта,
         с оговоркой, что реальный срок зависит от структуры волос, мытья и ухода.
      6. Тексты преимуществ переписаны от бренда вместо «Волосы спокойнее ведут
         себя…», «Состав заполняет пустоты…». В первое вернулась конкретика
         старого сайта — укладка 5–10 минут вместо «занимает меньше времени».
      Проверки после правок: 25 страниц, 0 проблем; 204 из 204 цен; 60 тестов.
      Вёрстка: горизонтальной прокрутки нет на 390, 768 и 1440 px при всех
      раскрытых ответах FAQ.
- [x] 32. `/davines-naturaltech-tailoring/` — Davines Naturaltech Tailoring.
      Целевой запрос уведён с марки на потребность, как в задаче 29: title
      «Davines Naturaltech Tailoring на Самуи — уход по формуле» →
      «Уход за кожей головы на Самуи — Davines Naturaltech». Марку ищут те, кто
      уже про неё слышал, а «уход за кожей головы на Самуи» в кластере волос
      никем не занят: раздел держит «парикмахера», задача 28 — «стрижку
      и окрашивание», соседи — восстановление и выпрямление. `h1` не был задан
      вообще — сборка подставляла голое «Davines Naturaltech Tailoring» без
      географии и без объяснения, что это; теперь «Уход за кожей головы
      Davines Naturaltech на Самуи». `image_alt` тоже не был задан: теперь
      описывает кадр — флаконы баз и бустеров на полке салона.
      Правки по извлекаемости:
      1. Ответ на «сколько стоит персональный уход Davines на Самуи» был отсылкой
         в раздел «Цены» — седьмая страница подряд с этой болезнью. Теперь цена
         прямо в ответе, в лиде и в `description`.
      2. Единая цена независимо от длины волос — сильное отличие от соседей
         по разделу, где всё считается по длине. Раньше это было спрятано в конце
         ответа про цену, теперь стоит и в сниппете поиска, и в лиде.
      3. Добавлен вопрос «чем персональная формула отличается от готового шампуня
         из магазина» — главное возражение к цене, которого на странице не было.
      4. Добавлен вопрос «подойдёт ли при выпадении волос или перхоти»: это
         реальные запросы, а список задач лежал только в преимуществах, где
         поиск и ИИ ответом на вопрос его не считают.
      5. Вопрос «чем это отличается от салонного ухода» начинался с «это» —
         вне контекста страницы такой заголовок не читается. Теперь в вопросе
         названа услуга.
      6. Ответ про обновление формулы получил местную конкретику: солнце,
         морская вода и постоянная влажность Самуи вместо общего «сезона и воды».
      7. Тексты преимуществ переписаны от бренда вместо «Сначала оценивают…»,
         «Дают рекомендации…».
      Проверки после правок: 25 страниц, 0 проблем; 204 из 204 цен; 60 тестов.
      Вёрстка: горизонтальной прокрутки нет на 390, 768 и 1440 px при всех
      раскрытых ответах FAQ.
      Находка на задачу 50: фото услуги — это набор Davines Naturaltech из
      пронумерованных баз и бустеров, то есть формулу собирают из базы и бустера.
      Механику в текст не выносил: на старом сайте её нет, а выдумывать нельзя.
      Стоит спросить заказчика и, если подтвердит, добавить — это самый
      цитируемый факт про услугу.

**Услуги: Эпиляция**

- [x] 33. `/lazernaya-epilyaciya/` — Лазерная эпиляция.
      Целевой запрос: «лазерная эпиляция на Самуи». Title был 42 символа и не
      дотягивал до нижней границы — добавлен бренд: «Лазерная эпиляция на Самуи —
      диодный лазер, Neva Beauty» (55). `h1` не был задан вообще — сборка
      подставляла голое «Лазерная эпиляция» без географии; теперь «Лазерная
      эпиляция на Самуи». `image_alt` тоже не был задан: вместо «Лазерная
      эпиляция — Neva Beauty — Koh Samui» описывает кадр — манипула диодного
      лазера над кожей ног.
      Правки по извлекаемости:
      1. Ответ на «сколько стоит лазерная эпиляция на Самуи» был отсылкой
         в раздел «Цены» — восьмая страница подряд с этой болезнью. Теперь
         в ответе шесть опорных зон от верхней губы до ног полностью и цена
         сета «подмышки + глубокое бикини», плюс объём прайса: 24 зоны и 8 сетов.
      2. Цена появилась в лиде и в `description` — «от 500 ฿ за зону». Цифру
         подставляет сборка из прайса, поэтому сниппет не разойдётся с таблицей.
      3. Добавлен вопрос «сколько длится одна процедура»: 10–60 минут по зонам
         лежали в преимуществах, где поиск и ИИ ответом на вопрос их не считают.
      4. Добавлен вопрос «больно ли делать лазерную эпиляцию» — главный вопрос
         по услуге, которого на странице не было. Ответ опирается на факт
         со старого сайта: у диодного лазера насадка охлаждает кожу во время вспышки.
      5. Добавлен вопрос «делают ли лазерную эпиляцию мужчинам». В прайсе есть
         спина, плечи, поясница, шея, бакенбарды и межъягодичная зона, но в тексте
         это было только словами «у женщин и мужчин» в лиде.
      6. Вопрос «чем отличается от сахарной» переименован в «чем отличается
         от сахарной и электроэпиляции» и досказан: методы часто сочетают —
         лазер снимает объём, электроэпиляция убирает единичные волоски.
         Раньше страница электроэпиляции ссылалась сюда, а обратно — вскользь.
      7. Вопрос «сколько сеансов нужно» вне контекста страницы не читался —
         теперь в вопросе названа услуга.
      8. Ответ про загар получил туристическую конкретику: если вы на острове
         ненадолго, курс планируют вместе с пляжными днями.
      9. Тексты преимуществ переписаны от бренда вместо «Сертифицированный
         аппарат…», «Параметры подбирают…».
      Проверки после правок: 25 страниц, 0 проблем; 204 из 204 цен; 60 тестов.
      Вёрстка: горизонтальной прокрутки нет на 390, 768 и 1440 px при всех
      раскрытых ответах FAQ.
- [x] 34. `/elektroepilyaciya/` — Электроэпиляция.
      Целевой запрос: «электроэпиляция на Самуи», вторым — «удаление светлых
      и седых волос». Title был 49 символов и не дотягивал до нижней границы;
      вместо бренда в него добавлена третья группа волос, ради которой услугу
      и ищут: «Электроэпиляция на Самуи — светлые, седые и пушковые волосы» (59).
      `h1` не был задан вообще — сборка подставляла голое «Электроэпиляция»
      без географии; теперь «Электроэпиляция на Самуи». `image_alt` тоже не был
      задан: теперь описывает кадр — мастер обрабатывает волос микроэлектродом.
      Правки по извлекаемости:
      1. Ответ на «сколько стоит электроэпиляция на Самуи» описывал устройство
         прайса словами («считают по позициям “до 5”, “до 10”»), но ни одной
         цифры не называл — девятая страница подряд с этой болезнью. Теперь
         в ответе все четыре цены, включая поминутный тариф и цену иглы.
      2. Цена появилась в лиде и в `description` — «от 300 ฿ за 5 волос».
         Единица счёта у услуги нестандартная, поэтому без неё цифра
         в сниппете была бы непонятной.
      3. Добавлен вопрос «подходит ли электроэпиляция для светлых и седых волос» —
         это главное отличие метода и самый вероятный запрос к нему. Раньше факт
         лежал в преимуществах и внутри ответа про сравнение с лазером.
      4. Добавлен вопрос «помогает ли электроэпиляция от вросших волос»:
         реальный запрос, а факт был спрятан в карточке преимущества.
      5. Вопрос «насколько это ощутимо» начинался с «это» — вне контекста
         страницы такой заголовок не читается. Теперь «больно ли делать
         электроэпиляцию», как на странице лазерной эпиляции.
      6. Вопрос «когда нужна электроэпиляция, а когда лазерная» превращён
         в «чем отличается от лазерной и сахарной»: раздел сравнивает все три
         метода, а на этой странице сахарная эпиляция не упоминалась вовсе.
      7. В ответ про курс добавлена туристическая оговорка: если вы на Самуи
         ненадолго, об этом стоит сказать при записи — курс растянут во времени.
      8. Тексты преимуществ переписаны от бренда и услуги вместо «Микроэлектрод
         вводится…», «Метод не зависит…», «На первый визит закладывают…».
      Проверки после правок: 25 страниц, 0 проблем; 204 из 204 цен; 60 тестов.
      Вёрстка: горизонтальной прокрутки нет на 390, 768 и 1440 px при всех
      раскрытых ответах FAQ.
- [x] 35. `/saharnaya-epilyaciya/` — Сахарная эпиляция.
      Целевой запрос: «сахарная эпиляция на Самуи», вторым — «шугаринг на Самуи».
      Title был 46 символов — добавлен бренд: «Сахарная эпиляция на Самуи —
      шугаринг всех зон, Neva Beauty» (59). `h1` не был задан вообще — сборка
      подставляла голое «Сахарная эпиляция» без географии и без разговорного
      названия; теперь «Сахарная эпиляция (шугаринг) на Самуи», обе формулировки
      в одном заголовке. `image_alt` тоже не был задан: теперь описывает кадр —
      тёплая сахарная паста на шпателе.
      Правки по извлекаемости:
      1. Ответ на «сколько стоит сахарная эпиляция на Самуи» был отсылкой
         в раздел «Цены» — десятая страница подряд с этой болезнью. Теперь
         в ответе восемь опорных зон и цена комплекса из трёх зон.
      2. Цена появилась в лиде и в `description` — «от 400 ฿». Цифру подставляет
         сборка из прайса, поэтому сниппет не разойдётся с таблицей.
      3. Срок гладкости получил цифру: до 4 недель со старого сайта, с оговоркой,
         что реальный интервал зависит от зоны и скорости роста. Раньше в ответе
         стояло размытое «обычно несколько недель».
      4. Добавлен вопрос «больно ли делать шугаринг» — главный вопрос по услуге,
         которого на странице не было. Ответ опирается на факты со страницы:
         паста тёплая, а не горячая, и снимается по росту волос.
      5. Добавлен вопрос «можно ли делать шугаринг в зоне глубокого бикини
         и на лице»: чувствительные зоны — причина выбрать метод, но факт лежал
         в карточке преимущества, где поиск и ИИ ответом его не считают.
      6. Вопрос про пляж выделен из общего ответа о подготовке в отдельный
         «можно ли купаться и загорать после сахарной эпиляции» — на острове это
         решает, на какой день записываться.
      7. Сравнение с лазером дополнено электроэпиляцией: раздел сравнивает
         все три метода, а страница упоминала только лазер.
      8. Тексты преимуществ переписаны от бренда и услуги вместо «Сахар, вода
         и лимонный сок…», «Метод применяют…».
      Проверки после правок: 25 страниц, 0 проблем; 204 из 204 цен; 60 тестов.
      Вёрстка: горизонтальной прокрутки нет на 390, 768 и 1440 px при всех
      раскрытых ответах FAQ.
      Находка на задачу 50: сахарная эпиляция на старом сайте была страницей-сиротой
      (нет в меню). На новом сайте она есть и в меню, и в разделе «Эпиляция»,
      и в блоках «Смотрите также» у двух соседних услуг — пункт промта закрыт.

**Услуги: Аппаратная косметология**

- [x] 36. `/igolchatyj-rf-lifting/` — Игольчатый РФ-лифтинг.
      Целевой запрос: «игольчатый РФ-лифтинг на Самуи», вторым — названия аппаратов
      «Morpheus 8» и «Scarlet S». Title 54 символа и по формуле — не трогал.
      `h1` не был задан: сборка подставляла «Игольчатый РФ-лифтинг» без географии,
      теперь «Игольчатый РФ-лифтинг на Самуи». `image_alt` тоже не был задан —
      теперь описывает кадр: насадка с микроиглами на лбу.
      Правки по извлекаемости:
      1. Ответ на «сколько стоит РФ-лифтинг на Самуи» был отсылкой в раздел «Цены».
         Теперь в ответе опорные зоны по обоим аппаратам и цена комбинации; цифры
         подставляет сборка из прайса, поэтому разойтись с таблицей они не могут.
         Названия зон в прайсе повторяются у Morpheus 8 и Scarlet S, поэтому
         плейсхолдеры уточнены секцией — `{price:...:Лицо полностью|Scarlet S}`.
      2. Цена появилась в лиде и в `description` — «от 10000 ฿» за лицо целиком.
      3. Главный вопрос выбора «Morpheus 8 или Scarlet S» получил цифры: 10000 ฿
         против 15000 ฿ за лицо. Раньше ответ говорил «отличаются стоимостью»,
         не называя разницы, — цитировать было нечего.
      4. Добавлен вопрос «с какими задачами работает» — показания (овал, морщины,
         поры, следы акне, рубцы, растяжки) на новом сайте потерялись, хотя
         на старом были: без них страница не отвечает на «поможет ли мне».
      5. Добавлен вопрос «больно ли» — по услуге с иглами это первый вопрос,
         на странице его не было. Ответ опирается на факт со старого сайта:
         в аппаратах предусмотрено охлаждение.
      6. Добавлен вопрос о противопоказаниях: у радиочастотной процедуры список
         шире обычного (импланты, кардиостимулятор), а страница о нём молчала —
         для YMYL-темы это минус к доверию.
      7. Ответ «сколько нужно процедур» получил цифры вместо «нескольких месяцев»:
         кожа меняется 3–6 месяцев после курса, заявленный срок результата —
         1,5–3 года, обе цифры со старого сайта, с оговоркой об индивидуальности.
      8. Вопрос о коже после процедуры дополнен пляжем — на острове это решает,
         на какой день записываться.
      9. Тексты преимуществ переписаны от бренда и услуги вместо «Иглы доставляют…»,
         «Кожа перестраивается…»; в них добавлены прогрев до 60 °C и срок 3–6 месяцев.
      Проверки после правок: 25 страниц, 0 проблем; 204 из 204 цен; 60 тестов.
- [x] 37. `/smas-lifting/` — SMAS-лифтинг.
      Целевой запрос: «SMAS-лифтинг на Самуи», вторым — «ультразвуковая подтяжка
      лица». Title был 47 символов — добавлен бренд: «SMAS-лифтинг на Самуи —
      ультразвуковая подтяжка, Neva Beauty» (60). `h1` не был задан, сборка
      подставляла голое «SMAS-лифтинг»: теперь «Ультразвуковой SMAS-лифтинг
      на Самуи» — и география, и метод в одном заголовке, потому что «SMAS»
      без пояснения понятен не всем, кто ищет подтяжку лица. `image_alt`
      не был задан: теперь описывает кадр — насадка у скулы.
      Правки по извлекаемости:
      1. Ответ на «сколько стоит SMAS-лифтинг на Самуи» был отсылкой в раздел
         «Цены». Теперь в ответе все шесть зон и акция на фулфейс с шеей
         и декольте — с ценами из прайса.
      2. Цена появилась в лиде и в `description` — «от 10000 ฿» за зону.
      3. В лид добавлена длительность сеанса (30–90 минут): факт лежал
         в карточке преимущества, где ответом на «сколько длится» он не считается.
      4. Добавлен вопрос «кому подходит SMAS-лифтинг» — показаний на странице
         не было вообще, страница не отвечала на «поможет ли мне». В ответе есть
         и граница метода: при выраженных изменениях аппаратная подтяжка
         не заменяет хирургию.
      5. Добавлен вопрос «больно ли» — по процедуре, которую сравнивают
         с операцией, это первый вопрос.
      6. Добавлен вопрос о противопоказаниях: страница о них молчала.
      7. Ответ о результате получил цифры вместо «нескольких месяцев»: коллаген
         вырабатывается до 6 месяцев, заявленный срок результата — 1,5–3 года.
         Обе цифры со старого сайта, с оговоркой об индивидуальности.
      8. Добавлен вопрос «сколько длится и сколько нужно сеансов» — на странице
         не было ответа на второй вопрос, а он отличает SMAS от курсовых процедур.
      9. Тексты преимуществ переписаны с явным названием услуги и салона.
      Проверки после правок: 25 страниц, 0 проблем; 204 из 204 цен; 60 тестов.
- [x] 38. `/udalenie-tatuirovok-lazerom/` — Удаление тату и татуажа.
      Целевой запрос: «удаление тату на Самуи», вторым — «удаление татуажа
      лазером». Title был 40 символов — добавлен бренд: «Удаление тату и татуажа
      лазером на Самуи — Neva Beauty» (54). `h1` не был задан: сборка подставляла
      «Удаление тату и татуажа» без географии и без слова «лазером», хотя ищут
      именно лазерное удаление; теперь «Удаление тату и татуажа лазером на Самуи».
      `image_alt` не был задан — теперь описывает кадр: манипула лазера
      над бровью и защитные очки.
      Правки по извлекаемости:
      1. Ответ на «сколько стоит удаление тату на Самуи» был отсылкой в раздел
         «Цены». Теперь в ответе шесть размеров рисунка и три позиции татуажа
         с ценами из прайса и явной оговоркой, что цена за сеанс, а не за курс.
      2. Цена появилась в лиде и в `description` — «от 1000 ฿» за сеанс.
      3. Ответ «сколько сеансов» получил цифру: заметное осветление обычно
         после двух-трёх сеансов (факт со старого сайта). Раньше в ответе
         не было ни одного числа.
      4. Добавлен вопрос «какие цвета пигмента убирает лазер» — цветной пигмент
         был назван только в карточке преимущества, где ответом он не считается.
         В ответе заодно снято завышенное ожидание: полное исчезновение следа
         заранее не гарантируют, цветной рисунок требует больше сеансов.
      5. Добавлен вопрос «можно ли удалять тату с загаром и загорать между
         сеансами» — на острове это решает, стоит ли начинать курс в отпуске.
      6. Тексты двух преимуществ переписаны с явным названием услуги и салона.
      Проверки после правок: 25 страниц, 0 проблем; 204 из 204 цен; 60 тестов.
      Находка на задачу 55: проверить вёрстку трёх страниц этапа на 390 px —
      в этой сессии окно браузера не удалось уменьшить (resize не применялся),
      а правки текстовые, новых блоков и таблиц не добавлялось.
- [x] 39. `/fotoomolozhenie-m22/` — Фотоомоложение M22.
      Целевой запрос: «фотоомоложение на Самуи», вторым — «удаление пигментных
      пятен на лице». Title был 50 символов без бренда — стал «Фотоомоложение M22
      на Самуи — пигментация и сосуды, Neva Beauty» (63). `h1` не был задан:
      сборка подставляла «Фотоомоложение M22» без географии и без слова «лица»,
      хотя лицо — основная зона и главный запрос; теперь «Фотоомоложение лица M22
      на Самуи». `image_alt` не был задан — теперь описывает кадр: манипула M22
      у лица клиентки в защитных очках.
      Правки по извлекаемости:
      1. Ответ на «сколько стоит фотоомоложение на Самуи» был отсылкой в раздел
         «Цены» — ни одной цифры. Теперь в ответе все пять позиций: лицо 10000 ฿,
         шея 7000 ฿, декольте 8000 ฿ и обе комбинации. Заодно видно, что комбинация
         действительно дешевле: лицо с шеей 14000 ฿ против 17000 ฿ по отдельности.
      2. Цены появились в лиде и в `description` — раньше в `<head>` не было ни
         одной суммы.
      3. Добавлен вопрос «чем фотоомоложение отличается от РФ-лифтинга и SMAS».
         Три аппаратные процедуры раздела конкурируют между собой, и сравнение
         было только на странице раздела — сама услуга на вопрос не отвечала.
         В ответе ссылки на обе соседние страницы.
      4. Добавлен вопрос о болезненности и восстановлении — его не было, хотя
         это второй вопрос после цены у любой аппаратной процедуры. Формулировка
         без обещаний: «короткая вспышка тепла», а не «безболезненно», как
         на старом сайте.
      5. Добавлен вопрос о противопоказаниях — у соседних SMAS и РФ-лифтинга он
         есть, здесь его не было, при том что светочувствительность и свежий загар
         для M22 критичнее.
      6. Ответ про загар усилен: свежий загар теперь назван поводом перенести
         сеанс, а не просто «меняет тактику».
      7. Тексты трёх преимуществ переписаны с явным названием услуги и салона.
      Проверки после правок: 25 страниц, 0 проблем; 204 из 204 цен; 60 тестов.

**Услуги: Косметология**

- [x] 40. `/uhodovaya-kosmetologiya/` — Уходовая косметология.
      Целевой запрос: «чистка лица на Самуи» — его ищут заметно чаще, чем
      «уходовую косметологию», а точной фразы «чистка лица» на странице не было
      нигде в `<head>`. Title остался непохожим на раздел, как записано в задаче
      26, но «чистки и пилинги» заменены на «чистка лица и пилинги» (54 символа).
      `h1` не был задан: сборка подставляла «Уходовая косметология» без географии
      и без слова «чистка»; теперь «Уходовая косметология и чистка лица на Самуи».
      `image_alt` не был задан — теперь описывает кадр: нанесение уходовой маски
      кистью.
      Правки по извлекаемости:
      1. Ответ на «сколько стоит чистка лица на Самуи» перечислял виды чисток
         и отправлял в раздел «Цены» — ни одной цифры при том, что это самый
         частый вопрос страницы. Теперь в ответе пять видов чистки с ценами
         (1000–3500 ฿) и чистка спины отдельно, женская 4000 ฿ и мужская 5000 ฿.
      2. Ответ про Hydra Facial и карбокситерапию тоже был без цифр — теперь
         3000 ฿ за базовую программу, +500 ฿ за каждую дополнительную и 3500 ฿
         за карбокситерапию.
      3. Цена появилась в лиде и в `description` — «от 1000 ฿».
      4. Добавлен вопрос «чем чистка отличается от пилинга». На странице восемь
         позиций чисток и три позиции пилингов, а разницы между ними страница
         не объясняла — при этом выбирают между ними именно так.
      5. Добавлен вопрос «можно ли загорать после чистки лица». У раздела
         «Косметология» такой вопрос есть с задачи 26, у самой услуги не было,
         хотя перерыв от пляжа нужен именно после чисток и пилингов.
      6. Добавлен вопрос о противопоказаниях — его не было ни в каком виде.
      7. Тексты трёх преимуществ переписаны с явным названием салона; в описание
         атравматичной чистки Christina вернулись восемь этапов из прайса.
      Проверки после правок: 25 страниц, 0 проблем; 204 из 204 цен; 60 тестов.
- [x] 41. `/botulinoterapiya/` — Ботулинотерапия.
      Целевой запрос: «ботулинотерапия на Самуи», вторым — «уколы ботокса
      на Самуи». Title был 54 символа без бренда: «коррекция мимических морщин»
      сжата до «мимические морщины», освободившееся место занял бренд (58).
      `h1` не был задан — сборка подставляла «Ботулинотерапия» без географии,
      хотя приходят на страницу по геозапросу; теперь «Ботулинотерапия на Самуи».
      `image_alt` не был задан — теперь описывает кадр: шприц у зоны лба
      в руке косметолога в перчатках.
      Правки по извлекаемости:
      1. Ответ на «сколько стоит ботулинотерапия на Самуи» отправлял в раздел
         «Цены» — ни одной цифры. Теперь в ответе все пять препаратов с ценой
         за единицу: Диспорт 100 ฿, Набота, Ботулакс и Иннотокс по 120 ฿,
         Рефайнекс 150 ฿. Оговорка про итог по числу единиц сохранена — это
         не цена процедуры, и подменять её нельзя.
      2. Цена «от 100 ฿ за единицу» появилась в лиде и в `description`.
      3. Добавлен вопрос «это то же самое, что уколы ботокса». Слова «ботокс»
         на странице не было вообще, хотя запрос идёт именно с ним. В ответе
         честно: Ботокс — одна из торговых марок, в салоне пять других
         препаратов. Заодно снята путаница с «ботоксом волос» — на сайте есть
         страница с таким названием, и это другая процедура.
      4. Добавлен вопрос о длительности и восстановлении: 15–30 минут
         и отсутствие реабилитации были только в карточке преимущества,
         где ответом они не считаются.
      5. Ответ «когда проявляется эффект» получил цифры со старого сайта:
         3–5 дней до первых изменений, около двух недель до максимума,
         заявленный срок до 6 месяцев — раньше в ответе не было ни одного числа.
      6. Добавлен вопрос «когда делать процедуру, если я в отпуске». Для
         туристической аудитории он решает больше, чем цена: процедура за день
         до вылета результата в поездке не даст.
      7. Тексты двух преимуществ переписаны с явным названием услуги и салона.
      Проверки после правок: 25 страниц, 0 проблем; 204 из 204 цен; 60 тестов.
      Находка на задачу 55: вёрстку трёх страниц этапа на 390 px в этой сессии
      не проверяли — правки текстовые, новых блоков и таблиц не добавлялось.

**Услуги: Коррекция фигуры и Макияж**

- [x] 42. `/endosfera-terapiya/` — Эндосфера-терапия.
      Целевой запрос: «эндосфера-терапия на Самуи», вторым — «аппаратный массаж
      на Самуи». Title был 51 символ без бренда: «аппаратный массаж тела» сжат
      до «аппаратный массаж» (слово «тела» и так закреплено за страницей массажа),
      освободившееся место занял бренд (59). `h1` не был задан — сборка подставляла
      «Эндосфера-терапия» без географии; теперь «Эндосфера-терапия на Самуи».
      `image_alt` не был задан — теперь описывает кадр: манипула аппарата
      в руках косметолога прорабатывает бедро.
      Исправлена неверная фраза: и карточка преимущества, и ответ FAQ утверждали,
      что курс «выгоднее, чем те же процедуры поштучно». Для всего тела это правда
      (10 × 1500 ฿ = 15 000 ฿ против 13 000 ฿ за курс), но для живота и ног —
      наоборот: десять сеансов поштучно стоят 8000 ฿ и 10 000 ฿, то есть дешевле
      курса. Формулировка сужена до всего тела.
      Правки по извлекаемости:
      1. Ответ на «сколько стоит эндосфера-терапия на Самуи» перечислял зоны
         и отправлял в раздел «Цены» — ни одной цифры. Теперь в ответе все четыре
         позиции с длительностью: всё тело 1500 ฿ за час, ноги с бёдрами 1000 ฿
         за 45 минут, живот 800 ฿ за 30 минут, курс из 10 сеансов 13 000 ฿.
      2. Цены появились в лиде первого экрана и в `description` — «от 800 ฿»
         и курс 13 000 ฿.
      3. Добавлен вопрос «сколько длится сеанс». Длительности зон лежали только
         в карточке преимущества, где ответом они не считаются.
      4. Добавлен вопрос «как проходит процедура и больно ли это» — самый частый
         вопрос по аппаратным методикам, а на странице не было ни слова о том,
         что происходит во время сеанса.
      5. Добавлен вопрос «успею ли пройти курс, если я на Самуи в отпуске».
         Курс из 10 сеансов продаётся туристической аудитории, а сроков страница
         не называла. Ответ честный: интервал подбирают на консультации, цифры
         не выдуманы.
      6. Тексты трёх преимуществ переписаны с явным названием услуги и салона.
      Заперто проверкой: в `check_entity_spelling` добавлено написание
      «Эндосфера терапия» без дефиса — так писал старый сайт, и для поиска это
      другая сущность. Проверено падение проверки на подменённой странице.
      Проверки после правок: 25 страниц, 0 проблем; 204 из 204 цен; 60 тестов.
- [x] 43. `/massazh/` — Профессиональный массаж.
      Целевой запрос: «массаж на Самуи», вторым — «антицеллюлитный массаж
      на Самуи». Title начинался со слова «Профессиональный», то есть главный ключ
      стоял третьим словом, а бренда не было вовсе: «Профессиональный массаж
      на Самуи — тело и лицо» → «Массаж на Самуи — тело, лицо, антицеллюлитный,
      Neva Beauty» (58). Слово «антицеллюлитный» в `<head>` не было — это отдельный
      денежный запрос. `h1` не был задан — сборка подставляла «Профессиональный
      массаж» без географии; теперь «Профессиональный массаж на Самуи»: ключ на месте,
      название из хлебных крошек сохранено. `image_alt` не был задан — теперь
      описывает кадр: руки мастера прорабатывают спину.
      Исправлена неточность: карточка преимущества и ответ FAQ говорили, что курс
      «выгоднее поштучной оплаты». По прайсу курс стоит ровно столько же —
      10 × 1500 ฿ = 15 000 ฿; выгода не в цене, а в одиннадцатом массаже в подарок.
      Так теперь и написано.
      Правки по извлекаемости:
      1. Ответ на «сколько стоит массаж на Самуи» отправлял в раздел «Цены» —
         ни одной цифры. Теперь в ответе три вида массажа с длительностью
         и ценой (1500 ฿ за каждый) и курс из 10 процедур за 15 000 ฿.
      2. Цены и длительности появились в лиде первого экрана и в `description`.
      3. Добавлен вопрос «что входит в курс из 10 процедур» — курс есть в прайсе,
         но страница не объясняла ни его состав, ни в чём его смысл.
      4. Добавлен вопрос о противопоказаниях: у раздела «Коррекция фигуры» он есть
         с задачи 27, у самой услуги не было. В ответе отдельно про обгоревшую
         на солнце кожу — на острове это самая частая причина перенести сеанс.
      5. Тексты трёх преимуществ переписаны с явным названием услуги и салона.
      Проверки после правок: 25 страниц, 0 проблем; 204 из 204 цен; 60 тестов.
- [x] 44. `/permanentnyj-makiyazh/` — Перманентный макияж.
      Целевой запрос: «перманентный макияж на Самуи», вторым — «татуаж бровей
      на Самуи». Слова «татуаж» на странице не было вообще, хотя ищут этим словом
      не реже: единственное его упоминание на сайте — в названии соседней услуги
      «удаление тату и татуажа», то есть по запросу «татуаж бровей» страница
      отдавала показ странице про удаление. Синоним добавлен в title, `h1`, лид
      и в отдельный вопрос FAQ. Title был 51 символ без бренда: «брови, губы,
      стрелки» заменены на «татуаж бровей», освободившееся место занял бренд (57);
      губы и стрелки остались в `description`. `h1` не был задан — сборка
      подставляла «Перманентный макияж» без географии; теперь «Перманентный макияж
      и татуаж на Самуи». `image_alt` не был задан — теперь описывает кадр: мастер
      в перчатках прорисовывает бровь манипулой.
      Правки по извлекаемости:
      1. Ответ на «сколько стоит перманентный макияж на Самуи» перечислял зоны
         и отправлял в раздел «Цены» — ни одной цифры. Теперь в ответе все 11
         позиций прайса парами «первая процедура и коррекция»: брови, волосковая
         техника, губы и стрелки — 6000 и 3000 ฿, межресничное пространство —
         5000 и 2500 ฿, обновление до года — 5000 ฿. Каждая цифра привязана
         к своей позиции прайса отдельным плейсхолдером, как в задаче 27: четыре
         зоны стоят одинаково, но подставляются по отдельности, чтобы текст
         не разошёлся с прайсом, если одну из цен поменяют.
      2. Цены появились в лиде первого экрана и в `description` — 6000 ฿
         за процедуру и 3000 ฿ за коррекцию.
      3. Добавлен вопрос «перманентный макияж и татуаж — это одно и то же».
         Заодно снята путаница с соседней услугой: удаление старого татуажа
         лазером — другая процедура, и на неё стоит ссылка.
      4. Ответ «сколько держится» не называл ни одного срока — «держится месяцами».
         Теперь в нём заявленный старым сайтом диапазон 1,5–3 года с оговоркой
         про тип кожи, уход и солнце, и цена обновления до года — 5000 ฿.
      5. Добавлен вопрос «когда делать перманент, если я в отпуске на Самуи».
         Для туристической аудитории он решает больше цены: первые дни нельзя
         на пляж, а коррекция — только через 1–2 месяца, то есть в короткий
         отпуск вторую встречу здесь не пройти. Раньше оба факта лежали
         на странице порознь и вывод из них никто не делал.
      6. Тексты трёх преимуществ переписаны с явным названием услуги и салона.
      Проверки после правок: 25 страниц, 0 проблем; 204 из 204 цен; 60 тестов.
      Этап 6 закрыт: главная, пять разделов и все 16 услуг разобраны по обоим
      промтам.
      Находка на задачу 50: срок стойкости 1,5–3 года взят из копирайта старого
      сайта — это единственное место во всём контенте, где число пришло
      из рекламного текста, а не из прайса или из общей практики. Стоит
      подтвердить его у мастера на финальном аудите.

---

## Этап 7. Маркетинговый аудит

- [x] 45. Разобрать путь клиента: как посетитель доходит от входа до записи.
      Разобрано на живом домене в мобильном окне (вьюпорт 500×723) — трафик
      салона в основном мобильный, а весь этап 6 затачивал под поиск именно
      страницы услуг, поэтому вход считаю не с главной, а с любой из 23 страниц.
      Три сценария входа дают три разных пути:
      1. **Главная → раздел → услуга → запись.** Работает: в первом экране есть
         «Записаться», под ним контактная полоса, ниже шесть карточек разделов
         и блок «Частые процедуры» с ценами. Единственный путь, где клиент
         видит цену до того, как откроет страницу услуги.
      2. **Поиск → страница услуги → запись.** Тоже работает: кнопка в первом
         экране, прайс на 3.3 экрана, блок записи на 8.0.
      3. **Поиск → страница раздела → запись.** Проваливается. В шаблоне
         `category.html.j2` кнопки записи в шапке страницы нет вообще: первая
         точка записи — блок «Запишитесь на консультацию» на 5.5 экрана
         (замер на `/epilyaciya/`, вся страница 7.9 экрана). Так устроены все
         пять страниц разделов.
      Находки по самому пути:
      1. Карточки услуг на странице раздела — только фото и название, ни цены,
         ни строки описания (`related-card`, тот же компонент в «Смотрите также»).
         Выбирая между тремя видами эпиляции, клиент открывает каждую страницу.
      2. Выбрать внутри раздела помогает таблица сравнения, но она есть только
         у `/epilyaciya/` и `/korrekciya-figury/`. Нет там, где выбор сложнее
         всего: `/volosy/` — 5 услуг, `/apparatnaya-kosmetologiya/` — 4,
         `/kosmetologiya/` — 2.
      3. Контактная полоса (остров, «Приём по записи», WhatsApp) есть только
         на главной. Клиент, пришедший из поиска на услугу или раздел, нигде
         на странице не видит ни локации, ни формата приёма — только в тексте.
      4. Записаться можно исключительно через мессенджеры: ссылки `tel:` на
         сайте нет ни одной, ссылки на карту — тоже. Телефон в `site.yml` есть,
         но уходит только в JSON-LD. Для клиента, который хочет позвонить или
         посмотреть, далеко ли ехать, пути нет.
      5. Обратный ход неравномерный: с услуги в раздел ведут хлебные крошки
         и «Смотрите также» (`related` заполнен у всех 17 услуг), а с раздела
         в соседний раздел — только через меню в шапке.
      Правки — задача 49, вместе с находками 46–48.
- [x] 46. Оценить работу призывов к действию и заметность кнопок записи.
      Замерял позиции всех ссылок записи в экранах прокрутки, мобильный
      вьюпорт 500×723, живой домен. Считал только видимые ссылки — те, что
      в закрытом дровере, клиенту не показаны.
      1. **В первом экране кнопки записи нет ни на одной странице.** На главной
         «Записаться» стоит на 1.26 экрана, на странице услуги — на 1.49.
         Причина в порядке блоков: у `.hero__media` и `.svc-hero__media`
         до 768 px стоит `order:-1` (`home.css:14`, `service.css:12`), фото
         поднимается над заголовком и сдвигает вниз всё остальное. Это та же
         находка, что записана сюда из задач 16 и 22 («фото перед заголовком»,
         «лид в 6 строк вместо трёх») — корень у них один.
      2. **Кнопка в шапке на мобильном скрыта.** `.site-header__cta{display:none}`
         и показ только с 1024 px (`components.css:88–89`). Шапка при этом
         `position:sticky` и едет за клиентом всю страницу — держит логотип
         и бургер, но не действие. На мобильном запись из шапки — это три шага:
         бургер → дровер → «Записаться».
      3. **Длинные участки без единого призыва.** Главная — 13.2 экрана,
         между контактной полосой (1.6) и блоком записи (10.1) провал в
         8.5 экрана: туда попали все шесть разделов, преимущества, «Частые
         процедуры» с ценами и весь FAQ. Страница услуги — 12.6 экрана,
         провал 6.4 экрана между кнопкой первого экрана (1.49) и блоком
         записи (7.89).
      4. **У прайса нет кнопки.** На `/lazernaya-epilyaciya/` блок цен занимает
         3.26–5.87 экрана, и ни внутри него, ни сразу после нет ссылки на
         запись — ближайшая на 7.89. Момент, когда клиент увидел цену и решил,
         не поддержан ничем: надо либо листать два экрана вниз через FAQ, либо
         возвращаться наверх.
      5. **Призывы копятся в хвосте.** На главной 12 из 14 ссылок записи лежат
         за отметкой 10 экранов — блок «Запишитесь», ссылки в ответах FAQ
         и футер идут подряд. До хвоста доходит меньшинство, а те, кто дошёл,
         получают три одинаковых набора WhatsApp/Telegram/Instagram.
      6. **Формулировка одна на все страницы.** «Запишитесь на консультацию.
         Ответим в мессенджере и подберём удобное время» — дословно одинаково
         на главной, всех разделах и всех услугах. Предзаполненный текст
         сообщения при этом сделан правильно: на услуге в WhatsApp уходит
         «Хочу записаться на: <название услуги>».
      7. Три кнопки рядом (WhatsApp, Telegram, Instagram) в одном визуальном
         весе — выбор вместо действия. Instagram при этом ведёт в аккаунт
         старого бренда (задача 64) и записаться там нельзя.
      Правки — задача 49.
- [x] 47. Оценить подачу цен и понятность прайса для клиента.
      Разбирал 204 позиции в `prices.json` и их вывод на живом домене. Цены
      верные и полные (это закрыто задачей 17) — здесь только о подаче.
      Что работает: прайс настоящей таблицей, валюта подписана над каждым
      блоком, на главной шесть популярных процедур сразу с ценой, в ответах
      FAQ цены подставляются из того же источника — расхождений быть не может.
      Находки:
      1. **Больше половины прайса скрыто за вкладками.** Вкладки у пяти услуг,
         клиент видит только первую: `/uhod-za-volosami/` — 7 строк из 40,
         `/igolchatyj-rf-lifting/` — 16 из 33, `/uhodovaya-kosmetologiya/` —
         8 из 14, `/lazernaya-epilyaciya/` — 24 из 32. Ни счётчика позиций
         на вкладке, ни «показать всё» нет; на `/uhod-za-volosami/` шесть
         вкладок переносятся в две строки и читаются как фильтр, а не как
         «здесь ещё 33 цены».
      2. **Нигде не сказано, сколько процедура длится.** Ни в одной из 204
         строк нет времени, отдельной колонки нет. Длительность упоминается
         только россыпью в текстах FAQ (34 совпадения по контенту). Для записи
         в салон это второй вопрос после цены.
      3. **169 позиций из 204 без единого слова пояснения.** Клиент видит
         «Контуринг — 6500 ฿» и не понимает, что входит. Пояснения там, где
         они есть, оформлены сносками со звёздочкой («*Только совместно
         с другой процедурой»).
      4. **37 названий несут суффикс из выгрузки Tilda** — «Женская - Длинные /
         густые волосы», «Тату - 3x5 см», «Перманентный макияж бровей -
         1-я процедура». Читается как строка базы, а не как пункт меню: это
         не отдельные услуги, а модификатор цены, и его место — во второй
         колонке или подстрокой. Сюда же типографика с дефисом вместо тире,
         записанная в задаче 16 и не закрытая на этапе 6.
      5. **Три разных формата диапазона:** «600 -1200 ฿», «400 -1000 ฿»
         (пробел не с той стороны) и «500-1000 ฿». Плюс девять позиций «от»
         без объяснения, от чего зависит итог.
      6. **Крупные суммы без разделителя разрядов:** «20000 ฿», «13000 ฿»,
         «10500 ฿». На беглом взгляде путается порядок.
      7. **Акция подана как обычная строка.** `smas-lifting`, «Акция -
         Фулфейс + зона шеи + декольте» за 35000 ฿ — самая дорогая позиция
         сайта, лежит в таблице рядом со всеми и ничем не выделена. Так же
         теряются комбинированные сеты лазерной эпиляции: в шаблоне есть
         отдельная вёрстка `.pricelist__combos`, но она ищет позиции с именем
         на «КОМБО», а таких в данных нет ни одной — ветка не срабатывает
         никогда, сеты выводятся обычными строками. Мёртвую ветку убрать
         или задействовать — записал находкой в задачу 58.
      8. **На страницах разделов цен нет вообще.** Единственный ценовой сигнал
         там — в ответах FAQ, но пять вопросов из шести свёрнуты по умолчанию,
         так что при загрузке страницы символ ฿ на экране не появляется ни разу.
      Правки — задача 49.
- [x] 48. Оценить доверие: что убеждает выбрать салон, чего не хватает
      Разбирал все 23 индексируемые страницы: сверил живой домен с локальной
      сборкой (`index.html` совпадает байт в байт), дальше считал по собранному
      HTML и `content.yml`. Вопрос один: за что клиенту зацепиться, если он
      видит салон впервые и находится за границей.
      Что убеждает:
      1. **Конкретика вместо общих слов.** Аппараты названы поимённо —
         Morpheus 8, Scarlet S, M22; препараты тоже — Диспорт, Набота,
         Ботулакс, Иннотокс, Рефайнекс; косметика — Davines, LEBEL, Christina,
         Renoderm, Tokio Inkarami. Это проверяемые факты, и на сайте это
         самый сильный сигнал: салон, который не боится назвать оборудование.
      2. **Открытый прайс на 204 позиции** вместо «уточняйте». Там, где расчёт
         честно индивидуальный (ботулинотерапия — за единицу, электроэпиляция —
         по объёму), это прямо сказано, а не спрятано.
      3. **Противопоказания названы на 11 страницах из 23.** Салон, который сам
         говорит «вам может быть нельзя, решаем очно», читается профессионально.
      4. **Фото настоящие** — из материалов салона, реальные аппараты и кабинеты
         (`assets/img/CREDITS.txt`), не сток. Но нигде на сайте об этом не сказано,
         и клиент по умолчанию считает такие кадры стоковыми.
      5. **Русский и английский** — для русскоязычного клиента на Самуи снимает
         главный барьер, заявлено и на главной, и в JSON-LD.
      Чего не хватает:
      1. **Ни одного отзыва.** 0 совпадений по «отзыв» на всех 23 страницах,
         ссылки на внешний источник тоже нет. `aggregateRating` и `Review`
         в разметке пустые — и правильно, выдумывать нельзя, но и настоящего
         источника пока не существует: Google Business Profile не заведён
         (задача 65). Для салона красоты это первое, что ищут.
      2. **Салон анонимен: за 23 страницы ни одного человека.** «Мастер» —
         24 упоминания, «специалист» — 5, «косметолог» — 8, и всегда безлично.
         Ни имени, ни фото, ни образования, ни стажа: по «опыт / стаж /
         сертификат / диплом / квалификация» — 0 совпадений на всём сайте.
         Клиент выбирает, кому доверить лицо, и не знает о человеке ничего.
      3. **Не сказано, сколько салон работает на Самуи.** Ни года основания,
         ни «N лет на острове», ни числа клиентов. Факт при этом есть — старый
         сайт на Tilda с тем же прайсом это подтверждает, — он просто не заявлен.
      4. **Ни одной работы «до/после».** Все 24 фото — иллюстрации процесса
         (манипула у колена, маска на лице), ни одного результата. Для
         перманентного макияжа, окрашивания и коррекции фигуры результат —
         главный аргумент, и его на сайте нет вообще.
      5. **Инвазивные процедуры без слова о безопасности.** Стерильность,
         одноразовые материалы и дезинфекция упомянуты на 3 страницах из 23:
         `/elektroepilyaciya/` (одноразовая игла — и то потому, что она строка
         прайса), `/udalenie-tatuirovok-lazerom/` (блок «Стерильность
         и приватность»), `/massazh/`. Их нет там, где вопрос острее всего:
         `/permanentnyj-makiyazh/` — пигмент вводят в кожу на несколько часов,
         и `/botulinoterapiya/` — инъекции. У ботулинотерапии не сказано ни кто
         делает укол, ни что препарат оригинальный и вскрывается при клиенте.
         Для инъекций в чужой стране это два первых вопроса.
      6. **Разнобой в том, кто выполняет процедуру.** На ботулинотерапии это
         «специалист», на РФ-лифтинге и SMAS — «мастер», слова «врач» нет
         ни разу. «Мастер» рядом с инъекциями и лазером работает против
         доверия: за него платят как за медицинскую компетенцию, а звучит он
         как парикмахерская. Термин надо выбрать по типу процедуры, а не
         вперемешку (единообразие проверяется в задаче 51).
      7. **Instagram ведёт на аккаунт чужого бренда.** Кнопка стоит в блоке
         записи всех 23 страниц и в футере, а открывается
         `avocado.beauty.samui`. Клиент видит другое название и не понимает,
         туда ли попал. Пока аккаунт не переименован (задача 64), это не
         соцдоказательство, а разрыв доверия — и при этом единственное место,
         где можно было бы увидеть живые работы.
      8. **Нет адреса, карты и телефона.** Адрес не публикуется по решению
         заказчика — это осознанно, но компенсации нет: не названы ни район,
         ни ориентир, ни «столько-то минут от Чавенга». Клиент не понимает,
         ехать ему через весь остров или нет, и позвонить не может — ссылок
         `tel:` на сайте нет ни одной (находка задачи 45).
      9. **Нет ответа на «а что если».** Ни слова о том, можно ли перенести
         или отменить запись, нужна ли предоплата и что будет, если результат
         не устроит. Единственное обязательство на сайте — «ответим
         в мессенджере».
      10. **Политика конфиденциальности собрана под чужую юрисдикцию.**
          Тайский салон ссылается на российский 152-ФЗ (перенос с Tilda), и это
          единственная «юридическая» страница: юрлица, регистрации или хотя бы
          формы «кто оператор» на сайте нет. Мелочь, но при беглом чтении
          выдаёт шаблон.
      Правки — задача 49.
- [x] 49. Свести находки в отчёт и внедрить согласованные правки
      Отчёт — `docs/audits/2026-08-06-stage7-marketing-audit.md`. Внедрено
      всё, что закрывается кодом; находки, требующие данных от салона, вынесены
      в раздел «Вне кода» задачами 73–78 — выдумывать отзывы, стаж и стерильность
      нельзя.
      Путь и призывы (задачи 45–46):
      1. Снят `order:-1` у `.hero__media` и `.svc-hero__media` — фото идёт после
         текста и на мобильном. Кнопка на главной поднялась с 1.26 на 0.70
         экрана, на странице услуги с 1.49 на 0.90 (там же поджаты отступы
         героя до 768 px).
      2. `.site-header__cta` больше не скрыта до 1024 px, а сжата в круг 44×44
         с иконкой мессенджера: запись из липкой шапки в один тап вместо трёх.
      3. На странице раздела появилась кнопка под заголовком — первая точка
         записи сместилась с 5.5 экрана на 0.72.
      4. Новый `.cta-inline`: на главной после сетки разделов и после «Частых
         процедур», на услуге сразу под прайсом, на разделе под таблицей
         сравнения. Самый длинный участок без призыва на главной сократился
         с 8.5 до 4.3 экрана, на услуге — с 6.4 до 4.3.
      5. В блоке записи одна основная кнопка «Написать в WhatsApp» и строка
         запасных каналов ниже; подводка теперь своя у главной, раздела
         и услуги вместо одной фразы на 23 страницы.
      6. Контактная полоса вынесена в `partials/contact_strip.html.j2` и стоит
         на всех страницах (стили переехали из `home.css` в `components.css`).
         В неё добавлено «Позвонить» — до этого телефон жил только в JSON-LD.
      7. Внизу страницы раздела — блок «Другие направления» на остальные пять.
      Цены (задача 47):
      8. Карточка услуги в разделе и в «Смотрите также» показывает «от N ฿».
         Нижняя граница берётся из того же каталога, что уходит в AggregateOffer.
      9. Счётчик позиций на вкладках прайса: «Цены 24», «Комбинированные сеты 8».
      10. `format_price()` — единый диапазон («600 -1200 ฿» → «600–1200 ฿»)
          и разделитель разрядов от пяти знаков («20000 ฿» → «20 000 ฿»).
      11. `price_name_parts()` — суффикс выгрузки Tilda уходит в подпись
          (37 позиций), префикс «Акция» стал меткой: SMAS-лифтинг за 35 000 ฿
          больше не теряется в общем списке.
      12. Удалена ветка `.pricelist__combos` со стилями `.combo*` — она искала
          позиции на «КОМБО», которых в прайсе нет (снимает находку из задачи 58).
      Доверие (задача 48):
      13. На аппаратных процедурах «мастер» заменён на «косметолог» (РФ-лифтинг,
          SMAS-лифтинг, эндосфера): рядом с лазером и ультразвуком «мастер»
          читается как парикмахерская. На ручных работах термин оставлен.
      Проверки: `prices.json` остаётся источником истины, `check_prices.py`
      сверяет HTML с тем же видом, что строят новые функции. Добавлен
      `tests/test_price_display.py` — 16 тестов. Итог: 76 тестов зелёные,
      парити 204 из 204, `check_content.py` — 25 страниц без замечаний.

---

## Этап 8. Финальный аудит SEO и GEO

- [x] 50. Сквозная проверка метатегов, заголовков и перелинковки по всем 23 страницам
      Разбор собранного HTML всех 23 индексируемых страниц: длины и уникальность
      title и description, canonical, состав заголовков, граф внутренних ссылок
      с разделением на карточки и ссылки в тексте, сверка с `sitemap.xml`.
      Что в порядке: title, description и h1 уникальны на всех 23 страницах —
      ни одного повтора; описания укладываются в 140–159 знаков; canonical
      везде совпадает с адресом страницы; `sitemap.xml` содержит ровно 23
      адреса, ни одного лишнего и ни одного пропущенного; хлебные крошки верны
      на всех страницах и отсутствуют только на главной; h1 ровно один
      на страницу; сирот в перелинковке нет — на каждую услугу ведут минимум
      четыре ссылки из контента, не считая шапки и футера.
      Находки:
      1. **Бренда в title нет у 9 страниц из 23** — `/volosy/`, `/epilyaciya/`,
         `/apparatnaya-kosmetologiya/`, `/uhodovaya-kosmetologiya/`,
         `/igolchatyj-rf-lifting/`, `/elektroepilyaciya/`, `/biozavivka-volos/`,
         `/tokio-inkarami/`, `/davines-naturaltech-tailoring/`. Это три раздела
         из пяти и шесть услуг: в выдаче они выглядят как чужие страницы.
      2. **У оставшихся 14 связка с брендом двух видов:** через тире («…волос
         на Самуи — Neva Beauty», 4 страницы) и через запятую («…морщины,
         Neva Beauty», 10 страниц). Внутри одного сайта это читается как
         недоделка.
      3. **Два title длиннее 60 знаков** и обрежутся в выдаче:
         `/fotoomolozhenie-m22/` — 63, `/keratinovoe-vypryamlenie-volos/` — 61.
         В обоих случаях под нож попадает как раз бренд.
      4. **Заголовки h2 шаблонные.** «Запишитесь на консультацию» — на 23
         страницах из 23, «Почему это работает» — на 17, «Смотрите также» —
         на 17. У страницы услуги из пяти h2 своих только два: «Цены на услугу
         …» и «Частые вопросы — …». Блок преимуществ на всех 17 услугах назван
         одинаково, хотя содержимое у каждой своё, — заголовок не работает
         ни на клиента, ни на робота.
      5. **`og:title` дословно равен `title` на всех 23 страницах.** В мессенджере
         человек видит SEO-строку с перечислением ключей через тире, а на девяти
         страницах — ещё и без названия салона. Это разные задачи: title пишется
         под выдачу, og:title читает человек в чате.
      6. **Раздел и его услуги конкурируют за один запрос.** Title раздела
         перечисляет методы, которые дословно вынесены в title услуг:
         «Эпиляция на Самуи — лазерная, шугаринг, электроэпиляция» против
         «Лазерная эпиляция на Самуи» и «Сахарная эпиляция на Самуи — шугаринг
         всех зон»; то же у `/apparatnaya-kosmetologiya/` с `/smas-lifting/`.
         Разделу нужна своя роль — «куда прийти и как выбрать», а не повтор
         названий услуг.
      7. **Две услуги держатся на минимуме связей:** `/biozavivka-volos/`
         и `/endosfera-terapiya/` — по 4 входящие ссылки из контента, из них
         текстовых только 2. Ещё четыре страницы упомянуты ссылкой в тексте
         ровно один раз и только со своей же страницы раздела:
         `/botulinoterapiya/`, `/fotoomolozhenie-m22/`, `/uhod-za-volosami/`,
         `/uhodovaya-kosmetologiya/` — соседние услуги на них не ссылаются.
      Правки — задача 54.
- [x] 51. Проверить единообразие написания бренда, услуг, аппаратов и локации
      Разбор собранного HTML 24 страниц: отдельно свой текст страницы (`main`
      без блока «Смотрите также»), отдельно подписи в шапке, футере, крошках,
      карточках, заголовках прайса и `Service.name` в JSON-LD. Плюс сверка
      источников — `content.yml`, `prices.json`, `build.py`.
      Что в порядке: кириллических подмен латинских марок нет ни одной —
      «М22», «СМАС», «Токио Инкарами» не встречаются; марки написаны
      единообразно везде, где упомянуты (Davines, Naturaltech, Tailoring,
      Tokio Inkarami, LEBEL, Christina (Comodex), Renoderm, Oxygen, M22,
      Morpheus 8, Scarlet S); `Service.name` в разметке совпадает с подписью
      в навигации на всех 17 услугах; h1 у всех 23 страниц собран по одной
      формуле «<Название> на Самуи»; синонимы вводятся явно и один раз —
      «Сахарная эпиляция (шугаринг)», «Перманентный макияж, он же татуаж»;
      старого бренда «Avocado» в текстах нет, единственный след — ссылка
      на Instagram (переименование — задача 64).
      Находки:
      1. **Одна и та же сумма пишется в двух видах на одной странице.**
         В прайс-таблице «30 000 ฿», в тексте и в ответах FAQ — «30000 ฿».
         Причина в коде: `price_by_name` (`generator/build.py:128`) кладёт
         в указатель для токенов `{price:…}` сырую строку из `prices.json`,
         а таблица прогоняет ту же строку через `format_price`
         (`generator/build.py:230`). Затронуты 6 страниц и 28 вхождений:
         `/smas-lifting/` (5 сумм), `/fotoomolozhenie-m22/` (3),
         `/igolchatyj-rf-lifting/` (3), `/uhod-za-volosami/` (2),
         `/endosfera-terapiya/` (1), `/massazh/` (1). Правка — одна строка.
      2. **Раздел «Макияж» назван четырьмя способами.** В меню и в подзаголовке
         над h1 — «Макияж», в хлебных крошках — «Перманентный макияж», в h1 —
         «Перманентный макияж и татуаж», в заголовке прайса и в `Service.name` —
         «Перманентный макияж». Пятый вариант — адрес `/permanentnyj-makiyazh/`.
         Ни одна пара «меню → страница» не совпадает по подписи.
      3. **Ещё две страницы теряют слово при переходе из меню в h1:**
         `/davines-naturaltech-tailoring/` — везде «Davines Naturaltech
         Tailoring», в h1 «Уход за кожей головы Davines Naturaltech» без
         Tailoring; `/fotoomolozhenie-m22/` — везде «Фотоомоложение M22»,
         в h1 «Фотоомоложение лица M22». Человек кликает одно название,
         попадает на другое.
      4. **Аббревиатуры лифтингов разведены по алфавитам произвольно:**
         SMAS латиницей (30 упоминаний), РФ-лифтинг кириллицей (34).
         Внутри себя каждая консистентна, но рядом в одном предложении
         («игольчатый РФ-лифтинг … SMAS-лифтинг», 6 страниц) пара выглядит
         случайной. Единственное латинское `RF` на сайте — в строке прайса
         `/uhodovaya-kosmetologiya/` «Алмазная шлифовка, RF, микротоки».
         Там же в `prices.json` «Нано насадка РФ-лифтинг» без дефиса.
      5. **Локация названа тремя формами вперемешку:** «на Самуи» (83),
         «о. Самуи, Таиланд» (69, в шаблонной вводной), «на острове Самуи»
         (3, только на главной). Плюс бренд `Neva Beauty — Koh Samui` сам
         содержит локацию, отсюда двойное указание: `/uhod-za-volosami/` —
         «В Neva Beauty — Koh Samui на Самуи стригут женщин, мужчин и детей».
      6. **Единообразие достигнуто шаблоном, а не редактурой.** Вводная
         первого экрана на 22 из 23 страниц собрана по одной формуле
         «<Услуга> в салоне Neva Beauty — Koh Samui (о. Самуи, Таиланд) —
         это …», оборот «в Neva Beauty на Самуи» открывает блок преимуществ
         на 16 страницах, «В салоне Neva Beauty — Koh Samui» открывает первый
         ответ FAQ на 19. Формально это единообразие, читается как копипаст —
         смыкается с находкой 4 задачи 50 про шаблонные h2.
      7. **Мелочи.** Марка в шапке набрана двумя строками без тире и
         копируется как «Neva Beauty Koh Samui», хотя везде — «Neva Beauty —
         Koh Samui». Аббревиатура «РФ» на сайте значит и «радиочастотный»,
         и «Российская Федерация» (`/privacy/`, «законодательство РФ»).
         Расхождения с официальным написанием марок: «Hydra Facial»
         (правообладатель пишет слитно), «LEBEL» капсом (марка — LebeL).
      Правки — задача 54.
- [x] 52. Проверить, что каждая страница даёт извлекаемый ответ на свои типовые вопросы
      Разбор 163 пар «вопрос — ответ» на 23 страницах: сверка видимого FAQ
      с `FAQPage` в разметке, покрытие восьми типовых тем (цена, длительность,
      боль, противопоказания, курс, стойкость результата, солнце и море,
      запись), форма ответа — есть ли конкретика в первом предложении и
      понятен ли ответ, вырванный из страницы.
      Что в порядке: `FAQPage` дословно совпадает с видимым текстом на всех
      23 страницах — ноль расхождений, робот и человек читают одно и то же;
      161 вопрос из 163 уникален; цены в ответах подставляются из прайса
      токеном `{price:…}`, поэтому не могут разойтись с таблицей; 18 из 21
      ответа «Сколько стоит…» ставят сумму уже в первое предложение; вопрос
      «как записаться» закрыт на всех 23 страницах и ведёт в мессенджеры;
      медианная длина ответа 282 знака — ровно тот объём, который витрина
      забирает целиком.
      Находки:
      1. **Треть ответов заканчивается отсылкой к консультации.** 54 из 163
         ответов на всех 23 страницах закрываются оборотом «точные сроки
         называют на консультации», «план составят после осмотра», «подскажут
         при записи». Вопрос задан, но ответа страница не даёт — извлекать
         нечего ни поисковику, ни клиенту.
      2. **«Когда можно на солнце и в море» — 11 страниц спрашивают, ни одна
         не отвечает.** Для острова это главный вопрос, и он честно вынесен
         в FAQ на `/lazernaya-epilyaciya/`, `/saharnaya-epilyaciya/`,
         `/epilyaciya/`, `/fotoomolozhenie-m22/`, `/udalenie-tatuirovok-lazerom/`,
         `/keratinovoe-vypryamlenie-volos/`, `/kosmetologiya/`,
         `/uhodovaya-kosmetologiya/`, `/apparatnaya-kosmetologiya/`,
         `/tokio-inkarami/`, `/uhod-za-volosami/`. Ни в одном из 11 ответов
         нет срока: ни «сутки», ни «3 дня», ни «неделя». Человек, который
         планирует процедуру между пляжными днями, уходит без ответа.
      3. **Длительность процедуры почти не отвечена.** Вопрос «сколько
         длится» не задан на 16 из 22 страниц услуг и разделов; конкретные
         минуты называют только 6 ответов. Смыкается с задачей 77 —
         в прайсе длительности нет ни у одной из 204 позиций.
      4. **Курс без чисел.** Из 12 вопросов «сколько сеансов / как часто»
         число сеансов называют 5 (`/endosfera-terapiya/`,
         `/korrekciya-figury/`, `/massazh/` — курс на 10 процедур,
         `/udalenie-tatuirovok-lazerom/` — «после двух-трёх сеансов»).
         Интервал между сеансами не назван ни в одном из 163 ответов.
      5. **Противопоказания не спрошены там, где их спрашивают чаще всего.**
         Вопроса нет на 11 из 23 страниц, и среди них — `/permanentnyj-makiyazh/`
         (прокол кожи), `/udalenie-tatuirovok-lazerom/`, `/elektroepilyaciya/`,
         `/lazernaya-epilyaciya/`, `/saharnaya-epilyaciya/`,
         `/keratinovoe-vypryamlenie-volos/`. Ещё на 3 страницах вопрос есть,
         а в ответе ни одного конкретного ограничения. Смыкается с задачей 76.
      6. **18 закрытых вопросов не начинаются с ответа.** «Больно ли делать
         электроэпиляцию?» → «Чувствительность у всех разная…»; «Можно ли
         делать лазерную эпиляцию с загаром?» → «Перед процедурой обязательно
         уточняют…». Витрина забирает первое предложение — и забирает оговорку
         вместо «Да» или «Нет».
      7. **6 ответов открываются местоимением** и вне страницы теряют смысл:
         «Это три разные задачи.», «Это противоположные по задаче процедуры.»,
         «Это одна из причин, по которой её выбирают.», «Это два разных
         аппарата…», «Это решают по состоянию кожи…», «Это услуга
         персонального подбора ухода…».
      8. **Мелочи.** Определение услуги есть на всех страницах во вводной
         («<Услуга> … — это …»), но в форме вопроса продублировано только
         на 3 из 23 — для блоков «люди спрашивают» это упущенная позиция.
         Два вопроса повторяются дословно на разных страницах: «Чем
         эндосфера-терапия отличается от массажа?» и «Чем лазерная эпиляция
         отличается от сахарной и электроэпиляции?» — раздел и услуга задают
         один и тот же вопрос, как в находке 6 задачи 50.
      Правки — задача 54.
- [x] 53. Проверить `sitemap.xml`, `robots.txt`, `llms.txt` и разметку на живом домене
      Проверка выполнена по боевому `https://th.neva.beauty/`: скачаны все
      24 страницы, три служебных файла и 23 og-картинки, разобран JSON-LD
      каждой страницы, `AggregateOffer` сверен с `prices.json`.
      Что в порядке: все 23 адреса из `sitemap.xml` отдают 200, файл содержит
      ровно 23 `<loc>`, `/privacy/` и `/404.html` исключены как noindex;
      живая страница побайтно совпадает с собранной в репозитории на всех
      24 — деплой не отстаёт; `robots.txt` отдаётся `text/plain`, разрешает
      обход всем и указывает абсолютный адрес карты; canonical и `og:url`
      на всех 23 совпадают с собственным адресом, `hreflang` `ru`
      и `x-default` ссылаются на себя, `meta robots` = `index, follow`
      ровно на 23 страницах, `/privacy/` — `noindex`; адрес без слеша отдаёт
      301 на слеш, `http://` → `https://` 301, несуществующий адрес — 404;
      JSON-LD парсится на всех 24 страницах, `Organization`, `BeautySalon`
      и `WebSite` связаны через `@id`, `BreadcrumbList` есть на 22 страницах
      с абсолютными адресами, `FAQPage` — на 23, `ItemList` — на 5 разделах;
      `AggregateOffer` сходится с прайсом на всех 16 услугах — доплаты,
      тарифы за минуту и расходники помечены `addon` и в диапазон не попадают;
      23 уникальные og-картинки, все 1200×630, все отдают 200, вес 48–123 КБ.
      Находки:
      1. **`lastmod` в sitemap — дата сборки, а не дата изменения страницы.**
         `generator/build.py:517` подставляет `date.today()` во все 23 записи,
         поэтому каждая пересборка объявляет весь сайт изменённым. Поисковик,
         увидев такое два-три раза, перестаёт доверять полю и игнорирует его
         целиком — то есть механизм «вот что реально обновилось» теряется.
      2. **`llms.txt` дублирует `/permanentnyj-makiyazh/`** — один раз
         в «Направлениях» как «Макияж», второй в «Услугах» как «Перманентный
         макияж». Из 23 индексируемых адресов в файле 22 уникальных, главная
         дана только заголовком, ссылкой на себя не выведена. Это та же
         путаница с разделом «Макияж», что в находке 2 задачи 51.
      3. **`llms.txt` не содержит ни одной цены и ни одного факта** — только
         карту ссылок. Для файла, вся задача которого дать ИИ-ассистенту
         готовую выжимку, это упущение: спека llmstxt.org прямо допускает
         раздел с ключевыми фактами, а у нас есть что положить — валюта,
         диапазоны цен по разделам, языки обслуживания, приём по записи.
      4. **Узлы страницы не привязаны к её адресу.** Ни одного узла `WebPage`
         и ни одного `mainEntityOfPage` на сайте нет, а `Service`, `FAQPage`
         и `BreadcrumbList` не имеют ни `@id`, ни `url`. Бизнес, организация
         и сайт связаны корректно, но из графа не следует, какой странице
         принадлежит конкретный `Service`.
      5. **Instagram в `llms.txt` и в `sameAs` ведёт на аккаунт старого
         бренда** `avocado.beauty.samui` — то есть машина, читающая сайт,
         получает «Avocado» как связанный профиль (задача 64).
      6. **`www.th.neva.beauty` не резолвится** — DNS-записи нет, браузер
         покажет ошибку соединения. Кто наберёт адрес с www, сайт не увидит.
      7. **Мелочи.** `robots.txt` перечисляет семь ботов поимённо, хотя
         `User-agent: *` уже всё разрешает — блок декоративный; из значимых
         для ИИ-выдачи не названы `Applebot-Extended`, `CCBot`, `Amazonbot`,
         `meta-externalagent` (на доступ это не влияет, но список придётся
         переписывать, если что-то понадобится запретить). Файл `CNAME`
         отдаётся публично как `application/octet-stream`.
      Правки — задача 54.
- [x] 54. Свести находки в отчёт и исправить
      Отчёт — `docs/audits/2026-08-06-stage8-seo-geo-audit.md`. Внедрено всё,
      что закрывается кодом и редактурой; находки, требующие данных от салона,
      вынесены в раздел «Вне кода» задачами 79–81 (плюс уже заведённые 64, 76, 77).
      Метатеги и заголовки (задача 50):
      1. Все 23 title переписаны по правилу «<запрос> на Самуи — Neva Beauty»:
         бренд есть на всех 23 страницах вместо 14, связка одна вместо двух,
         самый длинный заголовок — 52 знака вместо 63, в выдаче не обрезается
         ни один.
      2. Title разделов больше не перечисляет названия своих услуг: «Эпиляция
         на Самуи: какой метод выбрать», «Косметология на Самуи: с чего начать»,
         «Волосы на Самуи: все услуги салона» — раздел и услуга перестали
         конкурировать за один запрос.
      3. `og:title` отделён от title: заголовок карточки в мессенджере
         собирается из h1 (`og_title()` в `build.py`), переопределяется полем
         `og_title` — так задан заголовок главной.
      4. Шаблонные h2 убраны: заголовок блока записи приходит от страницы
         (`booking_title`, 23 формулировки), блок преимуществ — тоже
         (`benefits_title`, 17 формулировок), «Смотрите также» → «Что выбирают
         вместе с услугой «…»», «Чем ещё занимается салон» → «Что ещё делают
         в Neva Beauty кроме раздела «…»». Повторяющихся h2 не осталось ни одного.
      5. Перелинковка: шесть страниц получили текстовые ссылки от соседних услуг —
         биозавивка 6 входящих вместо 2, эндосфера-терапия 8 вместо 2,
         ботулинотерапия 5, фотоомоложение 5, уходовая косметология 5,
         уход за волосами 4.
      Единообразие (задача 51):
      6. `price_by_name()` прогоняет цену через тот же `format_price()`, что
         и таблица, — все 28 расхождений «30 000 ฿» против «30000 ฿» на 6 страницах
         сняты одной строкой. Сумм без разделителя разрядов на сайте нет.
      7. Раздел «Макияж» переименован в «Перманентный макияж»: подпись совпадает
         в надзаголовке, крошках, заголовке прайса и `Service.name`. Короткое
         «Макияж» осталось только в верхней панели — как «Аппаратная» и «Фигура».
      8. h1 `/davines-naturaltech-tailoring/` вернул Tailoring,
         `/fotoomolozhenie-m22/` совпал с названием в меню.
      9. Убраны шаблонные зачины: оборот «В салоне Neva Beauty — Koh Samui …»
         больше не открывает первый ответ FAQ на 19 страницах, «в Neva Beauty
         на Самуи» — блок преимуществ на 16. Снято двойное указание локации
         («В Neva Beauty — Koh Samui на Самуи»).
      Извлекаемость ответов (задача 52):
      10. Переписаны 26 ответов: 16 закрытых вопросов открываются «да», «нет»
          или прямой оценкой, 6 ответов больше не начинаются с местоимения,
          в остальных отсылка к консультации перестала быть последней фразой.
          Два дословно повторявшихся вопроса переформулированы.
      Служебные файлы и разметка (задача 53):
      11. `lastmod` стал датой изменения страницы: дата хранится в
          `generator/data/lastmod.json` вместе с отпечатком и обновляется только
          вместе с ним. Отпечатки ассетов (`?v=…`) из хеша вырезаны — правка CSS
          дату не двигает. Проверено на трёх сценариях.
      12. `llms.txt` получил блок «Коротко» (локация, формат приёма, языки,
          валюта, телефон) и цены по разделам и услугам из того же каталога,
          что уходит в `AggregateOffer`. Разделы без своей страницы больше
          не выводятся — дубль ушёл, главная выведена ссылкой: 23 уникальных
          адреса вместо 22 с дублем.
      13. У каждой страницы появился узел `WebPage` с `@id` от её адреса,
          связанный с `WebSite` через `isPartOf` и с бизнесом через `about`.
          `Service`, `FAQPage`, `BreadcrumbList` и `ItemList` получили свои `@id`
          и `mainEntityOfPage`.
      14. `robots.txt` больше не перечисляет семь ботов поимённо. Марка в шапке
          копируется как «Neva Beauty — Koh Samui»: тире добавлено скрытым.
      Осознанно оставлено как есть: SMAS латиницей и РФ-лифтинг кириллицей (обе
      формы частотны, написание уже закреплено в `ENTITY_SPELLING`); формула
      вводной первого экрана (она делает первый абзац самодостаточным для
      ИИ-ответов); `Hydra Facial` и `LEBEL` (приходят из `prices.json` — источника
      истины и предмета парити-теста).
      Проверки: парити 204 из 204, `check_content.py` — 25 страниц без замечаний,
      добавлены три проверки (бренд в title, наличие `og:title`, отсутствие
      повторяющихся h2). Тестов 85, из них 9 новых в `tests/test_seo_output.py`.

---

## Этап 9. Скорость и изображения

- [x] 55. Прогнать Lighthouse по главной, странице раздела и странице услуги
      Замеры сделаны Lighthouse 13.4.1 (headless Chrome 151) по боевому домену:
      главная `/`, раздел `/kosmetologiya/`, услуга `/lazernaya-epilyaciya/` —
      каждая в мобильном и десктопном профиле; главная прогнана трижды, чтобы
      оценить разброс; отдельно — контрольный прогон с заблокированным
      `mc.yandex.ru` и прогон `vn.neva.beauty` как эталона из задачи 57.
      Баллы: десктоп — производительность 99 (главная), 100 (раздел),
      100 (услуга), доступность 100/100/96, лучшие практики 77 везде,
      SEO 100 везде. Мобильный — производительность 91/93/93, доступность
      100/100/97, лучшие практики 77, SEO 100. `CLS` 0–0.013 на всех шести
      прогонах, `TTFB` 280–430 мс, вес страницы 512 КБ, 27 запросов.
      Разброс мобильной главной от прогона к прогону — 84, 88, 91: `LCP`
      гуляет 1.7–3.8 с, потому что канал до GitHub Pages нестабилен. Одиночный
      балл ни о чём не говорит, сравнивать правки в задаче 57 нужно медианой
      трёх прогонов.
      Находки:
      1. **Лучшие практики 77 — это целиком Яндекс.Метрика.** Единственные
         замечания категории — `third-party-cookies` (6 кук) и `inspector-issues`,
         оба указывают на `mc.yandex.ru`. Контрольный прогон с заблокированным
         доменом даёт 100. Кодом это не чинится: либо счётчик, либо балл.
      2. **`TBT` — тоже Метрика.** `tag.js` 93 КБ + `tag_phono.js` 13 КБ,
         из них 50 КБ кода не выполняется; 177 мс скриптинга. С блокировкой
         `TBT` падает с 310–360 мс до 10 мс. Свой JS сайта — 4 КБ на три
         модуля, его вклад в районе нуля. В `init` включён `webvisor:true` —
         именно вебвизор тянет основную часть веса; если запись сессий
         заказчику не нужна, это самая дешёвая правка на странице.
      3. **Шрифты — самая тяжёлая категория ресурсов: 10 файлов, 220 КБ**
         против 192 КБ на все картинки. Латинские подмножества весят больше
         кириллических (Cormorant 35 КБ против 22 КБ, Manrope 24 КБ против
         14 КБ), а тянутся всегда: `unicode-range` латиницы включает
         `U+0000-00FF`, то есть любая цифра, точка и слово «Neva» его
         активируют. Предзагружены только два файла из десяти, остальные
         восемь браузер находит после разбора CSS — цепочка из трёх шагов
         (HTML → CSS → шрифт). Кандидаты на правку в задаче 57: сабсеттинг
         латиницы по реально используемым знакам и отказ от лишних
         начертаний Manrope (в вёрстке их четыре: 400, 500, 600, 700).
      4. **Ни у одной картинки нет `srcset`/`sizes`.** Главная отдаёт
         `hero.webp` 800×1000 под контейнер 497×621 на мобильном и 380×475
         на десктопе, карточки разделов — 900×675 под 634×475 и 268×201.
         Перерасход: 97 КБ на мобильной главной и 165 КБ на десктопной,
         9 КБ на странице услуги. Это задача 56.
      5. **У карточек разделов объявленные размеры не совпадают с реальными:**
         в шаблоне `width="600" height="440"` (1.364), в файле 900×675 (1.333).
         `CLS` это пока не ломает — спасает `object-fit` в CSS, — но
         зарезервированное место считается по неверной пропорции.
      6. **Контраст счётчика в активной вкладке прайса — 3.81:1 при норме
         4.5:1** (`.pricelist__tab-count`, `#e9e0df` на `#92655e`, 13 px).
         Из-за него доступность 96–97 вместо 100 на каждой странице услуги
         с вкладками. Единственное замечание доступности на всём сайте.
      7. **Кеш на GitHub Pages — `max-age=600` на всё, включая шрифты
         и картинки** (замечание на 300–445 КБ в каждом прогоне). Имена
         ассетов уже версионируются (`?v=hash`), то есть их можно было бы
         кешировать на год, но GitHub Pages не даёт настраивать заголовки.
         Правится только сменой хостинга — вносить в отчёт как ограничение
         площадки, а не как дефект сборки.
      8. **Мелочи.** `bundle.min.css` блокирует рендер на 150–175 мс (7 КБ
         в gzip — терпимо); `nav.js:63` вызывает принудительный пересчёт
         вёрстки на 37 мс синхронным чтением `scrollY` при старте; ни один
         внешний домен не указан в `preconnect` (Lighthouse при этом считает,
         что кандидатов нет).
      9. **Эталон «как на vn» из задачи 57 недостижим и уже достигнут
         одновременно.** `vn.neva.beauty` в мобильном профиле даёт 92/100/77/100
         — тот же потолок и та же причина (Метрика). `th` c 91–93 уже на
         уровне vn, а 100 не показывает ни один из двух сайтов. Формулировку
         задачи 57 надо читать как «убрать всё, что чинится кодом», потолок
         же задаёт счётчик.
      10. **Проверенная и отвергнутая гипотеза.** Фон «Аврора» с `blur(60px)`
         и шумом подозревался как причина задержки отрисовки: A/B на локальной
         копии с отключёнными `.aurora` и `.hero__blob` дал ровно те же
         `FCP` 2.7 с, `LCP` 3.4 с и 87 баллов. Фон не при чём, в задаче 57
         его не трогать.
- [x] 56. Оптимизировать изображения: вес, форматы, размеры под точки останова
      Было: один файл кадра на все экраны и ни одного `srcset` на сайте.
      Мобильная главная скачивала 192 КБ картинок, из которых около сотни
      браузер выбрасывал при масштабировании (находка 4 задачи 55), а в карточку
      «Смотрите также» с пропорцией 4:3 грузился портретный герой услуги 4:5 —
      четверть скачанных пикселей срезал `object-fit`.
      Что сделано:
      1. **`generator/images.py` — таблица слотов вёрстки.** Для каждого места
         (герой главной, герой услуги, обложка раздела, карточка «Смотрите
         также») лежат лестница ширин, строка `sizes` и пропорция кадра.
         Таблица одна на двоих: по ней режутся файлы и по ней же собирается
         разметка, разъехаться им негде. `sizes` не выдуман, а посчитан из CSS:
         ширина контейнера 1200, поля 24, промежуток сетки 24, раскладка
         1 → 2 → 3 или 4 колонки.
      2. **`generator/make_images.py` — нарезка производных.** Рядом с
         полноразмерным `hero.webp` появились `hero-320.webp` и далее по
         лестнице; для карточек добавлен отдельный кроп 4:3 `card-<slug>.webp`
         — тот самый кадр, который сейчас обрезал браузер, только без
         скачивания лишнего. Скрипт запускается руками при смене картинок,
         сборка страниц их не трогает.
      3. **Макрос `responsive_img`** вместо четырёх разных `<img>` в шаблонах.
         Заодно исправлены объявленные размеры: у карточек стояло `600×440`
         при файле 900×675 (находка 5 задачи 55) — теперь атрибуты берутся
         из той же таблицы, что и файлы.
      4. **Предзагрузка героя получила `imagesrcset`/`imagesizes`.** Без них
         `preload` тянет один файл, а разметка потом просит другой — вместо
         ускорения выходит двойной вес. Проверка на это добавлена в
         `check_content.py`.
      5. **Мастер-кадры JPG (1,8 МБ, 24 файла) убраны из публикуемой папки**
         в `generator/sources/img/`. Их никто никогда не запрашивал: JPG-фолбэк
         из `CREDITS.txt` в разметку так и не попал, а в деплой они ехали.
      6. **Качество WebP снижено с 82 до 78.** На самом сложном кадре сайта
         (волосы крупным планом) PSNR 37,4 дБ, разницы с q82 при попиксельном
         сравнении не видно, файл легче на 17%. Полноразмерные кадры
         перекодированы: `hero.webp` 34,3 → 29,5 КБ, `tokio-inkarami.webp`
         93 → 80 КБ.
      7. **`check_content.py` научился двум проверкам:** каждый файл из
         `srcset` существует и его настоящая ширина совпадает с дескриптором;
         `imagesrcset` предзагрузки совпадает с `srcset` картинки. Обе
         проверены на подложенной ошибке — ловят.
      Результат (Lighthouse, мобильная эмуляция 412×823, DPR 1,75):
      главная 192 → 107 КБ картинок, раздел 67 → 29 КБ, услуга 17 → 9 КБ.
      На странице услуги замечание `image-delivery` снято совсем, на разделе
      тоже; на главной осталось 26 КБ «перерасхода», но это спор с методикой:
      Lighthouse сравнивает файл с размером в CSS-пикселях (284) и не
      учитывает плотность экрана — отдать на экран 1,75× картинку шириной
      284 значит показать мыло. Ширины подобраны под реальную плотность.
      Не сделано намеренно: **AVIF не добавлен.** Кодера в окружении нет,
      ставить libavif ради 4–7 КБ на кадр поверх уже сжатого WebP —
      несоразмерная цена за новую зависимость сборки.
- [x] 57. Проверить LCP, CLS и render-blocking, добиться Lighthouse 100 как на vn
      `CLS` проверять было нечего: 0 на всех прогонах ещё в задаче 55, размеры
      картинок объявлены, шрифты не двигают вёрстку. Работа свелась к весу
      первого экрана и к двум проверенным гипотезам.
      Что сделано:
      1. **Шрифты: десять файлов и 220 КБ → пять и 70 КБ.** С Google Fonts
         гарнитура приезжает разрезанной по алфавитам, и каждое начертание
         тянуло два файла. Латинский кусок весил больше кириллического
         (24 КБ против 14 у Manrope) и приходил всегда: в его `unicode-range`
         попадают цифры и знаки препинания, а они есть на любой странице,
         при том что внутри — три сотни знаков под все европейские языки.
         `generator/make_fonts.py` режет из вариативного мастера по одному
         файлу на начертание сразу с обоими алфавитами и только с нужными
         сайту знаками. Мастера и лицензии OFL — в `generator/sources/fonts/`.
      2. **Мёртвое начертание Cormorant 500 удалено** — два файла на 57 КБ
         и два правила в render-blocking бандле, при том что заголовки
         набраны только 600-м. Браузер их не качал, но в деплой они ехали.
      3. **Контраст счётчика на вкладке прайса** (находка 6 задачи 55):
         фон активной вкладки переведён с `--rose` на `--rose-deep`. Стало
         4,74 у счётчика и 6,33 у подписи при норме 4,5. Доступность страницы
         услуги 97 → 100, и это было последнее замечание доступности на сайте.
      4. **Лишний пересчёт вёрстки в `nav.js`** (находка 8 задачи 55): первый
         замер `scrollY` перенесён в `requestAnimationFrame`. Синхронное чтение
         сразу после правки классов стоило 37 мс; замечание снято.
      5. **`check_content.py` проверяет, что каждый знак страницы есть
         в подрезанных шрифтах.** Подрезка меняет цену опечатки: латинская «z»
         в названии аппарата отрисуется системным шрифтом, и в вёрстке это
         одна буква не в ту гарнитуру. Проверена на подложенном «Zürich» —
         ловит `ü` и пропускает буквы из набора.
      Две гипотезы проверены и **отвергнуты замером**, обе не надо делать:
      6. **Встраивание CSS в `<head>` делает хуже.** Lighthouse обещал
         «сэкономить 300 мс» на render-blocking бандле, но A/B на локальной
         копии с gzip и кешем как на GitHub Pages дал обратное: `FCP` тот же
         1,2 с, `LCP` 1,5 → 1,8 с, балл 100 → 99. Предупреждение исчезает,
         метрики ухудшаются: раздутый документ отодвигает момент, когда
         сканер предзагрузки находит картинку первого экрана. Бандл в 6,5 КБ
         gzip остаётся отдельным файлом.
      7. **Отложить Метрику до `load` не помогает.** Вариант с переносом
         счётчика в обработчик `load` дал ровно те же 100 баллов и те же
         77 «лучших практик»: замечание категории — сторонние куки, а их
         Lighthouse видит независимо от момента загрузки.
      Результат на боевом домене, мобильный профиль, медиана трёх прогонов:
      главная 88 → 99, раздел 92 → 93, услуга 92 → 94; доступность 100 на всех
      трёх (была 97 на услуге), SEO 100, лучшие практики 77 без изменений.
      Вес страницы: главная 512 → 306 КБ и 27 → 23 запроса, раздел 403 → 227
      и 23 → 18, услуга 362 → 215 и 23 → 18. Без учёта Метрики, то есть
      по своим файлам, вес упал на 51–60%.
      Разброс баллов от прогона к прогону (91–100 на одной и той же странице)
      целиком объясняется `TBT`: 380 мс → 91 балл, 140 мс → 98. Это время
      исполнения `tag.js` Метрики, и попадает оно в окно замера или нет —
      зависит от задержки сети.
      **Потолок остаётся за Метрикой, и он не берётся кодом.** Эталон «100 как
      на vn» недостижим ни здесь, ни там: `vn.neva.beauty` в том же профиле
      даёт 92/100/77/100 (задача 55, находка 9). На сайте нет ни одного
      замечания, которое чинится вёрсткой или сборкой; всё, что осталось, —
      сторонние куки, неиспользуемый код и время исполнения счётчика плюс
      `max-age=600` от GitHub Pages. Решение об отказе от вебвизора или
      от самой Метрики — за заказчиком, это задача 68.
      Побочная находка: **знака ฿ нет ни в Manrope, ни в Cormorant** — во всех
      ценах на сайте бат рисуется системным шрифтом. Так было и до правок;
      если это заметно раздражает, лечится подмешиванием третьей гарнитуры
      только под один знак.
- [x] 58. Проверить качество кода: мёртвые стили, неиспользуемый JS, дубли.
      Мёртвая ветка `.pricelist__combos` со стилями `.combo*` уже удалена
      в задаче 49 — остальное проверяется здесь.
      Проверка сплошная, не выборочная: скрипт сверил каждый селектор всех
      девяти CSS-файлов с разметкой 25 собранных страниц и с четырьмя файлами
      JS, отдельно — объявленные и использованные CSS-переменные; `pyflakes`
      прошёл по генератору и тестам; отдельный проход искал повторяющиеся
      блоки в шаблонах Jinja.
      Главная находка — не мёртвый код, а **три переменные-призрака**:
      1. **`--sp-10`, `--sp-14`, `--sp-20` использовались, но нигде не были
         объявлены.** Браузер в таком случае молча выбрасывает всё свойство,
         и вместо запасного значения получается ноль. Замером в браузере:
         `var(--sp-10)` давал `normal`, `var(--sp-20)` — `0px`. Последствия
         были видны на трёх правилах: у первого экрана главной и страницы
         услуги в диапазоне 768–1023 px пропадал зазор между колонками
         (должно 40 px) и оба вертикальных отступа (должно 80 и 56 px),
         а у блока `.cta-inline` верхний отступ отсутствовал на всех ширинах.
         Токены добавлены в шкалу (`--sp-10:2.5rem` и далее — имя равно
         четвертям rem, как у остальных). После правки замер даёт 40 и 80 px.
      2. **`--font-display` в `legal.css` не существует** — заголовки
         «Политики» держались на запасном значении внутри `var()`. Заменено
         на реальный `--font-head`.
      3. **`--fs-700` объявлен и не использован** — удалён.
      Чтобы это не вернулось, `check_content.py` теперь проверяет, что каждая
      `var(--x)` без запасного значения объявлена в CSS, в разметке или в JS.
      Проверена на подложенной опечатке `--tracking-tight` — ловит.
      Остальное:
      4. **Мёртвая разметка:** классы `.booking__main` и `.site-footer__brand`
         стояли на всех 23 и 25 страницах, стилей под ними нет — убраны
         из шаблонов.
      5. **`price-tabs.js` грузился на 17 страницах, а нужен на 5.** Прайс
         с одним разделом вкладок не рисует, и переключать там нечего. Скрипт
         подключается по условию; на страницах без вкладок прайс по-прежнему
         виден (панель помечена активной в разметке) и лишнего запроса больше
         нет. Проверено кликом по вкладкам на `/lazernaya-epilyaciya/`.
      6. **Мёртвые иконки `activity.svg` и `send.svg`** удалены.
      7. **Дубли сведены.** Четыре пары правил совпадали дословно:
         карточка с фото (`.cat-card` на главной и `.related-card` на услуге),
         плитка преимущества (`.benefit` / `.svc-benefit`), свечение и фото
         первого экрана (`.hero__*` / `.svc-hero__*`) и подчёркнутая ссылка
         в абзаце (`.faq__a a` / `.booking__alt a`). Общее тело перенесено
         в `components.css`, в `home.css` и `service.css` остались только
         различия — размеры и кегль. `home.css` 67 → 46 строк, `service.css`
         99 → 79, минифицированный бандл 31 569 → 30 171 байт.
         **Выигрыш здесь в сопровождении, а не в весе:** после gzip разница
         5 байт из 6,3 КБ — повторы всё равно сжимались. Ценность в том, что
         правка тени или кривой перехода теперь делается в одном месте.
      Что рефакторить **не стал**: пять адаптивных сеток (`.cat-grid`,
      `.benefits-grid`, `.svc-benefits`, `.related-grid`, `.popular-grid`)
      совпадают только идиомой «одна колонка → две → три-четыре», а число
      колонок, шаг и точки останова у всех разные. Общий класс потребовал бы
      править разметку ради экономии десятка строк и размазал бы каждую сетку
      по двум файлам.
      Проверено, что вёрстка не поехала: снят слепок вычисленных стилей —
      все свойства каждого элемента пяти страниц на ширинах 390, 900 и 1400
      (6168 элементов), до и после сведения дублей. Расхождений по существу
      ноль; отличаются только пятна «авроры» (бесконечная анимация, ±1 px)
      и момент срабатывания появлений при скролле.
      Мёртвого кода в остальном нет: неиспользуемых функций, констант
      и импортов в генераторе `pyflakes` не нашёл, все четыре файла JS живые
      и все их крючки (`data-header`, `data-drawer`, `data-pricelist`,
      `data-reveal`) есть в разметке, все 22 изображения используются,
      разметка шаблонов не дублируется — повторяются только импорты
      партиалов, что в Jinja неизбежно.
      Побочная находка для задачи 59: **папка `assets/icons/` (80 КБ, 20 файлов)
      деплоится зря.** Иконки встраиваются в HTML на сборке, ни одна страница
      не запрашивает их по ссылке. Место им в `generator/sources/` рядом
      с мастерами шрифтов и картинок.

---

## Этап 10. Чистка и документация

- [x] 59. Удалить рабочие артефакты: одноразовые скрипты, временные отчёты, черновики
      Одноразовых скриптов и черновиков в репозитории не нашлось: всё в
      `generator/` — рабочие части сборки (`build.py`, `images.py`, `schema.py`),
      две проверки-ворота (`check_prices.py`, `check_content.py`) и две ручные
      утилиты пересборки исходников (`make_images.py`, `make_fonts.py`).
      Отчёты в `docs/audits/` — не временные: на них ссылаются задачи этапов 5–8.
      Настоящая находка та же, что в задаче 58, но шире, чем думали: в публикуемой
      папке лежали не только иконки. Сверено по разметке — страницы ссылаются
      только на `bundle.min.css`, а рядом с ним лежали все девять слоёв-исходников,
      из которых он склеен. Уехало из деплоя:
        * `assets/icons/` → `generator/sources/icons/` — 80 КБ, 20 SVG.
          Встраиваются в HTML на сборке, по ссылке не запрашиваются.
        * `assets/css/{aurora,base,components,fonts,home,legal,reveal,service,
          tokens}.css` → `generator/sources/css/` — 62 КБ. В деплое остаётся
          только `bundle.min.css` (30 КБ), папка `assets/css` теперь 92 → 32 КБ.
        * `assets/img/CREDITS.txt` → `generator/sources/img/CREDITS.txt` — 6 КБ.
          Это документация пайплайна изображений для разработчика, а не файл
          сайта; теперь лежит рядом с мастер-кадрами, которые описывает.
      Правки в коде: `build.py` — `ICONS` и `build_css_bundle()` читают из
      `sources/`, папку `assets/css` бандл создаёт сам; `check_content.py` —
      проверка CSS-переменных читает слои из `sources/css` (заодно ушёл костыль
      с пропуском `bundle.min.css`: в исходниках его нет); `make_fonts.py` —
      пишет `fonts.css` в `sources/css`.
      Проверено: собранный `bundle.min.css` побайтово тот же, ни один HTML не
      изменился — значит отпечатки `?v=` в ссылках на ассеты те же и кэш
      посетителей не сбрасывается. Сборка с нуля из удалённой `assets/css`
      проходит. `check_prices.py`, `check_content.py` (25 страниц) и 85 тестов
      зелёные.
      **Правило на будущее: в `th.neva.beauty/` лежит только то, что скачивает
      браузер. Всё, из чего это собрано, — в `generator/sources/`.**
- [x] 60. Написать README репозитория с описанием архитектуры и запуска
      Репозиторий портфолио, поэтому README — не инструкция разработчику,
      а презентация работы потенциальному заказчику. Подача взята с
      `vn.neva.beauty`: баннер, бейджи, ссылка на прод, витрина направлений,
      таблица технологий, mermaid-схема сборки, «ключевые инженерные
      решения», структура проекта, запуск, деплой, контент сайта.
      Файлы: `README.md` (русский), `README.en.md` (английский, полный
      перевод, переключатель языков вверху обоих) и `docs/banner.svg` —
      баннер в палитре сайта (`tokens.css`) с надписью Koh Samui и чипами
      стека. Проверен рендером: текст не наезжает и не вылезает за поле.
      Витрина — шесть кадров разделов, 3×2, из опубликованных `-700.webp`.
      Продающая рамка проекта: перенос действующего салона с конструктора
      Tilda на свой генератор — без CMS, базы, платного хостинга и
      абонплаты, с полным сохранением прайса. Главный аргумент вынесен
      в лид: 204 цены и качество 25 страниц под автотестами в конвейере
      деплоя, ошибка в цене или битая ссылка до прода не доезжают.
      В «инженерных решениях» девять пунктов, каждый с фактом, а не
      с обещанием: единая таксономия, парити-тест цен и плейсхолдеры
      `{price:…}` в текстах, сквозные проверки `check_content.py` вплоть
      до знака, которого нет в подрезанном шрифте, граф JSON-LD с `Offer`
      на каждую позицию и флагом надбавок, свой пайплайн картинок,
      скорость (шрифты 220 → 70 КБ, вес страниц −51…60%, Lighthouse
      A11y 100 / SEO 100 / Perf 93–99, CLS 0, две отвергнутые замером
      гипотезы), честный `lastmod`, `base_path` для превью, `llms.txt`.
      Числа сверены с данными и отчётами, а не взяты из головы:
      6 разделов, 17 услуг, 25 страниц, 204 позиции, 85 тестов; цифры
      скорости — из задач 55 и 57. Команды из README прогнаны как
      написаны, все три ворот зелёные; прод отвечает 200.
- [x] 61. Проверить `.gitignore` и что в репозитории нет лишнего
      Проверено не по именам файлов, а сверкой «что лежит» против «что кто-то
      запрашивает»: 331 файл в индексе, рабочее дерево и индекс совпадают
      полностью — незакоммиченных и забытых файлов нет.
      Деплой чист: из 197 файлов `th.neva.beauty/assets/` на все 197 есть
      ссылка из HTML, CSS, JS, `sitemap.xml` или `llms.txt`, и ни одна ссылка
      не ведёт в пустоту. Правило задачи 59 держится: в публикуемой папке
      только то, что скачивает браузер. Четыре шрифта поначалу выглядели
      лишними — их подключает `bundle.min.css` относительным путём
      `../fonts/`, а не от корня; вес 500–700 в бандле реально используется
      (8, 17 и 6 объявлений).
      Исходники сверены обратной проверкой — не «есть ли файл», а «читает ли
      его сборка». Все 9 слоёв CSS в `CSS_BUNDLE_ORDER`, все 24 мастер-кадра
      названы в `content.yml`. Нашлась одна мёртвая иконка:
        * `generator/sources/icons/instagram.svg` — удалена. `icon()` берёт
          файл по имени, а имени `instagram` нет ни в одном шаблоне, ни в
          `content.yml`. Причина: в дровере иконка Instagram врисована в
          разметку прямо, рядом с WhatsApp и Telegram, — из lucide-набора
          она не бралась никогда. Осталось 19 иконок, все вызываются.
      Правки в `.gitignore` — обе про риск утечки, а не про порядок:
        * `.pytest_cache/` добавлен явно. Раньше папка не попадала в git
          только потому, что pytest кладёт внутрь себя свой `.gitignore`.
          Защита чужая и держится на поведении стороннего инструмента.
        * `Данные и промты/` добавлен рядом со старым `Данные и промты /`.
          Папка на диске названа с лишним пробелом перед слешем, правило
          было написано под это имя буквально. Стоило бы переименовать
          папку — и промты с данными заказчика перестали бы игнорироваться
          в публичном репозитории. Теперь правило ловит оба имени.
      Секретов в отслеживаемых файлах нет: поиск по ключам, токенам, паролям
      и заголовкам приватных ключей дал только совпадения на слово `token`
      в коде подстановки плейсхолдеров. `deploy.yml` работает на встроенном
      `id-token`, своих секретов не требует. Самый тяжёлый файл репозитория —
      переменный шрифт Cormorant, 560 КБ, это исходник для подрезки.
      Проверено: сборка после удаления иконки проходит, ни один файл в
      `th.neva.beauty/` не изменился; `check_prices.py` (204 позиции),
      `check_content.py` (25 страниц) и 85 тестов зелёные.
- [x] 62. Финальный прогон всех проверок и тестов на чистом клоне
      Проверялась не рабочая копия, а `git clone` с GitHub в пустую папку:
      330 файлов, 19 МБ, HEAD `bfdc511` — тот же, что локально. Питон 3.12.13,
      как в CI; восемь пакетов `requirements.txt` встали в закреплённых версиях.
      Команды взяты из README как написаны, все три ворот зелёные: `build.py`
      собрал сайт за 1,8 с, `check_prices.py` — 204 позиции, `check_content.py` —
      25 страниц, `pytest` — 85 тестов за 0,09 с.
      Главное, что дал чистый клон: сборка воспроизводима. После `build.py`
      `git status` пустой — генератор из тех же исходников выдал папку
      `th.neva.beauty/` байт в байт такой, какая лежит в репозитории. Значит
      опубликованное действительно собрано этим кодом, а не правилось руками.
      Сверка с продом сплошная, а не выборочная: все 227 файлов сайта скачаны
      с `https://th.neva.beauty` и сравнены по SHA-256 — совпали 227 из 227,
      ни одного расхождения и ни одного ответа кроме 200. Прод, репозиторий
      и свежая сборка — одно и то же. Последний прогон CI на этом коммите
      тоже зелёный (36 с).
      Мусор после прогона (`.venv`, `.pytest_cache`, два `__pycache__`) в git
      не просочился — правки `.gitignore` из задачи 61 отработали.

---

## Отдельные задачи

- [x] 102. Прогон обновлённого SEO-промта по всему сайту. В базе знаний появился
      новый шаблон `Промт — SEO-оптимизация страницы (шаблон).md`; задача —
      адаптировать его под th.neva.beauty и прогнать по всем 24 страницам.
      Отчёт: `docs/audits/2026-08-31-seo-audit-po-obnovlennomu-promtu.md`.

      **Промт и файл констант переписаны.** Старая адаптация описывала сайт,
      которого больше нет: Tilda, незавершённый переезд, инструкция «открой
      страницу и правь». Главное добавленное правило — собранный HTML это
      артефакт, правки идут в генератор. `neva-samui-facts.md` был опаснее:
      он всё ещё предлагал перманентный макияж (снят задачей 94), старые слаги
      и старый Instagram — агент по такому файлу вернул бы на сайт услугу,
      которой салон не оказывает. Переписан в профиль проекта с разделом
      «решения, которые не надо пересматривать» (65, 69, 71, 81, 94).

      **Шесть находок, все закрыты:**
      1. Ни одна страница не сообщала дату проверки материала. Для YMYL это
         сигнал доверия, а дата была ровно одна — `lastmod` в sitemap, видимый
         краулеру и невидимый на странице. Мешал замкнутый круг: подставить дату
         значит изменить страницу, а значит и её дату. Разорван тем же приёмом,
         что и версии ассетов: рендер с заглушкой `0000-00-00`, отпечаток по ней,
         дата подставляется после. Дата считается теперь при записи страницы
         (`write_page`), а не чтением 24 файлов с диска после сборки;
         `page_file()` стал не нужен и удалён.
      2. `Service` не знал ни своей картинки, ни как на услугу записаться —
         добавлены `image` и `availableChannel` → `ServiceChannel`.
      3. Звенья `ItemList` в разделах вели на голые адреса: список раздела
         и услуга были в графе двумя сущностями с одним адресом. Теперь `item`
         ссылается на `@id` узла `Service` целевой страницы, добавлено
         `numberOfItems`.
      4. Главная не говорила, что у салона есть разделы: пять карточек на виду,
         в графе ничего. Добавлен `ItemList` направлений, `mainEntity` на него.
      5. `Organization` не называл своих тем — добавлен `knowsAbout` из 16
         названий услуг, собирается из `content.yml`.
      6. 14 заголовков из 23 занимали 29–48 знаков при доступных 60. Переписаны,
         второй запрос в каждом взят со страницы. Теперь все 22 индексируемые
         в 50–59, первые 20 знаков по-прежнему уникальны.

      **Сверх промта:** в `meta robots` добавлены `max-image-preview:large`
      (крупный кадр рядом с результатом вместо миниатюры) и `max-snippet:-1`
      (ответ FAQ в выдаче целиком, а не обрезанный).

      **Новые ворота:** `TITLE_MIN = 45` только для индексируемых страниц,
      `check_page_dates()` сверяет `dateModified` с `lastmod` в sitemap,
      `tests/test_schema_graph.py` (7 тестов) и 3 теста на поведение даты
      в `test_seo_output.py`.

      Проверено по всем 24 страницам, не выборочно: метатеги, крошки, семантика,
      82 картинки, перелинковка, вопросы FAQ против разметки. Ворота зелёные:
      24 страницы, 193 позиции прайса, 111 тестов. Две пересборки подряд дают
      одинаковый `lastmod.json`.

      Вне кода осталось: Telegram `@shyrakras` (личный хэндл от старого бренда),
      именованный специалист с дипломом для YMYL, реальные часы работы,
      `datePublished` (настоящих дат публикации нет), диапазон цен
      ботулинотерапии в `llms.txt` и IndexNow.

- [x] 101. Дизайн сведён с vn.neva.beauty. Просьба заказчика 2026-08-31: у бренда
      два сайта, и оформление должно совпадать настолько, насколько позволяет
      разный набор услуг. Сверены токены, все восемь слоёв CSS, шаблоны шапки,
      подвала, меню и дровера, четыре скрипта. Сверка машинная: объявления
      разобраны по селекторам и сопоставлены, а не просмотрены глазами, —
      иначе половина расхождений тонет в разном порядке правил и комментариях.

      **Что исправлено здесь:**
      1. Иконки в кнопках рисовались по 24 px вместо 18. На vn размер задаёт одно
         общее правило `.btn .icon`; на th его не было, размер стоял точечно
         у кнопок записи и плашки призыва, а в полосе контактов иконка приезжала
         своим размером из lucide. Добавлено общее правило, два точечных убраны.
      2. Крошки переведены на схему vn: отступ от шапки даёт `.breadcrumbs--lead`,
         секция под ними обнуляет свой верхний `padding` через `.section--lead`.
         Свой класс `cat-hero` из задачи 99 убран — он решал ту же задачу вторым
         способом. На телефоне отступ вырос с 1.25 rem до 2 rem; «Записаться»
         на первый экран услуги по-прежнему попадает, проверено на 390 px.
      3. Шрифты: не было подложки с подогнанными метриками. `font-display:swap`
         рисует текст системным шрифтом сразу, а Georgia шире Cormorant на 14% —
         первые секунды страница выглядела чужой и прыгала при подмене. Перенесены
         два `@font-face` с `size-adjust` из vn, `--font-head` и `--font-body`
         теперь их называют. Сами файлы шрифтов остались наши: они подрезаны
         по знакам (70 КБ против 96 КБ у vn), и пайплайн менять незачем — правило
         дописано в `make_fonts.py`, чтобы пережить перегенерацию.
      4. Сорвавшаяся картинка: перенесены retry-скрипт из `<head>` и плашка
         `img[data-img-error]`. Раньше Chromium рисовал на её месте текст alt —
         он не подчиняется размерам `<img>` и читается как поломка вёрстки.
      5. Замок бренда: просвет `.8rem` и `white-space:nowrap` на названии — как
         на vn. На 320 px шапка по-прежнему без горизонтальной прокрутки.
      6. Заголовки правовых страниц: вес 500 → 600. Начертания 500 у Cormorant
         на сайте нет, браузер всё равно подставлял 600 — значение было неправдой.
      7. `.related-card__body :is(h2,h3)` вместо `h3`: правило слово в слово как
         на vn, вид карточки не меняется.

      **Что перенесено отсюда на vn** (коммит в репозитории vn.neva.beauty):
      контраст активной вкладки прайса (`--rose` → `--rose-deep`), правый край
      крайнего дропдауна, ловушка фокуса в дровере и один открытый список за раз,
      `role="dialog"` у дровера, перенос длинных слов в заголовках, токен шрифта
      и прокрутка широкой таблицы на правовой странице, поджатый первый экран
      услуги, общий класс знака `.brand-mark__flower`, зазор в шапке.

      **Осталось разным намеренно.** Кнопка «Записаться» в шапке: у нас она
      появляется с 1100 px, на vn с 1024 — у th на два раздела больше, и втроём
      логотип, меню и кнопка на 1024 не помещаются (задача 90). Цветок в знаке
      (плюмерия против лотоса), город в замке и блоки, которых на vn просто нет:
      сравнения, факты над прайсом, значок «акция», счётчики на вкладках, ссылки
      на соседние разделы, «Или так» под кнопкой записи.

      Проверка: 101 тест, `check_content.py`, `check_prices.py`, обход пяти
      страниц на семи ширинах от 320 до 1280 — горизонтальной прокрутки нет
      нигде, крошки на разделе и на услуге стоят на одной высоте.

- [x] 100. Полоса контактов оставлена только на главной, как на vn.neva.beauty.
      Заказчик 2026-08-31 прислал скриншот: кнопки Telegram и WhatsApp уезжали
      влево от середины полосы. Воспроизведено на ширине 560–767 px: сетка полосы
      на этой ширине шла в две колонки, кнопки попадали в первую ячейку второго
      ряда и центрировались по ней, а не по полосе. На трёх колонках (от 768 px)
      и на одной (до 560 px) всё стояло ровно, поэтому раньше и не заметили.
      Сверились с vn.neva.beauty: там полоса стоит только на главной, стили лежат
      в `home.css`, а промежуточной сетки в две колонки нет вовсе — одна колонка
      до 768 px и сразу три после. Решение заказчика: сделать так же.
      Сделано: `{{ contact_strip(site) }}` снят со `service.html.j2`
      и `category.html.j2`, стили `.contact-strip` переехали из `components.css`
      в `home.css`, брейкпоинт 560 px убран. Партиал оставлен файлом, его зовёт
      только `home.html.j2`.
      Плата: на услуге и в разделе больше нет строки «о. Самуи, Таиланд / Приём
      по записи» — довод из задачи 97 о клиенте, который приходит из поиска сразу
      на внутреннюю страницу, снят в пользу единообразия с vn. Контакты там
      остаются в блоке записи и в футере, разметка организации не затронута.

- [x] 99. Хлебные крошки в разделах стояли ниже, чем на страницах услуг.
      Замечено заказчиком 2026-08-31 по двум скриншотам рядом.
      Причина: у услуги крошки лежат в `.svc-hero` с собственным
      `padding-top:var(--sp-5)` (`--sp-8` от 768 px), у раздела — в обычной
      `.section` с `padding-block:var(--sp-24)`, то есть 6 rem сверху.
      Стало: первой секции раздела добавлен класс `cat-hero` с тем же верхним
      отступом, что у `.svc-hero .breadcrumbs`; нижний отступ секции прежний.

- [ ] 98. Обновить версии actions в `.github/workflows` — они собраны под Node 20,
      который GitHub выключает. Найдено 2026-08-31 в аннотациях прогона 33349378078
      (деплой задачи 64): «Node.js 20 is deprecated... being forced to run on
      Node.js 24». Сейчас прогон зелёный — раннер подставляет Node 24 сам, — но
      это временная подпорка, а не совместимость. Когда её уберут, деплой встанет.
      Что стоит и что актуально на 2026-08-31:
        * `actions/checkout@v4` → `v7.0.1`;
        * `actions/setup-python@v5` → `v7.0.0`;
        * `actions/upload-pages-artifact@v3` → `v5.0.0`;
        * `actions/deploy-pages@v4` → `v5.0.0`.
      У всех, кроме третьего, в `action.yml` уже `using: node24` — проверено
      чтением самих файлов, а не по обещаниям в описании.
      **Третий пункт объясняет странность в аннотации:** там ругались и на
      `actions/upload-artifact@v4`, которого в нашем workflow нет ни строчки.
      Он приезжает внутри `upload-pages-artifact@v3` — это composite-действие,
      и на строке 77 своего `action.yml` оно зовёт `upload-artifact@v4`. В v5.0.0
      тот же вызов уже прибит к SHA версии v7.0.0. То есть чинится всё одним
      обновлением, руками до внутреннего шага лезть не надо.
      Осторожность при подъёме: у `checkout` и `setup-python` перескакиваем через
      два-три мажора, а мажор на то и мажор, что меняет поведение. Читать
      changelog обоих, особенно `setup-python` — там между версиями менялись
      дефолты кэширования, а у нас зависимости ставятся голым `pip install`
      без кэша, и молчаливое включение кэша поменяет время прогона и поведение
      при смене `requirements.txt`.
      Проверка после правки простая: сайт от этого не меняется ни на байт,
      значит достаточно зелёного прогона и живой страницы после деплоя.
      Не срочно: пока Node 24 подставляется принудительно, всё работает.

- [x] 97. Полоса контактов: «Позвонить» заменена на Telegram, обе связи — кнопками.
      Просьба заказчика 2026-08-30: сделать как на vn.neva.beauty, где вместо
      «Позвонить» стоит Telegram и обе связи оформлены кнопками.
      Было: четыре одинаковых строки-ссылки в ряд — локация, часы, «Позвонить»,
      «WhatsApp». Справка и действие выглядели одинаково.
      Стало как на vn: две справочные строки и блок `.contact-strip__actions`
      с двумя кнопками `btn--ghost btn--sm` — Telegram и WhatsApp. Сетка полосы
      с четырёх колонок переведена на три, добавлены `.btn--sm` и правило
      `.contact-strip__actions .btn .icon{color:var(--rose)}`, чтобы иконка в кнопке
      была того же цвета, что в справочных строках слева.
      Иконка `send` взята из той же lucide-static v1.17.0, что и остальные на сайте.
      Первая новая иконка после починки `.gitignore` в задаче 95 — до неё файл
      просто не попал бы в коммит.
      Ссылка WhatsApp получила заранее подставленный текст «Здравствуйте! Хочу
      записаться», как во всех остальных кнопках записи на сайте; в полосе его
      раньше почему-то не было.
      **Телефон убран только из полосы.** Номер российский (+7), для салона
      в Таиланде звонок по нему клиенту не бесплатен и не привычен, а оба
      мессенджера работают одинаково откуда угодно. Сам номер остался ссылкой
      в блоке записи и в разметке организации — со страниц он не исчез.
      Когда появится тайский номер (задача 69), решение стоит пересмотреть.
      **Пересматривать нечего: 2026-08-31 задача 69 закрыта отказом**, тайского
      номера не будет, и полоса остаётся с двумя мессенджерами навсегда.
      Полоса стоит на всех страницах, а не только на главной (так было и раньше,
      причина в комментарии шаблона), поэтому изменились все 24.
      Проверено на 390, 560, 768 и 1440: горизонтальной прокрутки нет нигде.
      Известное поведение, унаследованное от образца: на 560–1023 px колонка под
      кнопки уже их суммарной ширины, и они встают в столбик. У vn при тех же
      правилах и тех же размерах кнопок ровно так же. Если мешает — лечится одной
      строкой: `grid-column:1/-1` для блока действий на этом диапазоне.
      Розовая заливка Telegram на скриншоте от заказчика оказалась состоянием
      наведения, а не отдельным стилем: обе кнопки одинаковые, проверено ховером.

- [x] 96. Карточка превью ссылки: сток вместо бренда, да ещё и с чужой услугой.
      Поднято заказчиком 2026-08-30 следом за задачей 95: «стиль должен быть везде
      одинаковый». При разборе выяснилось, что дело не только в стиле.
      Было: `assets/img/og-cover.jpg` — стоковое фото девушки с маникюром, ни знака,
      ни названия, ни города. Это картинка, которую видит каждый, кому кинули ссылку
      в WhatsApp, Telegram или Facebook. **Маникюра и педикюра в прайсе нет ни одной
      позиции** — сток рекламировал услугу, которой салон не оказывает. Тот же файл
      стоит в `business.image`, то есть его же Google берёт как картинку бизнеса.
      Стало: карточка 1200×630 по образцу vn — круглая печать с плюмерией слева,
      справа название Cormorant'ом, «KOH SAMUI» и строка из пяти разделов сайта.
      Шаблон `generator/templates/og-card.html.j2`, рендер в `make_brand_assets.py`
      через `qlmanage`: настоящие Cormorant и Manrope из `assets/fonts` даёт только
      браузерный движок, Pillow тут не годится.
      Отличие от vn: там карточка подключает отдельные `fonts.css` и `tokens.css`,
      а на этом сайте на выходе только `bundle.min.css` — шрифты и переменные палитры
      берутся из него, они лежат в начале бандла.
      Разделы в строку подставляются из `content.yml`, не выписаны руками: после
      снятия перманентного макияжа (задача 94) их пять, и карточка это уже учла сама.
      Имя файла оставлено прежним — `og-cover.jpg`, а не `og-default.jpg` как на vn:
      на него ссылаются `og_image.default` и `business.image` в `site.yml`, и менять
      имя ради симметрии с соседним репозиторием значило бы трогать разметку всех
      страниц без выигрыша. Из-за этого ни одна страница не изменилась и `lastmod`
      никому не сдвинулся — поменялся только сам файл.
      Проверено: 1200×630 JPEG, 55 КБ (было 78 КБ стока), ворота зелёные —
      24 страницы, 101 тест, 193 позиции.
      **Хвост, найденный уже после деплоя:** `og:image:alt` на главной остался от
      стока — «Безупречная кожа и макияж…». Он подставлялся из `hero_image_alt`,
      то есть описывал фотографию первого экрана, а не карточку превью; после замены
      картинки подпись стала прямо ложной, да ещё и про макияж, которого салон не
      оказывает. Подпись переехала в `site.yml` → `og_image.default_alt`, рядом с самой
      картинкой: alt принадлежит изображению, а изображение здесь — константа бренда.
      Запасной вариант в `base.html.j2` тоже переведён на неё, поэтому `/privacy/`
      и `/404.html` больше не отдают вместо подписи голое название бренда.
      Страницы услуг и разделов не тронуты: у них свои фотографии и свои честные alt.

- [x] 95. Логотип и фавикон: бренда на сайте не было вообще.
      Найдено заказчиком 2026-08-30. В `favicon.svg` лежала заглушка на 272 байта —
      буква N в скруглённом квадрате, сделанная 30 июля при первой сборке. Знака
      плюмерии из фирменной печати на сайте не было ни в шапке, ни в подвале,
      ни в `schema.org`: в разметке организации логотипом числилась та же буква.
      Разложено по образцу vn.neva.beauty, где эта система уже выстроена.
      Знак: `generator/sources/icons/plumeria.svg` — пять лепестков через 72°
      вокруг точки 500 420.6 плюс сердцевина, вырезаны из
      `Материалы/logo-варианты/E-плюмерия-печать-светлая.svg` **в исходных
      координатах печати 1000×1000**. Правило то же, что на vn: знак на сайте и
      знак на аватарах соцсетей должны быть геометрически одним цветком, а не
      двумя похожими. Цвета уже совпали с палитрой — `#CE9B91` это токен
      `--rose-soft`, `#92655E` у сердцевины это `--rose`.
      **Сердцевину чуть не потеряли.** В первом заходе знак собрался из одних
      лепестков, и на иконке в середине встала белая звезда: в центре лепестки
      не смыкаются, просвет закрывает отдельный `<circle r="28.2">`. Видно это
      только на отрендеренной картинке, в разметке ничего не сигналит.
      Растр: `generator/make_brand_assets.py` — порт скрипта с vn. Собирает
      `favicon.svg`, `favicon.ico` (48/32/16), `apple-touch-icon.png` (180),
      `icon-192.png`, `icon-512.png` и логотип организации 512 px. Запускается
      руками при смене логотипа, результат коммитится как статика.
      Из портированного скрипта выброшена гамма-коррекция непрозрачности: она
      написана под полупрозрачные лепестки лотоса, у плюмерии все пять
      непрозрачны. Разбор пути наоборот обобщён — у лепестка лотоса два
      кубических сегмента, у плюмерии три, и жёсткое «ровно два» здесь падало.
      Печать `assets/img/logo-neva-beauty.svg` — копия варианта «-круг»,
      из неё через `qlmanage` рендерится PNG для разметки.
      Разметка: в `base.html.j2` вместо одной строки `.ico`, `.svg`,
      `apple-touch-icon`, `manifest` и `theme-color`; `build.py` начал писать
      `site.webmanifest`; в `site.yml` `business.logo` переехал с `/favicon.svg`
      на новый логотип. Знак в шапке, подвале и мобильном меню — через общий
      макрос `brand_mark`, он и был единственным источником для всех трёх мест.
      Страж: `check_brand_assets` в `check_content.py` проверяет, что файлы из
      `<link rel="icon">`, `apple-touch-icon` и `manifest` лежат на диске.
      `check_images` смотрел только `<img>`, поэтому битая иконка прошла бы молча —
      именно так заглушка и дожила до конца августа. Проверен на отказе.
      Проверено: сборка, 24 страницы, 101 тест, 193 позиции прайса; вёрстка на
      390, 768 и 1440 — горизонтальной прокрутки нет нигде, знак 46×45 px в шапке
      и в подвале на всех трёх ширинах.
      Резайз окна браузера вьюпорт рендеринга не меняет (`innerWidth` остаётся
      2832), поэтому ширины мерились через iframe фиксированной ширины — на будущее.
      `og-cover.jpg` намеренно не тронут: он нарисован раньше и живёт своей жизнью.
      Перерисовать его под новый знак — отдельная задача, если понадобится.
      Все 24 страницы изменились и получили новый `lastmod` — здесь это честно,
      шапка поменялась на каждой.
      **Побочная находка, дорогая: `.gitignore` глушил весь каталог иконок.**
      Первый деплой упал с `FileNotFoundError: generator/sources/icons/plumeria.svg` —
      файл не попал в коммит, хотя `git add -A` отработал молча. Причина в правиле
      `Icon?` для служебного файла macOS: у git сравнение путей регистронезависимое
      (`core.ignorecase=true`), а `?` — это любой один символ, поэтому шаблон совпадал
      с сегментом пути `icons`. Двадцать старых иконок уцелели только потому, что уже
      лежали в индексе: на отслеживаемые файлы `.gitignore` не действует. Любая новая
      иконка выпадала бы из коммита так же тихо, и ломался бы не тест, а прод-сборка.
      Правило заменено на `Icon` с **двумя** байтами CR. Не с `Icon[\r]`: в gitignore
      обратный слэш экранирует букву, и такой класс совпадает с «Iconr». И не с одним
      CR: git срезает один хвостовой CR ради поддержки CRLF-файлов, поэтому в
      официальном шаблоне macOS и написано «Icon must end with two \r».
      Проверено на трёх случаях: файл `Icon`+CR игнорируется, `plumeria.svg` и
      `star.svg` (с `--no-index`) — нет.

- [x] 94. Перманентный макияж снят с сайта. Ответ заказчика 2026-08-26 на вопрос 9
      письма салону: «процедуру перманентного макияжа мы больше не оказываем,
      полностью удаляем с сайта».
      Услуга была шестым направлением таксономии, поэтому правка не сводится
      к удалению одной страницы — направлений на сайте стало пять.
      Что удалено:
        * услуга `permanentnyj-makiyazh` из `content.yml` целиком — 11 позиций
          прайса, интро, пять преимуществ, шесть вопросов FAQ;
        * раздел `makiyazh` из таксономии. Своей страницы у него не было —
          он состоял из одной услуги, и пункт меню вёл прямо на неё, — поэтому
          из `sitemap.xml` ушёл ровно один адрес, а не два;
        * `permanentnyj-makiyazh` из `prices.json`: 204 позиции стали 193
          у 16 услуг вместо 17;
        * адрес из `lastmod.json`, исходники `permanentnyj-makiyazh.jpg`
          и `cat-makiyazh.jpg`, 13 производных webp/jpg и две строки в CREDITS.
      Что переписано, а не удалено:
        * `llms_description`, `home.seo_desc`, текст «о салоне» и плитка
          «Все направления в одном месте» — из перечня направлений убран
          перманентный макияж;
        * ответ FAQ главной: «шесть направлений» стало «пять», ссылка на
          удалённую страницу заменена расшифровкой коррекции фигуры;
        * `related` у `/udalenie-tatuirovok-lazerom/`: вместо перманентного
          макияжа поставлена уходовая косметология. У всех услуг ровно три
          карточки «Смотрите также», поэтому замена, а не вычёркивание;
        * ответ FAQ там же «Можно ли удалить перманентный макияж?» — сам вопрос
          остаётся, салон по-прежнему удаляет татуаж лазером. Убрано второе
          предложение, которое звало на удалённую страницу за обновлением.
      Что осталось намеренно: два упоминания перманентного макияжа в тексте
      `/udalenie-tatuirovok-lazerom/`. Это не услуга салона, а то, с чем
      работает лазер, — формулировки проверены глазами.
      Фикстура `makiyazh` в `test_seo_output.py` переименована в `odna-usluga`:
      она проверяет правило «раздел из одной услуги своей страницы не получает».
      Правило в генераторе осталось, а в боевых данных такого раздела больше
      нет — фикстура с именем удалённой услуги сбивала бы с толку.
      Проверено: следов `permanentnyj-makiyazh` в сборке нет ни одного, меню
      отдаёт пять разделов, `sitemap.xml` — 22 адреса вместо 23.
      Вёрстка после потери пункта меню прогнана по методу задачи 90: 22 страницы
      на 22 ширинах (по обе стороны каждого брейкпоинта плюс крайние),
      484 комбинации, 0 переполнений. Отдельно проверены выпадающие панели
      на восьми ширинах от 1024 до 1920 — ни одна не выходит за окно, крайней
      группой вместо «Фигуры» никто не стал, кнопка записи по-прежнему
      включается с 1100 px. Глазами: на 1024 меню из пяти разделов влезает
      без кнопки, дропдаун «Фигуры» раскрывается вровень со своим пунктом.
      Ворота: 193 из 193 цен, 24 страницы, 85 тестов.
      **Побочный эффект: `https://th.neva.beauty/permanentnyj-makiyazh/` теперь
      отдаёт 404.** Адрес был в `sitemap.xml` с 3 августа. Редиректа не ставим:
      Pages статические, а услуги, на которую было бы честно перенаправить,
      больше нет. Риск невелик — Search Console и Вебмастер ещё не подключены
      (задачи 66 и 67), сайт две недели был снят с сети, так что переобход
      начнётся уже с новым составом.

- [x] 92. Полная сверка цен на пяти уровнях. Проверка по просьбе заказчика
      2026-08-26, после возврата сайта в сеть. Парити-тест в CI сверяет
      `prices.json` только с таблицей своей страницы — этого мало, цена живёт
      ещё в тексте, в карточках соседних услуг и в разметке для роботов.
      1. **Прод против `prices.json`** — скачаны все 25 страниц с боевого
         домена, сверены тройки «страница + название + цена». 204 из 204.
      2. **`prices.json` против выгрузки Tilda** — 204 из 204, **все суммы
         совпадают**. Различий два, и оба в написании имени, не в цене:
         «гель , пудра» → «гель, пудра» (задача 17) и «Нано насадка РФ лифтинг»
         → «РФ-лифтинг» (задача 55). Оба переименования сделаны осознанно.
      3. **Суммы в прозе** — 252 числа вне таблиц: описания, преимущества,
         ответы FAQ, карточки. Выдуманных нет ни одного, каждое есть в прайсе.
         76 указывают на прайс другой услуги — это карточки «от … ฿» и ответы
         про соседние услуги, так и задумано.
      4. **Карточки «от … ฿»** — 63 штуки, все равны минимуму прайса своей
         услуги. Первый прогон дал 15 «расхождений», но ошибка была в проверке,
         а не на сайте: генератор правильно не считает ценой услуги доплату
         («+500 ฿»), тариф за минуту («35 ฿/минута») и позиции с флагом `addon`
         (расходники, ампулы, препараты ботулотоксина за единицу). Пересчёт той
         же функцией `price_from`, что и на сайте, — 0 расхождений.
      5. **schema.org** — 213 чисел в `Offer` и `AggregateOffer`, 0 расхождений,
         диапазоны `lowPrice`/`highPrice` совпадают с прайсом.
      Найдено ровно одно противоречие — Davines, задача 91, уже известное.
      Метод сверки страниц между собой: у услуги со своей страницей берётся
      `title` и ищется строкой в чужих прайсах. Совпадений имени четыре, три
      (Tokio Inkarami) сходятся, четвёртое — Davines. После правки задачи 91
      противоречий 0.
      Дыра в воротах остаётся: три проверки из пяти (проза, минимумы «от …»,
      противоречия между страницами) гонялись руками. Через неё и проехал
      Davines. Дописать их в `check_prices.py` — задача 93.

- [x] 93. Довести `check_prices.py` до пяти уровней сверки из задачи 92.
      Было: в CI только уровень 1 — `prices.json` против таблицы своей
      страницы. Три проверки из задачи 92 гонялись руками, через эту дыру
      и проехал Davines (задача 91).
      Сделано: в `check_prices.py` дописаны три уровня, все четыре гоняются
      одной командой и роняют сборку до деплоя.
      1. **Суммы в прозе.** Каждое число со знаком бата вне таблицы прайса
         обязано быть хоть в одном прайсе. Проверено 235 сумм на 24 страницах
         плюс 39 в `llms.txt` (выжимка для ИИ раньше не проверялась вообще)
         против 55 разрешённых значений прайса. Расхождений 0.
      2. **Карточки «от … ฿».** 61 карточка, каждая сверена с `price_from`
         той услуги, куда ведёт её ссылка, — тем же кодом, что рисует подпись.
         Расхождений 0.
      3. **Противоречия между страницами.** 16 услуг со своей страницей:
         имя услуги ищется строкой в чужих прайсах, найденная цена обязана
         совпасть с одной из цен своей страницы. Расхождений 0.
      Сверку с выгрузкой Tilda в ворота не потащили — по причине из задачи 92.
      Уровень 5 (schema.org) отдельной проверкой не нужен: разметка собирается
      из того же `price_catalog()`, что и таблица, разойтись ей не с чем.
      Решения по разбору чисел. Диапазон пишется без пробелов вокруг тире
      («600–1200 ฿»), поэтому тире с пробелами разделяет, а не связывает:
      «на Morpheus 8 — 15 000 ฿» даёт одно число, а не два. Набор разрешённых
      сумм общий на весь сайт, а не по странице: карточки и ответы FAQ законно
      называют цену соседней услуги (задача 92, 76 таких чисел).
      Проверено не только тем, что ворота молчат, — тем, что они срабатывают.
      Три мутации на копии собранного сайта: выдуманные «4321 ฿» в тексте,
      подменённая цена на карточке `/volosy/` и возврат Davines к 2500 ฿.
      Каждая поймана, сообщение указывает страницу и обе цифры.
      Регрессия закрыта тестами: `generator/tests/test_price_checks.py`,
      16 тестов на разбор сумм и на все три уровня, включая ровно тот случай,
      что пропустили в задаче 91. Весь набор — 101 тест, зелёный.
      Заодно из уровня 1 убран отдельный подсчёт позиций по услугам: сверка
      идёт мультимножеством строк, равенство количеств из неё уже следует.

- [x] 91. Davines стоит по-разному на двух страницах. На странице услуги
      `/davines-naturaltech-tailoring/` цена 2500 ฿ («Любая длина»), а в прайсе
      раздела на `/uhod-za-volosami/`, в блоке «Уходы для волос», та же услуга
      идёт за 1500 ฿ — числом в таблице прайса и словами в ответе FAQ «Какие
      салонные уходы для волос есть и сколько они стоят?». Ответ FAQ уходит
      в разметку `FAQPage`, так что противоречие видно не только человеку,
      открывшему обе страницы, но и роботам.
      Найдено 2026-08-25 при сборе вопросов к салону по задаче 77.
      Не наша ошибка переноса: расхождение пришло с Tilda и было на старом сайте
      салона. Проверено по выгрузке — `page82202466.html` (страница Davines)
      отдаёт «Любая длина 2500 ฿», `page74422157.html` (уход за волосами) —
      «Davines Naturaltech Tailoring 1500 ฿».
      Почему не поймали раньше: парити-тест задачи 17 сверял пары «название +
      цена» с выгрузкой и обе строки нашёл на месте — он ищет расхождения между
      старым сайтом и новым, а не противоречия внутри самого прайса. Названия
      строк разные («Любая длина» и «Davines Naturaltech Tailoring»), поэтому
      и сверка одинаковых названий по всем 204 позициям на них не срабатывает:
      прогнал её отдельно, других пар «одно название — разные цены» между
      страницами нет, все совпадения имён — это одинаковые зоны у разных услуг
      (шея, живот, декольте).
      Чинить только после ответа салона: какая цена верна или это две разные
      услуги. Вопрос 10 в письме `docs/questionnaire/2026-08-25-salon-followup.md`.
      Выдумывать нельзя — обе цены названы салоном.
      Ответ заказчика 2026-08-26: **везде 1500 ฿**. Это цена из прайса раздела
      на `/uhod-za-volosami/`, то есть верной признана она, а 2500 ฿ со страницы
      услуги — ошибка, приехавшая с Tilda.
      Правка одна: `price` позиции «Любая длина» в `prices.json`. Больше нигде
      число не лежало — проверено поиском `2500` по `content.yml`, там его нет.
      Разошлось оно само по четырём местам страницы услуги: таблица прайса,
      `Offer` и `AggregateOffer` в schema.org, текст «Услуга стоит … ฿»
      в блоке преимуществ, ответ FAQ «сколько стоит» и `description` страницы —
      все они собираются из цены, а не написаны руками. Плюс карточки
      «Davines … от 2500 ฿» на `/volosy/` и `/tokio-inkarami/` стали «от 1500 ฿»
      и строка в `llms.txt`. Страница `/uhod-za-volosami/` не изменилась вовсе:
      там уже стояло 1500 ฿ и в таблице, и в ответе FAQ.
      `lastmod` в `sitemap.xml` обновился ровно у трёх адресов, которые правда
      изменились, — ложного сигнала на 25 страниц нет.
      Проверено после правки полной сверкой цен (см. запись ниже): противоречий
      между страницами 0, карточек «от …» 63 из 63, чисел в schema.org 213
      из 213. Ворота: 204 из 204 цен, 25 страниц, 85 тестов.

- [x] 90. Горизонтальная прокрутка на 1024 px — на всех 25 страницах.
      Страница шире окна на 52 px, вылезает `.site-header__cta` (кнопка
      «Записаться» в шапке). Ломается только диапазон 1024–1076 px, с 1100
      проходит. 1024 — это ровно та точка, где по `@media(min-width:1024px)`
      впервые включаются меню и кнопка записи, и втроём с логотипом они
      не влезают. Ширина не редкая: iPad в горизонтали — ровно 1024.
      Дефект не новый: проверено пересборкой на HEAD до правок задачи 78.
      Аудит вёрстки в задаче 16 и все последующие проверки гоняли 390, 768
      и 1440 — ровно на границу брейкпоинта никто не смотрел.
      Найдено в задаче 78.
      Лечить не разовой заплаткой, а заодно закрыть дыру в методе проверки:
      после правки прогнать все 25 страниц по ширинам вокруг каждого
      брейкпоинта, а не только в серединах диапазонов.
      Сделано, но дефект оказался двойной — второй вылез только после первой
      правки, поэтому в CSS две правки, а не одна.
      1. Кнопка записи в шапке переведена с `@media(min-width:1024px)`
         на `1100px`. Прятать кнопку, а не меню: перелинковка важнее дубля
         кнопки, которой ниже 1024 px и так нет, а на странице их несколько.
         Без кнопки шапке нужно 941 px вместо 1077 — влезает с запасом 83 px.
      2. После этого страница вылезала уже на 43 px, и виновник сменился:
         `justify-content:space-between` при двух детях вместо трёх прижал меню
         к правому краю, и панель дропдауна последней группы («Фигура», 248 px
         от левого края своей группы) ушла за окно. `visibility:hidden`
         от `scrollWidth` не спасает — скрытая панель всё равно считается.
         Лечится не сдвигом меню, а привязкой крайней панели к правому краю:
         `.nav__group:last-of-type .nav__menu{left:auto;right:0}`. Так панель
         не вылезет и если разделов станет больше — а сдвиг меню эту мину
         оставил бы лежать. `:last-of-type`, а не `:last-child`: последний
         ребёнок `.nav` — ссылка «Макияж», у неё дропдауна нет.
      Дыра в методе проверки закрыта: прогнаны все 25 страниц на 22 ширинах —
      320, 359/360, 390, 559/560/561, 767/768/769, 899/900/901, 1023/1024/1025,
      1099/1100/1101, 1280, 1440, 1920, то есть по обе стороны каждого из шести
      брейкпоинтов CSS плюс крайние. 550 комбинаций, 0 переполнений; до правки
      на том же прогоне их было 75 (три ширины × 25 страниц).
      Проверено и глазами на 1024 и 1440: на 1024 меню целиком влезает без
      кнопки, на 1440 кнопка на месте, дропдаун «Фигуры» в обоих случаях
      раскрывается влево и стоит вровень со своим пунктом.
      `sitemap.xml` не тронут: правка чисто в CSS, `page_digest` считает разметку
      страницы, поэтому ложного `lastmod` на 25 адресов не случилось — в отличие
      от задачи 80, где переписывался шаблон.
      Ворота: 204 из 204 цен, 25 страниц, 85 тестов.

- [x] 89. Вернуть сайт в сеть. Заказчик попросил 2026-08-13 временно убрать сайт
      из сети. Сделано в тот же день: сайт Pages удалён
      (`gh api -X DELETE repos/jw-git-hub/th.neva.beauty/pages`, ответ `204`),
      деплой-воркфлоу отключён (`gh workflow disable deploy.yml`), чтобы пуш
      в `main` не публиковал сайт обратно. Репозиторий, содержимое и DNS
      не тронуты: `th.neva.beauty` по-прежнему резолвится в `jw-git-hub.github.io`
      и четыре IP Pages, файл `th.neva.beauty/CNAME` на месте.
      Проверено после удаления: `https://th.neva.beauty/`,
      `http://th.neva.beauty/`, `https://th.neva.beauty/kosmetologiya/`
      и `https://jw-git-hub.github.io/th.neva.beauty/` отдают `404`.
      Отдача гасла не сразу — около трёх минут CDN и origin ещё раздавали
      прежнюю сборку с `200`.
      Конфигурация на момент снятия, её и восстанавливать:
      `build_type: workflow`, `source: main /`, `cname: th.neva.beauty`,
      `public: true`, `https_enforced: true`, сертификат `approved` на один
      домен `th.neva.beauty` с `expires_at: 2026-11-01`.
      Как возвращать: включить воркфлоу (`gh workflow enable deploy.yml`),
      создать сайт заново (`gh api -X POST repos/jw-git-hub/th.neva.beauty/pages
      -f build_type=workflow`), вписать домен
      (`gh api -X PUT repos/jw-git-hub/th.neva.beauty/pages -f cname=th.neva.beauty`),
      запустить сборку (`gh workflow run deploy.yml`), дождаться выпуска
      сертификата и только после этого включить `https_enforced`.
      Важно: сертификат выпускается заново, и до его выдачи `https` будет
      отдавать ошибку — от нескольких минут до нескольких часов. На это время
      `Enforce HTTPS` держать выключенным, иначе сайт недоступен совсем.
      Побочный эффект для задачи 81: прежней записи сертификата больше нет,
      а DNS для `www` уже существует — значит новый сертификат имеет шанс
      покрыть оба домена сразу. Проверить `https_certificate.domains` после
      возврата: если там появился `www.th.neva.beauty`, задача 81 закрыта сама.
      Сделано 2026-08-26 по команде заказчика. Порядок вышел короче
      записанного: перед возвратом прогнаны локальные ворота (204 из 204 цен,
      25 страниц, 85 тестов), сборка совпала с закоммиченной байт в байт.
      Дальше — пуш накопившегося коммита, `gh workflow enable deploy.yml`,
      `gh api -X POST .../pages -f build_type=workflow`,
      `gh api -X PUT .../pages -f cname=th.neva.beauty`.
      Пуш сам поднял деплой (14 с сборка + 8 с деплой, оба зелёные), поэтому
      `gh workflow run` не понадобился.
      Ожидания сертификата тоже не понадобилось, и это отличие от плана:
      GitHub не выпускал новый, а вернул прежний — `state: approved`, тот же
      `expires_at: 2026-11-01`, и `https_enforced` встал в `true` сам при
      создании сайта. Промежуточная попытка выставить `https_enforced=false`
      одним запросом с `cname` отвалилась с `404 The certificate does not
      exist yet`: пока домен не вписан, флаг трогать нельзя. Вписывать `cname`
      отдельным запросом — правильный порядок, окна без https не было.
      Проверено на боевом домене: все 23 адреса из `sitemap.xml` отдают 200,
      несуществующий адрес — 404, `http://` → 301 → `https://` на всех четырёх
      edge-нодах (`185.199.108–111.153`), сертификат Let's Encrypt
      `CN=th.neva.beauty` до 01.11.2026, `bundle.min.css` (30 715 Б) и три
      JS-бандла грузятся, `canonical` и `og:url` смотрят на боевой домен,
      следов `github.io` в разметке нет, `/privacy/` и `/404.html` под
      `noindex`, `robots.txt` открыт всем и указывает на `sitemap.xml`.
      Задача 81 сама не закрылась: в `https_certificate.domains` по-прежнему
      один `th.neva.beauty`, `https://www.th.neva.beauty` не отвечает.

- [x] 88. Финальная чистка рабочих файлов. Опросник свою работу отработал
      и в репозитории больше не нужен: удалить `docs/questionnaire/` целиком
      (текст опросника, письмо второго круга от 2026-08-25, `create-form.gs`
      и правило для CSV в `.gitignore`), саму Google-форму и таблицу ответов
      заказчик убирает у себя. Заодно пройтись по `docs/audits/` и решить, что
      из промежуточных отчётов остаётся как история проекта. Ответы салона
      к этому моменту должны быть перенесены в тексты, FAQ и разметку —
      проверить, что ни один факт не остался только в CSV.
      **Почему сделано сейчас, а не «последней».** Оговорка «когда закрыты
      остальные пункты» была про содержательные пункты, ради которых опросник
      и заводился, — 77–80 и 91. Все закрыты. Открытыми остаются 64, 65, 69,
      70 и 71: действия в кабинетах и решения заказчика, к опроснику отношения
      не имеющие. 73, 75 и 76 закрыты отказом 2026-08-31, их вопросы в опросник
      и не входили. Ждать больше нечего.
      **Сверка «ни один факт не остался только в CSV».** Прошёл по всем 65
      колонкам обеих анкет от 2026-08-11 — каждый непустой ответ либо стоит
      на сайте, либо отклонён сознательно и с записанной причиной:
        * длительность — `duration` у 13 услуг из 16 (задача 77); без неё
          Davines и стрижка («индивидуально» на страницу не ставим), эндосфера
          и массаж (время стоит в самих строках прайса);
        * «что входит» — `included` у 7 услуг (задачи 77 и 78), включая
          «оплаченные сеансы курса не сгорают» у эндосферы и массажа;
        * солнце и море — `sun` у 8 услуг, интервалы курсов — `course`
          у 7 (задачи 79 и 80), в том числе «8–10 сеансов» у лазерной
          эпиляции и «до 15» у удаления тату;
        * правила записи — предоплата 20 % только на курсы и дорогие
          процедуры, отмена за сутки, опоздание и неявка — два ответа FAQ
          на главной (задача 78);
        * ответы по перманентному макияжу (анестезия, коррекция, солнце)
          отпали вместе со страницей — услуга снята задачей 94.
      **Единственная строка, не дошедшая до сайта, — «сертификаты
      на оборудование»** из свободного вопроса «что ещё стоит знать».
      Оставлена за бортом сознательно: документов салон не присылал, а писать
      про сертификацию с чужих слов — то же обещание за салон, из-за которого
      закрыта отказом задача 76. «Акции и скидки» из той же строки на сайте
      есть — акция SMAS стоит и в прайсе, и в ответе FAQ.
      **CSV не удалён, а унесён из репозитория** — в `Данные и промты/`
      (`2026-08-11-ответы-опросника-салона.csv`). Причина: git этот файл
      никогда не видел, восстановить его было бы неоткуда, а после снятия
      правила из `.gitignore` он из невидимого стал бы обычным неотслеживаемым
      файлом — то есть первым же `git add .` уехал бы в публичную историю,
      ровно вопреки решению, ради которого правило и заводили. В `Данные
      и промты/` он лежит рядом с остальными данными салона, и каталог
      закрыт `.gitignore` целиком.
      **Правило из `.gitignore` снималось побайтно** (`open(...,'rb')`), и это
      не педантизм: файл заканчивается паттерном `Icon` с настоящим байтом CR,
      а обычное текстовое чтение Python приводит переводы строк к `\n` и этот
      байт съедает — правило про иконки macOS молча перестаёт работать, ровно
      та поломка, из-за которой заводилась задача 95. Первый заход это и сделал,
      поймано сверкой `xxd` до коммита. Предупреждение в самом файле («не
      редактировать вручную») теперь проверено на практике.
      **`docs/audits/` остаётся целиком** — решение задачи 59 подтверждено
      повторным проходом. Все три отчёта (этапы 5, 7 и 8) — не черновики:
      в каждом записаны метод, охват и находки, на них ссылаются задачи 15–21,
      45–49 и 50–54, и восстановить это из кода нельзя. Числа страниц в них
      (23–25) не правил: отчёты датированы, а 24 страницы стало после снятия
      перманентного макияжа задачей 94 — переписывать историю под сегодняшнее
      состояние значит её испортить.
      Ссылки `docs/questionnaire/…` в задачах 91, 77 и в шапке раздела «Вне
      кода» оставлены как есть, в шапке добавлена оговорка: это запись того,
      как пункт делался, а не указатель на живой файл.
      Ворота после чистки: 24 страницы, 193 позиции, 101 тест — зелёные.
      Сайт не изменился ни одной страницей: удалялись только рабочие файлы,
      `lastmod` никому не сдвинулся.

- [x] 87. Переписать политику конфиденциальности под факты салона.
      Сейчас `generator/templates/privacy.html.j2` — текст, перенесённый с Tilda:
      ссылается на российский 152-ФЗ и Роскомнадзор, к салону на Самуи они
      отношения не имеют.
      Факт от заказчика (2026-08-10): салон персональные данные не хранит —
      ни CRM, ни журнала записи, вся переписка остаётся в WhatsApp, Telegram
      и Instagram, то есть у самих мессенджеров.
      Значит, политика должна описывать это, а не выдуманный «Оператор с мерами
      по обеспечению безопасности»: какие данные видит салон, где они физически
      лежат, что салон их никуда не передаёт и не собирает формами на сайте.
      Проверить заодно, что на сайте нет форм сбора данных и что счётчик
      Метрики упомянут честно.
      Заказчик просил показать результат на вычитку до коммита в прод.
      Сделано: страница переписана целиком. Ушли 152-ФЗ, Роскомнадзор,
      «Оператор» и «меры по обеспечению безопасности»; вместо них — реальная
      схема «переписка живёт у мессенджера и в телефоне администратора»,
      таблица из четырёх каналов (WhatsApp, Telegram, Instagram Direct, сайт)
      с ответом, что видно и где лежит, и раздел «Чего на сайте нет».
      Право — Таиланд, PDPA B.E. 2562, одной строкой: полного соответствия
      PDPA страница не заявляет намеренно, салон данных не собирает, а
      имитация юридической машинерии — ровно то, от чего уходили.
      Проверки перед коммитом: форм на сайте нет ни одной (`grep` по всем
      HTML не нашёл ни `<form>`, ни `<input>`, ни `<textarea>`); в
      `partials/metrika.html.j2` стоят `webvisor:true` и `clickmap:true`,
      поэтому Вебвизор в тексте назван честно; вёрстка легла на уже
      существующий `.legal-table` в `sources/css/legal.css`, новый CSS
      не понадобился.
      Заодно поправлен `seo_desc` в `build.py`: старое описание обещало
      рассказ про отзыв согласия, которого в новом тексте нет.
      Контакты в разделе «Вопросы и просьбы» тянутся из `site.yml`, так что
      после задач 64 и 69 (Instagram и тайский номер) страница обновится
      сама, править её отдельно не придётся.

- [x] 84. Обложки разделов повторяют кадры услуг — найдено сплошной сверкой
      всех кадров сайта с выгрузкой Tilda (пиксельно, а не по именам файлов).
      Что сверка показала по героям: 17 страниц услуг из 18 стоят ровно на
      обложках своих страниц Tilda, отклонение около 1 из 255 — шум сжатия.
      Единственное исключение — `/permanentnyj-makiyazh/`: на Tilda обложкой
      той страницы был сток с кистями MAC (декоративный макияж, чужой логотип
      в кадре, к перманенту отношения не имеет), а сайт взял кадр, который
      сама же Tilda ставила этой странице в `og:image` и в плитку на главной, —
      прорисовку брови мастером. Замена осмысленная, оставлено как есть.
      Отдельно проверен `igolchatyj-rf-lifting`: отклонение 36 из 255 не
      ошибка, а намеренно сдвинутый вправо кроп (по центру срезало лоб
      с манипулой), это описано в `CREDITS.txt`.
      Открытая часть: 5 обложек разделов из 6 — это тот же кадр, что у одной
      из услуг раздела.
        * `cat-makiyazh` = герой `permanentnyj-makiyazh`. На Tilda это два
          разных файла (`tild6438-…` и `tild3665-…`), одно и то же фото,
          загруженное дважды, — потому и не бросалось в глаза. Раздела
          «Макияж» отдельной страницей нет, пункт меню ведёт сразу на услугу,
          так что клиент видит один кадр два экрана подряд: карточка
          на главной → герой страницы.
        * `cat-kosmetologiya` = `botulinoterapiya`,
          `cat-apparatnaya-kosmetologiya` = `fotoomolozhenie-m22`,
          `cat-korrekciya-figury` = `endosfera-terapiya`,
          `cat-volosy` = `keratinovoe-vypryamlenie-volos` — здесь кадр
          повторяется три экрана подряд: карточка на главной → карточка
          услуги в разделе → герой услуги.
        * уникальна только `cat-epilyaciya`.
      Починить из выгрузки нельзя: в архиве Tilda 25 пригодных кадров, 20 уже
      стоят на сайте, свободны три — телефон с иконками соцсетей со страницы
      «Контакты», кисти MAC и флат-лей баночек с алоэ со страницы магазина.
      Первый бесполезен, второй хуже нынешнего, третий годится разве что
      под «Косметологию» и всё равно слабее живого кадра процедуры.
      Ещё два кадра поначалу выглядели свободными, но сверка попиксельно дала
      отклонение 0,0 из 255 к уже стоящим: это обложка главной и обложка
      раздела «Эпиляция», просто залитые в Tilda по второму разу.
      Решение заказчика: оставляем как есть. Он прошёл по всем пяти цепочкам
      «главная → раздел → услуга» и подтвердил, что каждый кадр отвечает
      своему разделу по смыслу, а повтор его не смущает. Замена стоком
      отклонена по той же причине. Вопрос закрыт, к задачам 74 и 75 не
      привязан — если новые фото от салона появятся, обложки можно будет
      обновить отдельно, но как обязательство это не висит.

- [x] 82. Плашка призыва на главной выглядела съехавшей.
      Причина не в отступах: ширина `.cta-inline` была жёстко задана 760 px —
      это ширина прайса на странице услуги, где плашка ложится под него край
      в край и потому смотрится частью блока. На главной сверху сетка во всю
      ширину контейнера (1152 px на широком экране), и та же плашка висела
      с отступом почти 200 px с каждой стороны, не совпадая ни с одной
      колонкой. Хуже того, в последнем ряду сетки всего две карточки из
      четырёх, так что справа от них пустота — плашка читалась как сдвинутая
      влево-вправо наугад.
      Стало: у макроса `cta_inline` появился аргумент `wide` — он снимает
      потолок ширины, и плашка совпадает с блоком сверху по краям на всех
      ширинах экрана (проверено на 390, 760, 900, 1024, 1200 и 1440).
      С 1024 px текст и кнопка расходятся по краям: в полосе за тысячу
      пикселей центрированная пара снова висела бы в пустоте.
      Страницы услуг и разделов не тронуты — там плашка как была, 760 px
      по центру. Вертикальные отступы не менялись: 40 px сверху и padding
      секции снизу были и остаются одинаковыми на всех страницах сайта.
      Остаётся мелочь того же рода: на странице раздела таблица сравнения
      шириной 960 px, а плашка под ней 760 — расхождение вчетверо меньше
      и в глаза не бросается, поэтому оставлено как есть.

- [x] 83. Фото на первом экране главной — не то, что было на Tilda.
      Было: портрет 4:5 «маска на лице», и это тот же самый исходник, что
      у героя страницы «Уходовая косметология» и у превью для соцсетей
      `og-cover`, — один кадр в трёх местах.
      Стало: обложка главной со старого сайта на Tilda — макияж и маникюр
      крупным планом (`tild3161-3361-4237-a363-386661326233__photo.jpg`,
      1680×1157). Тот же кадр стоит героем главной на `vn.neva.beauty`:
      сверено попиксельно с `vn.neva.beauty/assets/img/hero.webp` — средняя
      разница 2,5 из 255, то есть кроп совпал, расхождение только от WebP.
      Кадр общий для двух салонов сети, так что главные страниц теперь
      выглядят одинаково.
      Кроп героя центральный, 4:5. У `og-cover` 1200×630 верх кадра сохранён
      целиком: центральный кроп срезал причёску. `hero.webp` потяжелел
      с 29,5 до 37 КБ — кожа и волосы кодируются хуже, чем ровная маска.
      Обновлены `hero_image_alt` в `content.yml` и `CREDITS.txt`.
      Страница «Уходовая косметология» осталась со своим прежним кадром —
      дублирования больше нет.

- [x] 85. Фото на мобильном шло после текста, а не перед ним — не как на vn.
      Разметка героя совпадает с `vn.neva.beauty` один в один, расходился CSS:
      в задаче 49 (пункт 1) у `.hero__media` и `.svc-hero__media` был снят
      `order:-1`, чтобы поднять кнопку записи в первый экран. На вертикальном
      экране это давало стену текста на весь экран — кадр процедуры клиент
      видел только после лида и кнопки.
      Стало как на vn: до 768 px `order:-1`, с 768 px `order:0` — на двух
      колонках порядок берётся из разметки (текст слева, фото справа).
      Цена решения, замерено на 390×844: «Записаться» уходит с 0.70 на
      1.12 экрана на главной и с 0.90 на 1.30 на странице услуги. Терпимо
      потому, что пункт 2 той же задачи 49 остался в силе: круглая кнопка
      записи 44×44 висит в липкой шапке на всех экранах — запись в один тап
      независимо от прокрутки.
      Правки: `home.css:13–15`, `service.css:14–18`.
      Уточнено задачей 86: круглой кнопки в шапке больше нет — на мобильном
      первая точка записи это кнопка героя на 1.12–1.30 экрана, как на vn.

- [x] 86. Кнопка записи в шапке на мобильном — ещё одно расхождение с vn.
      Было: `.site-header__cta` сжата в круг 44×44 с иконкой мессенджера и
      видна на всех экранах (задача 49, пункт 2). На `vn.neva.beauty` кнопка
      скрыта до 1024 px и появляется на десктопе текстом.
      Стало как на vn: `display:none` до 1024 px, `inline-flex` с 1024 px.
      Из шаблона убраны иконка и `<span class="site-header__cta-text">` —
      на десктопе кнопка всегда текстовая, показывать было нечего. Разметка
      кнопки теперь совпадает с vn символ в символ. `aria-label` снят: подпись
      «Записаться» и так читается вслух, дублирующая метка только мешала.
      На мобильном запись остаётся из первого экрана (кнопка героя) и из
      дровера — проверено, ссылка «Записаться» в меню на месте.

- [x] 63. Бэкпорт прайс-таблицы в `vn.neva.beauty` — там цены тоже свёрстаны дивами
      вместо `<table>`, из-за чего поиск и ИИ читают их хуже. Правки те же, что
      в задаче 5 плана сборки: шаблон, CSS, селекторы парити-теста.
      Делать нечего: бэкпорт уже сделан 31.07.2026 в отдельной сессии по vn,
      коммит `b4298b7` «SEO: прайсы на семантических `<table>` + рабочий
      ARIA-паттерн вкладок». Пункт написан 30.07 и с тех пор просто не был
      отмечен.
      Проверено по всем трём правкам, а не по одному коммиту:
        * шаблон — `service.html.j2` строит `<table class="pricelist__table">`
          с `<caption>`, скрытой шапкой и `<th scope="row">` на названии
          позиции; следов `<div class="pricelist__row">` в репозитории ноль;
        * CSS — `.pricelist__table` на месте, `.visually-hidden` в `base.css`;
        * парити-тест — селекторы читают строки таблицы, `check_prices.py`
          зелёный: 112 позиций в HTML против 112 `Offer` в JSON-LD, 10
          агрегатов, валюта VND. `check_headings.py` тоже зелёный, 16 страниц.
      Прод сверен сплошь: все 14 адресов из `sitemap.xml` скачаны, на всех
      10 страницах услуг прайс размечен таблицей, дивов-строк не осталось
      ни одного; на разделах и главной прайса нет и не должно быть.
      Заодно снят вопрос по комбо: они остались карточками `<li class="combo">`,
      но в машиночитаемые данные попадают — в `OfferCatalog` на
      `/lazernaya-epilyaciya/` 41 `Offer` на 35 строк таблицы и 6 комбо.
      То есть связь «позиция → цена» роботу видна и там, отдельного пункта
      это не требует.

- [x] 72. Конкретные `Offer` вместо одного `AggregateOffer` на страницах услуг.
      Было: робот видел только «столько-то позиций от X до Y ฿», а какая услуга
      сколько стоит — достраивал разбором таблицы, то есть эвристикой.
      Стало: `hasOfferCatalog` → `OfferCatalog` (раздел прайса) → `Offer` с
      `itemOffered`. Цена каждой позиции названа явно, диапазон в `AggregateOffer`
      теперь считается из того же каталога и разойтись с ним не может.
      Фиксированная цена идёт в `price`, «600 -1200 ฿» и «от 2500 ฿» — в
      `priceSpecification` с `minPrice`/`maxPrice`: выдавать нижнюю границу за
      точную цену нельзя.
      Позиции, которые отдельно не продаются, в разметку не попадают. Часть видна
      по строке цены («+500 ฿», «35 ฿/минута»), часть — нет, поэтому в
      `prices.json` появился флаг `"addon": true`. Помечено 9 позиций: одноразовая
      игла, доплата за густоту, уходовые ампулы и маски, все 5 препаратов
      ботулинотерапии (там цена за единицу препарата, а не за процедуру).
      На отображение прайса флаг не влияет — все 204 позиции на странице как были.
      Следствия для цен: электроэпиляция «от 100 ฿» → «от 300 ฿», кератиновое
      «от 500 ฿» → «от 2000 ฿», уходовая «от 500 ฿» → «от 1000 ฿», страница
      ботулинотерапии осталась без ценовой разметки осознанно.
      **При добавлении позиций в прайс: надбавку, расходник или цену за единицу
      помечать `"addon": true`, иначе страница заявит цену, по которой купить
      нельзя.** Тесты: `generator/tests/test_offer_catalog.py` и
      `test_prices_schema_parity.py` (сверка разметки с прайсом по всем услугам).
      Ожидания: цены в сниппете от этого не появятся — богатые результаты с ценой
      Google рисует для `Product`, а не для `Service`. Выигрыш в понимании бизнеса
      машинами и в ответах ИИ, а не в CTR.

---

## Вне кода — на стороне заказчика

Эти пункты нельзя закрыть правками сайта, но они сильно влияют на результат.

Содержательная часть — пункты 77–80 и правила записи из 78 — была собрана
в опросник для салона, рядом лежал `create-form.gs`, собиравший из него
Google-форму за один запуск в Apps Script. Опросник свою работу отработал
и удалён задачей 88; ответы салона разобраны по пунктам 77, 78, 79 и 80.
Пункты 64–71 и 81 — действия в кабинетах, в опросник не входили.
Пункты 73, 74, 75 и 76 закрыты отказом заказчика: вопросы по ним сначала убрали
из опросника, а 2026-08-31 сняты и сами пункты.

**Уточнение 2026-08-26: заголовок раздела больше не точен.** Доступы ко всем
кабинетам — Search Console, Вебмастер, Метрика, reg.ru, GitHub, Instagram —
у исполнителя. Значит 66, 67, 68, 70, 71 и 81 делаются нами, а не передаются
заказчику. За заказчиком оставалось то, что требует его решения или его
личности: 65 (Google Business Profile заводится на владельца бизнеса),
64 (на какое имя переименовывать Instagram) и 69 (тайская SIM).

**Все решения получены 2026-08-31.** 64 — имя `th.neva.beauty`, вписано
в сайт. 65 — Google Business Profile для Таиланда не используем, отказ.
69 — тайского номера не будет, WhatsApp остаётся на российском, отказ;
Line тоже не используем. Вместе с 71 (домен не продлеваем) и отказами
по 73, 75 и 76 из раздела открытым остаётся один пункт — 70.

Второй круг — письмо от 2026-08-25 на десять вопросов: остаток задачи 77
и цена Davines из задачи 91. Все десять закрыты ответами заказчика 2026-08-25
и 2026-08-26, письмо удалено задачей 88 вместе с опросником. Запасной блок
с формулировками по 73, 75 и 76 лежал там же и отправлен не был — 2026-08-31
эти пункты закрыты отказом.

Ссылки вида `docs/questionnaire/…` ниже по файлу — история: так было, когда
пункт делался. Самих файлов в репозитории больше нет.

- [x] 64. Переименовать аккаунт Instagram: было `avocado.beauty.samui` — старый
      бренд. После переименования старые ссылки автоматически ведут на новое имя.
      **Имя названо заказчиком 2026-08-31: `th.neva.beauty`** — то же, что домен,
      по образцу соседнего `vn.neva.beauty`. Сам аккаунт переименовывает заказчик,
      это действие в его кабинете.
      Сделано в репозитории: `instagram_url` и `business.same_as` в `site.yml`
      переведены на `https://www.instagram.com/th.neva.beauty/`. Ссылка стоит
      в трёх местах каждой страницы — иконка в подвале, ответ FAQ «как записаться»
      и `sameAs` сразу двух узлов JSON-LD (`Organization` и `BeautySalon`), —
      поэтому изменились все 24 страницы и всем сдвинулся `lastmod`.
      Заодно снято исключение в воротах: `check_content.py` держал
      `ALLOWED_BRAND_TRACE = "avocado.beauty.samui"`, чтобы строка «avocado»
      не роняла сборку. Исключение больше не нужно — запрет на след старого
      бренда снова сплошной, без дырки, и проверка стала на строку короче.
      **Условие для пуша:** ссылка уходит в прод вместе с коммитом. Если аккаунт
      к тому моменту ещё не переименован, 24 страницы будут вести на несуществующий
      профиль. Порядок правильный такой: сначала переименование, потом пуш.
- [x] 65. ~~Завести Google Business Profile~~ — закрыта отказом заказчика
      2026-08-31: «для Таиланда не используем».
      Предлагалось: профиль в режиме зоны обслуживания, адрес скрыт. Для локального
      поиска это рычаг сильнее любых правок в тексте.
      Что теряем, называю честно, раз решение принято: карточки в поиске и на
      Картах не будет, в выдачу «салон красоты рядом со мной» и в локальный блок
      сайт не попадёт, часы и контакты Google возьмёт только из разметки страниц.
      Остаётся органика по запросам — то, ради чего сделаны этапы 6 и 8.
      Сцепка с другими пунктами: задача 73 (отзывы) первым шагом называла именно
      этот профиль — она закрыта отказом в тот же день, так что повисшего хвоста
      нет. В `business.same_as` ссылки на профиль не будет, там остаётся один
      Instagram. **Тему заново не поднимать.**
- [x] 66. Подтвердить домен в Google Search Console, отправить sitemap
      Начато 2026-08-26. В аккаунте три ресурса: `vn.neva.beauty` (доменный),
      `https://doctor-cosmetolog.pro/` и `https://psy-krasnogor.pro/`.
      Ресурса для `th` не было. Планировали **доменный** ресурс `th.neva.beauty`,
      по образцу соседнего `vn`: отдельный ресурс, а не общий на `neva.beauty`,
      чтобы статистика двух сайтов не смешивалась, и доменный, а не с префиксом
      URL, чтобы покрыть `www` и `http` — заодно было бы видно состояние
      сертификата из задачи 81.
      ~~Доменный~~ — план отменён 2026-08-30, причина в задаче 67: на имени `th`
      стоит CNAME, TXT рядом с ним не живёт, а доменный ресурс подтверждается
      только через TXT. Заводим ресурс с префиксом URL.
      Токен подтверждения (TXT на `th`):
      `google-site-verification=OOD-Swoa1YW_JqvbsBwZuT87PpfK6dyFmofOnIH-bWU`
      Ресурс в кабинете **не сохранён** — диалог закрыт переходом на reg.ru.
      Потери нет: Google выдаёт тот же токен для того же домена и аккаунта,
      ресурс заводится заново, когда запись разойдётся.
      Сделано 2026-08-30: ресурс `https://th.neva.beauty/` с префиксом URL заведён
      и на этот раз сохранён. Токен способа «HTML-тег»:
      `n2Gup2kYzjw0pk2ZhTbgltdatmneJes7GSwy8JrXDCo` — лежит в `site.yml`,
      мета-тег на главной боевого домена.
      Способ «HTML-файл» Google предлагает первым, но он нам не подходит: файл
      пришлось бы класть в репозиторий руками мимо генератора, и его снесла бы
      первая же пересборка. Мета-тег живёт в шаблоне и защищён `check_verification`.
      **Закрыто 2026-08-30.** Право собственности подтверждено, способ в кабинете
      значится как «Тег HTML». `sitemap.xml` отправлен и принят сразу: статус
      «Успешно», обнаружено 22 страницы — ровно столько `<url>` и в файле.
- [x] 67. Подтвердить домен в Яндекс.Вебмастере, отправить sitemap
      Начато 2026-08-26. **Сайт `https://th.neva.beauty` в кабинет добавлен**,
      открыта страница подтверждения прав, способ — DNS-запись.
      Значение TXT на `th`: `yandex-verification: 073be936857d814d`
      В кабинете уже есть `https://doctor-cosmetolog.pro` (21 страница
      добавлена, 20 в поиске), `https://vn.neva.beauty` и два `psy-krasnogor`.
      Цифры по старому сайту пригодятся в задаче 71: отключать его вслепую
      не стоит, сначала посмотреть, что подхватит новый.
      **Закрыто 2026-08-30.** Догадка про единый код подтвердилась: на вкладке
      «Метатег» кабинет показал то же `073be936857d814d`, что и в строке DNS,
      правки не понадобились. Права подтверждены, роль «Владелец», метод в
      списке пользователей — «Meta-тег», дата проверки 30.08.2026.
      `sitemap.xml` добавлен, стоит в очереди на обработку: Яндекс обещает
      до 1–2 недель, до тех пор в Диагностике возможно предупреждение об
      отсутствии обработанных файлов — это ожидаемо, реагировать не нужно.
      Упёрлось в вход: reg.ru просит логин и пароль, ассистент их не вводит.
      **Способ подтверждения сменён 2026-08-30: мета-тег вместо DNS.**
      План с TXT-записью был нерабочим, и это не про доступ к reg.ru. Имя `th`
      в зоне `neva.beauty` занято записью CNAME на `jw-git-hub.github.io`, а
      рядом с CNAME других записей быть не может (RFC 1034 §3.6.2). Проверено:
      `dig +short TXT th.neva.beauty @8.8.8.8` отдаёт сам CNAME, а не TXT. Даже
      если reg.ru даст сохранить запись, резолвер её не покажет и подтверждение
      не пройдёт. Строка «обе записи уживаются с существующими CNAME» в задаче 67
      была ошибкой.
      Следствие для Google: доменный ресурс подтверждается только через DNS,
      поэтому заводим ресурс **с префиксом URL** (`https://th.neva.beauty/`).
      Плата за это — `www` и `http` в один ресурс не попадут, состояние
      сертификата из задачи 81 через Search Console видно не будет.
      Сделано в генераторе: блок `verification` в `generator/data/site.yml`
      (ключи `google` и `yandex`), блок `extra_head` в `base.html.j2` и его
      переопределение в `home.html.j2` — теги выводятся только на главной,
      обе панели читают корень сайта. Пустое значение = тег не выводится.
      Страж от молчаливой пропажи: `check_verification` в `check_content.py`
      сверяет тег на главной с токеном из `site.yml` и ловит лишние вхождения
      на внутренних страницах. Ворота зелёные: 24 страницы, 101 тест,
      193 позиции прайса.
- [x] 68. Проверить, что Яндекс.Метрика 99850063 принимает новый домен
      Проверить с рабочей машины 2026-08-26 не вышло, и причина не в сайте:
      локальный резолвер `10.2.0.1` отдаёт NXDOMAIN на `metrika.yandex.ru`
      и `mc.yandex.ru`, через `8.8.8.8` те же имена резолвятся штатно
      (`mc.yandex.ru` → 87.250.251.119). То есть VPN или роутер с трекер-
      фильтром режет домены Метрики — `mc.yandex.ru` стоит в любом блок-листе.
      `webmaster.yandex.ru` при этом резолвится, поэтому Вебмастер открылся.
      На посетителей сайта это не влияет. Чтобы открыть кабинет — выключить
      фильтр или зайти с другой сети.
      Со стороны сайта всё на месте: стандартный тег с `clickmap`, `trackLinks`,
      `accurateTrackBounce` и `webvisor`, ID 99850063, на боевом домене
      отдаётся (проверено curl'ом, два вхождения в разметке главной).
      Что смотреть в кабинете, когда откроется: адрес сайта в настройках
      счётчика (он перенесён со старого сайта, там наверняка ещё
      `doctor-cosmetolog.pro`) и идут ли данные с `th.neva.beauty`.
      Отдельно проверить, не висит ли тот же счётчик на живой Tilda — тогда
      два сайта пишут в один счётчик и данные смешаны. Прямой запрос к
      `doctor-cosmetolog.pro` отдал 403 (Tilda режет curl), смотреть браузером.
      Проверено 2026-08-30, оба вопроса сняты.
      Фильтр DNS исчез: `mc.yandex.ru` и `metrika.yandex.ru` теперь резолвятся
      и через системный `10.2.0.1`, и через `8.8.8.8`, IP совпадают. Кабинет
      открывается с рабочей машины, переключать сеть не нужно.
      Смешения данных нет: старый сайт на Tilda больше не отдаётся (см. задачу 71),
      на `doctor-cosmetolog.pro` теперь парковка Рег.ру, и счётчика 99850063
      в её разметке нет — ноль вхождений. Метрика там есть, но чужая, `98466329`:
      счётчик самого Рег.ру в шаблоне парковки, рядом с рекламным блоком.
      Значит 99850063 сейчас собирает данные только с `th.neva.beauty`, где тег
      отдаётся на боевом домене.
      **Закрыто 2026-08-30.** Адрес сайта в настройках счётчика заменён на
      `th.neva.beauty` (было `doctor-cosmetolog.pro`), галка «Принимать данные
      только с указанных адресов» оставлена включённой намеренно: после
      перехвата старого домена она не даст чужому сайту писать в счётчик.
      Данные с нового домена принимаются — отчёт «Популярное» за 30.08.2026
      показывает 6 просмотров: 5 с `https://th.neva.beauty/` (3 посетителя),
      1 с `https://doctor-cosmetolog.pro/` (1 посетитель).
      Фильтр на вкладке «Фильтры» оказался безобидным: «Оставить только трафик →
      URL страницы → сайт и дополнительные адреса», без вписанного домена.
      Он тянется за полем адреса, поэтому после правки означает новый домен,
      а не старый. Трогать его не понадобилось.
      **Опасение, что галка глушила данные, не подтвердилось.** Визиты
      15:38–16:25 пришли ещё до правки адреса (она сделана около 16:40),
      то есть счётчик принимал `th.neva.beauty` и со старым адресом в настройках.
      Правка всё равно нужна была — ради ссылок в отчётах, привязки к Вебмастеру
      и защиты от перехвата домена, — но неработающей статистики она не чинила.
      **Хвост на посмотреть:** один просмотр с `doctor-cosmetolog.pro` сегодня
      всё-таки был, хотя на парковке Рег.ру счётчика 99850063 нет (проверено
      в задаче 71). Скорее всего у кого-то в браузере осталась открытой или
      в кэше старая страница Tilda и она добила хит. С сегодняшнего дня такие
      хиты отсекает галка «только с указанных адресов». Если через неделю
      просмотры со старого домена продолжатся — искать источник всерьёз.
- [x] 69. ~~Завести тайский номер WhatsApp вместо российского~~ — закрыта
      отказом заказчика 2026-08-31: WhatsApp зарегистрирован на российском
      `+7 999 028-91-15`, второго номера не будет.
      Предлагалось ради локального сигнала доверия: тайский бизнес с тайским
      номером выглядит для клиента на острове привычнее.
      Следствия по сайту: `contacts.whatsapp_number`, `whatsapp_url`
      и `business.telephone` остаются на `+7`, править нечего.
      Снимается оговорка задачи 97: там телефон убрали из полосы контактов
      с пометкой «когда появится тайский номер, решение стоит пересмотреть».
      Номер не появится — значит, решение окончательное: в полосе стоят два
      мессенджера, телефонной ссылки в ней нет. Сам номер остаётся кликабельным
      в блоке записи и в разметке организации.
      Заодно 2026-08-31: **Line не используем.** Вопрос заводился потому, что
      в Таиланде это основной мессенджер и тайские клиенты часто пишут только
      туда; третьей кнопки в полосе контактов не будет. Связь с салоном —
      WhatsApp, Telegram и Instagram, все три работают откуда угодно.
- [x] 70. Обновить ссылку на сайт в Instagram и других профилях.
      **Сделано заказчиком 2026-08-31.**
      «Другие профили» отпали сами: Google Business Profile не заводим (65),
      Line не используем (69), Facebook и TikTok салон не ведёт. Instagram —
      единственная внешняя точка, он же единственная ссылка в `business.same_as`.
      Значит пункт закрыт целиком, а не наполовину.
      Срочность, из-за которой пункт торопили, снята: в шапке профиля больше
      не стоит `doctor-cosmetolog.pro`, а тот домен не продлевается и уходит
      на перехват (задача 71). Перехватить теперь могут пустой адрес, на который
      салон ниоткуда не ссылается.
      **Своими глазами не проверял, и это стоит записать честно.** Instagram
      отдаёт профиль только под логином: и обычный запрос страницы, и API
      `users/web_profile_info` возвращают `require_login`, независимо от
      заголовков. Проверить можно только браузером под аккаунтом. Тем же
      упирается и проверка задачи 64 — что аккаунт действительно переименован
      в `th.neva.beauty`, — а эта ссылка уже стоит в проде на всех 24 страницах.
- [x] 71. После запуска — отключить старый сайт на Tilda и снять домен
      `doctor-cosmetolog.pro`
      Проверено 2026-08-30: **отключать нечего, старый сайт отключился сам.**
      Домен `doctor-cosmetolog.pro` не продлён: NS — `ns1.expired.reg.ru` и
      `ns2.expired.reg.ru`, A-запись ведёт на парковку Рег.ру (194.67.71.122),
      не на Tilda. По http отдаётся страница «Срок регистрации домена истёк»
      с кнопками «Продлить» и «Перехватить»; https не работает вовсе —
      TLS рвётся, валидного сертификата на парковке нет. Следов Tilda
      в разметке нет ни одного.
      `robots.txt` парковки: `User-agent: *` / `Disallow: /`, в `<head>` ещё и
      `noindex, nofollow, noarchive`, `sitemap.xml` — 404. То есть старый сайт
      из индекса уйдёт сам, вслепую его отключать уже не придётся.
      **Но появилась плата, которой в задаче не было.** 301-редирект со старых
      адресов на `th.neva.beauty` поставить нельзя: домен не под нашим контролем,
      им распоряжается регистратор. В Вебмастере у старого сайта было 20 страниц
      в поиске — их ссылочный вес и переходы по старым адресам передать некуда.
      Дату истечения выяснить не вышло: WHOIS и RDAP с рабочей машины не
      отвечают, порт 43 и хосты RDAP режет VPN. Смотреть из другой сети или
      в кабинете Рег.ру, доступ к которому есть.
      **Решение за заказчиком, и оно срочное:** продлевать домен ради редиректа
      и защиты бренда или отпускать. Домен уже выставлен на «Перехватить» —
      после освобождения его может забрать кто угодно, включая чужой бизнес,
      и тогда старые ссылки на клинику приведут на чужой сайт.
      **Решение заказчика 2026-08-31: домен не продлевать, он больше не нужен.**
      Пункт закрыт: отключать было нечего, а снятие домена происходит само —
      регистрация не продлевается, дальше домен уходит на освобождение.
      Что принято вместе с этим решением:
        * 301-редиректа со старых адресов не будет никогда. 20 страниц старого
          сайта, что были в поиске, уходят из индекса вместе с доменом,
          ссылочный вес передать некуда. Новый сайт растёт с нуля — это уже
          заложено в задачах 66–68: ресурсы заведены, sitemap отправлен;
        * домен может забрать кто угодно, включая чужой бизнес. Старая визитка,
          старые ссылки и упоминания в сторонних каталогах после этого приведут
          не туда. Отсюда же срочность задачи 70: ссылку на сайт в профилях
          надо заменить до того, как домен подберут, а не после.
      В репозитории править нечего: в собранном сайте нет ни одного упоминания
      `doctor-cosmetolog.pro` (проверено поиском по всем 24 страницам), запрет
      на эту строку стоит воротами сборки ещё с плана этапа 0–4. Упоминания
      остались только в TODO, спеке и плане — это история, её не трогаем.
      **Хвост на потом, мелкий:** в Яндекс.Вебмастере остаётся ресурс
      `https://doctor-cosmetolog.pro` (21 страница, 20 в поиске), в Search
      Console — такой же. Когда домен освободится, цифры в них станут мёртвыми,
      а после перехвата — чужими. Удалять ресурсы прямо сейчас незачем: пока
      они живы, по ним видно, что именно уходит из индекса и подхватывает ли
      это `th.neva.beauty`. Вернуться, когда домен сменит владельца.
- [x] 73. ~~Отзывы клиентов~~ — закрыта отказом заказчика 2026-08-31.
      Было: на сайте ноль отзывов, ссылки на внешний источник тоже нет.
      Предлагалось начать с Google Business Profile (задача 65) и просьбы
      к постоянным клиентам оставить отзыв.
      Решение заказчика: отзывов на сайте не будет.
      Следствия: блока отзывов, ссылки на внешний источник и разметки
      `Review`/`AggregateRating` на сайте нет и не появится. Придумывать их
      было нельзя и раньше: `Review` без реальных отзывов — прямое нарушение
      правил Google. **Тему заново не поднимать.**
- [x] 74. Рассказать, кто работает в салоне — закрыто отказом заказчика.
      Было предложено: имя, специализация, образование или стаж и фото хотя бы
      по одному мастеру на направление, раз за 23 страницы нет ни одного
      человека, только безличный «мастер».
      Решение заказчика: мастеров на сайте не представляем совсем — состав часто
      меняется, смысла в этом нет. Отдельно и раньше отклонено упоминание
      образования и стажа: косметолог с высшим медицинским, но доказывать
      профпригодность он никому не должен, а упоминание таких вещей, по опыту
      заказчика, само рождает у клиента сомнения.
      Следствия: имён, фото и регалий мастеров нет ни в текстах, ни в FAQ, ни в
      schema.org; безличный «мастер» остаётся сознательно. Год начала работы
      салона на Самуи заказчик тоже отклонил, хотя это факт про салон, а не про
      людей, — от задачи не осталось ничего, в опросник она не вошла.
      **Тему заново не поднимать.**
- [x] 75. ~~Фото работ «до/после»~~ — закрыта отказом заказчика 2026-08-31.
      Было: все 24 кадра сайта показывают процесс, ни одного результата;
      для окрашивания и коррекции фигуры это главный аргумент. Требовалось
      10–15 кадров с письменным согласием клиентов.
      Решение заказчика: снимков «до/после» не будет.
      Следствия: галереи результатов нет, страницы услуг остаются на кадрах
      процесса. Перманентный макияж, самая требовательная к «до/после» услуга,
      с сайта снят задачей 94, так что и повода вернуться стало меньше.
      **Тему заново не поднимать.**
- [x] 76. ~~Безопасность инъекций и татуажа~~ — закрыта отказом заказчика
      2026-08-31.
      Было: предлагалось подтвердить и вынести на страницы, кто делает
      ботулинотерапию (врач или косметолог), оригинальные ли препараты,
      вскрывают ли флакон при клиенте, какие материалы одноразовые.
      Решение заказчика: раздела о безопасности на сайте не будет.
      Следствия: `/botulinoterapiya/` остаётся без блока о препаратах и
      стерильности. Писать это самим было нельзя: без подтверждения от салона
      любая такая строка — обещание за него. Смыкается с задачей 74 — кто
      именно проводит процедуру, на сайте не называется сознательно.
      Татуажа вопрос больше не касается: перманентный макияж снят с сайта
      задачей 94. **Тему заново не поднимать.**
- [x] 77. Длительность процедур и что входит в цену. Ни в одной из 204 позиций
      прайса нет времени, 169 позиций идут без единого слова пояснения. Нужны
      время по группам позиций и короткая строка «что входит» хотя бы по дорогим.
      Ответы салона получены 2026-08-11 — две заполненные анкеты опросника.
      Друг другу они почти не противоречат: где ответил один, второй чаще
      промолчал. Выгрузка лежит в `docs/questionnaire/`, в git не идёт.
      Сделано: на странице услуги над прайсом появилась полоска `.svc-facts` —
      две строки, «Сколько длится» и «Что входит в цену». Место выбрано так,
      чтобы факт читался там же, где человек смотрит на цену, а не в аккордеоне
      FAQ внизу. Данные — поля `duration` и `included` в `content.yml`,
      разметка в `service.html.j2`, стили в `sources/css/service.css`.
      Длительность встала на 10 страниц, «что входит» — на 4 (лазерная
      эпиляция, РФ-лифтинг, эндосфера, массаж). Проверено на 390, 560 и 1280:
      до 560 px подпись и значение идут в столбик, дальше — в две колонки,
      длинный текст (пять процедур в строке «Уход за волосами») переносится
      и никуда не вылезает. Ворота зелёные: 204 позиции, 25 страниц, 85 тестов.
      **Пункт не закрыт: пять услуг остались без длительности, и это находка,
      а не недоделка.** Сверка ответов с тем, что уже написано на сайте,
      показала расхождения — а нынешние цифры сайта не выдуманы при сборке,
      они взяты из выгрузки Tilda, то есть со старого сайта самого салона:
        * лазерная эпиляция — на сайте «от 10 до 60 минут», в анкете
          «малая зона 30, крупная 60–90»;
        * SMAS-лифтинг — на сайте «от 30 до 90 минут», в анкете «1,5–2 часа»;
        * эндосфера — время стоит **в самих строках прайса** (всё тело 1 час,
          ноги 45 минут, живот 30), в анкете «90 минут»;
        * массаж — так же в прайсе (тело 120 минут, лицо и антицеллюлитный
          по 60), в анкете «60–90 минут».
      У эндосферы и массажа полоска встала бы прямо над таблицей, которая
      говорит другое, — две разные цифры на одном экране хуже, чем ни одной.
      Поэтому принято правило: **полоска ставится только там, где своего числа
      на странице ещё нет.**
      Отдельная находка: «от 15 до 30 минут» у ботулинотерапии в выгрузке Tilda
      не нашлось — число появилось при сборке сайта и источника не имеет.
      Сейчас оно висит в проде в блоке преимуществ и в FAQ.
      Что спросить у салона (одним списком, вместе с вопросами по 79 и 80):
        1. Четыре расхождения выше — какая цифра верная. Для эндосферы и
           массажа это заодно вопрос, верен ли прайс.
        2. Ботулинотерапия — сколько на самом деле, чтобы убрать число без
           источника. В анкете «не более 60 минут», на сайте «15–30».
        3. Стрижка, Davines и перманентный макияж — в анкете «индивидуально».
           Полоску с таким словом не ставили: это тот же уход от ответа,
           от которого уходим. Нужна хотя бы вилка.
        4. «Что входит» по кератину с биозавивкой и по удалению тату — ответ
           «Входит» без уточнения, а вопрос перечислял три пункта (мытьё,
           укладка, домашний уход). Что именно входит.
        5. Перманентный макияж, «что входит» — ответ «не указываем», хотя
           вопрос был про коррекцию: входит ли она в цену. Это главный вопрос
           клиента по самой дорогой услуге, стоит вернуться к нему.
      Список собран в письмо 2026-08-25:
      `docs/questionnaire/2026-08-25-salon-followup.md`. Десять вопросов —
      пять пунктов выше плюс расхождение цены Davines (задача 91); формой
      не оформляли, восемь вопросов из десяти сводятся к выбору между двумя
      уже названными числами. Письмом же снят пятый пункт: в прайсе есть строки
      «2-я процедура (1-2 мес)» — 3000 ฿ за брови, губы и стрелки, 2500 ฿
      за межресничное, — то есть коррекция платная и делается через один-два
      месяца, и салона спрашиваем не «как у вас», а «верно ли прочитали ваш
      прайс». Это же закрывает интервал, которого не хватило задаче 80.
      **Ответы заказчика 2026-08-25 по первым шести вопросам** — в каждом назван
      источник, которому верим. Выбор шёл между двумя данными самого салона
      (анкета против прайса или против старого сайта), выдуманного нет ничего.
        1. Лазерная эпиляция — анкета. На сайте было «от 10 до 60 минут»
           (с Tilda), стало «Малая зона — 30 минут, крупная — 60–90 минут».
        2. SMAS-лифтинг — анкета. Было «от 30 до 90 минут», стало «1,5–2 часа».
        3. Эндосфера — прайс. Правок нет: время уже стоит в строках прайса
           (тело 1 час, ноги с бёдрами 45 минут, живот 30).
        4. Массаж — анкета, **но правку не делаем**, вопрос вернулся салону.
           «60–90 минут» противоречит прайсу, где время стоит в самих названиях
           позиций: «Массаж тела — 120 минут — 1500 ฿», лицо и антицеллюлитный
           по 60 минут. Это не текст, а строки прайса и цены за них: поставить
           рядом второе число значит выпустить в прод противоречие, а переписать
           прайс без прямого «да» салона нельзя — меняется, за что платят 1500 ฿.
        5. Ботулинотерапия — анкета. Было «от 15 до 30 минут» — единственное
           число на сайте без источника, — стало «Не больше часа». Дыра закрыта.
        6. Стрижка, Davines, перманентный макияж — анкета, то есть
           «индивидуально». На страницу такое не ставим (см. выше), значит
           времени у этих трёх услуг на сайте нет вовсе. Вернуться можно только
           с вилкой «от … до …».
      Что сделано в коде по 1, 2 и 5: у каждой услуги правка разошлась по трём
      местам — полоска `.svc-facts` над прайсом, блок преимуществ и ответ FAQ,
      плюс `intro`, из которого собирается `description` страницы. У всех трёх
      строка «Сколько длится» в полоске появилась впервые; поле `duration`
      добавлено в `content.yml`. Старых чисел в собранном сайте не осталось
      (проверено поиском по всем 25 страницам), других страниц с временем этих
      трёх услуг нет — сверил все тексты `content.yml`, где рядом встречаются
      название услуги и единица времени. Ворота: 204 из 204 цен, 25 страниц,
      85 тестов. Правку ширин не гонял: значения короче самого длинного уже
      проверенного в задаче 79 («Не загорать 4–6 дней до сеанса, 7 дней после»),
      которое стоит на той же странице лазерной эпиляции.
      **Открытыми остаются пять вопросов из десяти** — 4, 7, 8, 9 и 10:
      длительность массажа, что входит в кератин и биозавивку, что входит
      в удаление тату, коррекция перманентного макияжа и цена Davines
      (задача 91). Текст для отправки обновлён и сокращён до этих пяти
      в `docs/questionnaire/2026-08-25-salon-followup.md`.
      **Ответы заказчика 2026-08-26 — оставшиеся пять вопросов, пункт закрыт.**
        4. Массаж — «цена как на Tilda». Источник прайс, а не анкета: «Массаж
           тела — 120 минут — 1500 ฿», лицо и антицеллюлитный по 60 минут.
           Вилку «60–90 минут» из анкеты не используем и полоску над таблицей
           не ставим — время уже стоит в самих названиях позиций. Правок нет.
        7. Кератин и биозавивка — «всё входит в цену, кроме средства с собой».
           То есть мытьё головы, стрижка и укладка после процедуры входят,
           средство для домашнего ухода — нет. Поле `included` добавлено обеим
           услугам. У биозавивки это сходится с прайсом, где стрижка названа
           в каждой позиции («Короткие волосы со стрижкой»); у кератина прайс
           идёт по длине волос и про стрижку молчал — теперь сказано.
        8. Удаление тату — «ничего не входит, всё оплачивается отдельно».
           Написано положительной формулировкой, чтобы строка отвечала своему
           заголовку «Что входит в цену»: «Только сам сеанс. Уход после,
           средства для дома и повторный осмотр оплачиваются отдельно.»
           Это осознанно расходится с РФ-лифтингом, где уход и повторный осмотр
           в цену входят: услуги разные, оба факта названы салоном.
        9. Перманентный макияж — салон больше не оказывает услугу, страница
           снята с сайта целиком. Вопрос про коррекцию отпал вместе с ней.
           Сделано в задаче 94.
        10. Davines — 1500 ฿ везде. Сделано в задаче 91.
      Итог по пункту: длительность стоит у 13 услуг из 16, «что входит» —
      у 7 из 16. Без длительности остались три, и у каждой есть причина:
      Davines и стрижка — салон отвечает «индивидуально», а такое на страницу
      не ставим; у эндосферы и массажа время стоит в самих строках прайса.
- [x] 78. Правила отмены и переноса записи. Сайт не отвечает на «а что если»:
      можно ли перенести, нужна ли предоплата. Нужна формулировка от салона.
      Сюда же — по какому праву работает салон: политика конфиденциальности
      перенесена с Tilda и ссылается на российский 152-ФЗ.
      Вопрос о праве закрыт отдельно задачей 87: политика переписана под факты
      салона, 152-ФЗ и Роскомнадзор ушли, право — Таиланд.
      Данные — те же две анкеты от 2026-08-11, что закрыли задачи 77, 79 и 80.
      Правила от салона: отмена и перенос без последствий за сутки; при опоздании
      процедуру сокращают; при неявке предоплата не возвращается; предоплата 20 %
      и только на курсы и дорогие процедуры; оплаченные сеансы курса эндосферы
      и массажа остаются у клиента бессрочно.
      Сделано. Правило переноса разложено по двум местам, потому что у него две
      разные аудитории:
      1. Строка `booking_rule` в `site.yml` — под кнопкой записи на всех
         23 страницах с блоком записи (`booking__rule` в `partials/booking.html.j2`).
         Клиент из поиска попадает сразу на услугу и записывается с неё, главную
         он не открывает, — правило должно стоять там, где он нажимает кнопку.
         Ключ лежит в `site.yml`, а не в `content.yml`: это константа бизнеса,
         одна на весь сайт, а не текст конкретной страницы. В `cta_inline()`
         строка не пошла намеренно: это короткий призыв в потоке текста, условия
         в нём читаются как мелкий шрифт под подписью.
      2. Два новых вопроса в FAQ главной — «Можно ли перенести или отменить
         запись?» и «Нужна ли предоплата при записи?». Они же уходят в `FAQPage`
         schema.org, ради чего пункт и заводился: подробности (опоздание, неявка,
         размер предоплаты) в одну строку под кнопкой не влезают, а в ответ ИИ
         попадают только из разметки. Проверено: оба вопроса есть и в `FAQPage`,
         и заголовками на странице — `check_faq` сверяет их слово в слово.
      Бессрочность оплаченных сеансов ушла не на главную, а в FAQ эндосферы
      и массажа: салон отвечал про курсы этих двух услуг, и распространять
      правило на все курсы сайта было бы домыслом. На эндосфере она попала
      в ответ «Успею ли пройти курс, если я на Самуи в отпуске?» — это сильнейший
      аргумент для туриста: недобранный курс можно закончить в следующий приезд.
      Опубликовано с оговоркой: «дорогие процедуры» в анкете не определены,
      поэтому ответ говорит «нужна ли предоплата в вашем случае — скажут при
      записи». 20 % это конкретное число, а порог в батах за салон выдумывать
      нельзя. → открытый вопрос салону, к пяти вопросам задачи 77.
      Не делалось намеренно: страница `/zapis/` — четыре факта не наполняют
      26-ю страницу, а тонкая страница вредит. Правил нет и в JSON-LD:
      у `Service` поля под условия отмены не существует, а `Reservation`
      к салону не применим.
      CSS — три строки в `components.css`: правило и локация поставлены вплотную
      друг к другу и отделены от строки запасных каналов, иначе три подписи идут
      равным шагом и читаются как один список.
      Проверки: 204 из 204 цен, 25 страниц, 85 тестов. Вёрстка блока записи
      на 390, 768 и 1440: строка в две строки на мобильном и в одну на десктопе,
      горизонтальной прокрутки нет.
      Находка вне задачи (→ 90): на 1024 px горизонтальная прокрутка есть,
      и была до этих правок — проверено пересборкой на HEAD. Виновник
      `.site-header__cta`, страница шире окна на 52 px. Диапазон узкий,
      1024–1076 px, дальше проходит. Аудит вёрстки в задаче 16 гонял 390, 768
      и 1440, ровно 1024 никто не смотрел, а это точка, где кнопка записи
      и меню в шапке впервые появляются — и не влезают.
- [x] 79. Сроки «когда можно на солнце и в море» после каждой процедуры. Вопрос
      честно вынесен в FAQ на 11 страницах, и ни в одном ответе нет срока — ни
      «сутки», ни «3 дня», ни «неделя». Для острова это главный вопрос: клиент
      планирует процедуру между пляжными днями. Нужен список «процедура → сколько
      дней без активного солнца и моря» от того, кто их делает. Найдено в задаче 52.
      Данные — те же две анкеты от 2026-08-11, что закрыли задачу 77.
      Сделано: в полоске `.svc-facts` над прайсом появилась строка «Солнце и море»
      (поле `sun` в `content.yml`, иконка `sun`), она встала на 8 страницах:
        * лазерная эпиляция — не загорать 7 дней до, 7 дней после;
        * электроэпиляция — 7 дней после;
        * игольчатый РФ-лифтинг — неделя до и неделя после;
        * SMAS-лифтинг — 10 дней после;
        * удаление тату — не загорать 4–6 дней до, 7 дней после;
        * фотоомоложение M22 — не загорать 7 дней до, две недели после;
        * чистки и пилинги — 5–7 дней после;
        * уход за волосами — окрашиванию и осветлению солнце и море не мешают.
      Плюс 9 ответов FAQ, которые раньше задавали вопрос и не отвечали, теперь
      называют срок: лазер, РФ-лифтинг, SMAS, удаление тату, M22, чистки и три
      раздела — «Эпиляция», «Аппаратная косметология», «Косметология». Это же
      уходит в `FAQPage` schema.org, ради чего пункт и заводился.
      Чего на сайте нет намеренно: шугаринг, кератиновое выпрямление, биозавивка,
      Tokio Inkarami, перманентный макияж, ботулинотерапия, эндосфера и массаж —
      по всем восьми салон ответил «не указываем». Это отказ публиковать, а не
      пропуск, и выдумывать за салон сроки нельзя. В ответе раздела «Эпиляция»
      это сказано прямо: по сахарной эпиляции отдельного срока салон не называет.
      Единственная двусмысленность — «4–6» без единиц у загара перед удалением
      тату — снята заказчиком 2026-08-17: это дни.
- [x] 80. Интервал между сеансами в курсовых процедурах. Число сеансов названо
      у пяти услуг, а интервал — ни в одном из 163 ответов. Для лазерной эпиляции,
      удаления тату, фотоомоложения и эндосферы это решающий факт: от него зависит,
      влезет ли курс в поездку. Нужны интервалы по каждой курсовой услуге.
      Найдено в задаче 52.
      Сделано: вторая новая строка полоски — «Интервал в курсе» (поле `course`,
      иконка `repeat`, добавлена из lucide), она встала на 7 страницах:
        * лазерная эпиляция — раз в 1,5–2 месяца, курс 8–10 сеансов;
        * электроэпиляция — следующий сеанс, когда волосы отрастут;
        * удаление тату — раз в месяц, до 15 сеансов;
        * фотоомоложение M22 — раз в месяц;
        * игольчатый РФ-лифтинг — раз в месяц;
        * эндосфера-терапия — два раза в неделю;
        * массаж — два-три раза в неделю.
      Ещё 9 ответов FAQ получили интервал. Три из них теперь считают за клиента
      то, ради чего пункт и заводился, — влезет ли курс в поездку: курс эндосферы
      из 10 сеансов по два раза в неделю занимает около пяти недель, курс массажа
      из 10 — три-пять недель, а полный курс лазерной эпиляции идёт год и дольше.
      Из-за последнего в разделе «Эпиляция» пришлось поправить и старую фразу:
      раньше там обещали «один-два сеанса лазера за поездку», но при интервале
      1,5–2 месяца второй сеанс в поездку не попадает — осталось «один сеанс».
      Перманентный макияж остался без интервала до коррекции: ответ «не указываем».
      Вопрос про коррекцию уже висит открытым в задаче 77 (пункт 5) — там же и
      останется, отдельного пункта не заводим.
      Общее по 79 и 80: CSS не понадобился, `.svc-facts__row` работает на любом
      числе строк. Проверено на 390, 560 и 1280 (три ширины сразу, через iframe —
      окно браузера ресайзу не поддалось): до 560 px подпись и значение идут
      в столбик, дальше в две колонки, самое длинное значение — «Не загорать 4–6
      дней до сеанса, 7 дней после — без активного солнца и моря» — переносится
      на три строки и никуда не вылезает. Ворота зелёные: 204 позиции, 25 страниц,
      85 тестов.
      Побочное: четыре страницы (`biozavivka-volos`, `keratinovoe-vypryamlenie-volos`,
      `saharnaya-epilyaciya`, `tokio-inkarami`) изменились только пустыми строками —
      шаблон полоски переписан с четырёх копий блока на цикл. Содержания это
      не трогает, но `page_digest` считает байты, поэтому у них обновился `lastmod`
      в `sitemap.xml`. Ложный сигнал на четыре адреса, сайт всё равно снят с сети
      и после возврата будет переобойден целиком.
- [x] 81. ~~Довести `www.th.neva.beauty`~~ — закрыта отказом: адрес не нужен.
      Было: домен не резолвился вообще, кто набирал адрес с www — видел ошибку
      соединения. Найдено в задаче 53.
      Сделано заказчиком 2026-08-10: CNAME `www.th` → `jw-git-hub.github.io`.
      Проверено: запись разошлась (`8.8.8.8` и `1.1.1.1` отдают CNAME),
      `http://www.th.neva.beauty` → `301` → `https://th.neva.beauty/`.
      Осталось: сертификат Pages выпущен 3 августа, до появления записи, и
      покрывает только `th.neva.beauty` — в API
      `https_certificate.domains = ["th.neva.beauty"]`. На запрос по `https://www`
      отдаётся запасной `*.github.io`, браузер показывает ошибку сертификата.
      Обычно Pages включает в сертификат и www-вариант, но только если DNS-запись
      существовала в момент выпуска.
      Ожидание не сработало: 2026-08-13, через трое суток после появления
      записи, в API всё те же `domains: ["th.neva.beauty"]`. DNS при этом
      исправен с обеих сторон — и апекс, и `www` отдают `jw-git-hub.github.io`
      и четыре IP Pages.
      Ручной перевыпуск через API попробован 2026-08-13 и не помог. Домен снят
      (`PUT .../pages` с пустым `cname`) и вписан обратно, разрыв 40 секунд.
      Сайт пережил это без потерь: главная, `/privacy/` и `/kosmetologiya/`
      отвечают 200, `http://www.th.neva.beauty` → `301` → `https://th.neva.beauty/`,
      `https_enforced` остался `true` и выключать его не пришлось.
      Но сертификат вернулся тот же самый — `state: approved`,
      один домен, та же дата `expires_at: 2026-11-01`. То есть GitHub не выпустил
      новый, а переиспользовал существующий: записи сертификата между снятием и
      возвратом домена не исчезло. Дальше 20 замеров раз в минуту (15:23–15:42)
      — `domains` не менялись ни разу, `https://www` всё это время 000.
      **Вывод: через API этот путь не работает, повторять его бесполезно.**
      Что осталось: те же шаги, но в вебе — Settings → Pages → снять Custom
      domain, сохранить, вписать `th.neva.beauty` заново. Веб-интерфейс иногда
      чистит состояние глубже, чем API. Делать не в час пик и потом проверить,
      что `Enforce HTTPS` снова включён. Если и это не даст перевыпуска —
      писать в поддержку GitHub: со стороны DNS всё правильно, доказательства
      выше.
      Влияние на посетителей всё это время нулевое по http и ломающее по https:
      кто наберёт `www.th.neva.beauty` с явным `https://`, увидит ошибку
      сертификата. Без схемы браузер идёт по http и попадает на редирект.
      Надежда из задачи 89 не оправдалась. Сайт Pages был удалён целиком
      2026-08-13 и создан заново 2026-08-26 — то есть домен отсутствовал
      не 40 секунд, а тринадцать суток, и записи сайта в этот момент не было
      вообще. Всё равно вернулся тот же сертификат: `approved`,
      `domains: ["th.neva.beauty"]`, `expires_at: 2026-11-01`. Значит запись
      сертификата живёт не при сайте Pages, а при домене, и удалением сайта
      её не сбросить. Проверено сразу после возврата:
      `https://www.th.neva.beauty` — 000 (ошибка TLS),
      `http://www.th.neva.beauty` → 301 → `https://th.neva.beauty/`.
      Остаётся то же, что и было: снять и вписать домен через веб-интерфейс,
      а если не поможет — поддержка GitHub. Ждать выпуска «само» смысла нет,
      это уже третья проверка.

      **Закрыто 2026-08-30 отказом. Задача всё это время была не про починку.**
      Причина, которую стоило найти четырьмя днями раньше: GitHub Pages обещает
      автоматически защитить `www`-вариант **только для апекс-домена** — настроил
      `example.com`, получил и `www.example.com`. Наш кастомный домен
      `th.neva.beauty` апексом не является (апекс здесь `neva.beauty`), это обычный
      поддомен, и прилепить к нему вторую приставку Pages не умеет. Про поддомены
      документация не говорит «не выпускаем» — она про них молчит, но три
      независимые попытки с одинаковым исходом (перевыпуск через API, снятие
      и возврат домена, полное удаление сайта Pages на 13 суток) складываются
      в тот же ответ.
      **Решающий довод — сосед.** У `vn.neva.beauty` `https://www.vn.neva.beauty`
      точно так же не открывается, и записи `www.vn` там нет вовсе. То есть
      состояние, которое считалось на vn нормальным, и есть то, чего мы
      добивались на th. Чинили то, чего нет и у образца.
      Решение заказчика 2026-08-30: убрать CNAME `www.th` в reg.ru и привести th
      к тому же виду, что vn. Запись удаляет заказчик, доступ к reg.ru у него.
      Плата: сейчас `www.th.neva.beauty` без схемы уходит по http на 301
      к основному домену, после удаления записи браузер скажет «адрес не найден».
      На vn всегда было именно так, жалоб не было. Обратная сторона — исчезает
      предупреждение о недоверенном сертификате, которое видел каждый, кто
      набирал адрес с `https://www`.
      В репозитории ничего править не пришлось: упоминаний `www.th` нет ни в одной
      странице, ни в `sitemap.xml`, ни в `canonical`, ни в `site.yml`.
      Побочно снимается оговорка из задачи 66: там ресурс с префиксом URL был взят
      ценой того, что `www` в него не попадёт. Раз `www` больше не существует,
      цены нет.
      **Повторять попытки перевыпуска бессмысленно — это не сбой, а устройство.**
